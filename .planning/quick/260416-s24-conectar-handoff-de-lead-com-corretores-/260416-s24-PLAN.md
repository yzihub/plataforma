---
quick_id: 260416-s24
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/LeadsClient.tsx
  - src/components/yzihub/LeadDrawer.tsx
autonomous: true
requirements:
  - S24-01  # Carregar corretores ativos via API /api/brokers em LeadsClient
  - S24-02  # Exibir nome (não UUID) ao selecionar corretor no LeadDrawer (TabDados)
  - S24-03  # Salvar assigned_to (UUID) no lead via PATCH existente
  - S24-04  # Patch mínimo — não quebrar CorretorCard, QuickActions, fluxo de save, Kanban, DataTable

must_haves:
  truths:
    - "Ao abrir um lead no drawer, a seção 'Corretor Responsável' lista apenas corretores ativos do tenant"
    - "Na tab Dados, o campo 'Corretor responsável' é um <select> com nomes de corretores (não um input de UUID cru)"
    - "Selecionar um corretor e salvar atualiza lead.assigned_to com o UUID do corretor escolhido no Supabase"
    - "Se um lead já tem assigned_to, o drawer exibe o nome do corretor correspondente (não o UUID)"
    - "Clientes sem corretores ativos não quebram — select mostra apenas '— Sem corretor —'"
  artifacts:
    - path: "src/components/yzihub/LeadsClient.tsx"
      provides: "Fetch de /api/brokers no client, mapeamento para Corretor[], filtro is_active=true"
      contains: "useEffect.*fetch.*api/brokers"
    - path: "src/components/yzihub/LeadDrawer.tsx"
      provides: "Select de corretores no TabDados substituindo o input de UUID"
      contains: "corretores.map"
  key_links:
    - from: "src/components/yzihub/LeadsClient.tsx"
      to: "/api/brokers"
      via: "fetch em useEffect(mount)"
      pattern: "fetch.*\"/api/brokers\""
    - from: "src/components/yzihub/LeadDrawer.tsx (TabDados)"
      to: "form.assigned_to"
      via: "<select> onChange → set('assigned_to', value) → PATCH /api/leads/:id"
      pattern: "select.*assigned_to|onChange.*assigned_to"
---

<objective>
Conectar o handoff de lead com corretores ativos reais do tenant.
Atualmente `LeadsClient` passa `corretores: Corretor[] = []` hardcoded, então o `CorretorCard` do drawer nunca aparece (guard `corretores.length > 0`) e o campo "Corretor responsável" no TabDados é um input de texto que espera UUID cru.

Purpose: Permitir que o operador atribua um corretor a um lead selecionando pelo **nome** em um select, salvando o UUID em `assigned_to`. Reaproveitar a API `/api/brokers` (já retorna corretores com `is_active`) e o `CorretorCard` (já existe, já usa `onLeadSaved`). Patch mínimo, sem tocar em backend.
Output: Dois arquivos editados. Handoff funcional no drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/components/yzihub/LeadsClient.tsx
@src/components/yzihub/LeadDrawer.tsx
@src/app/api/brokers/route.ts

<interfaces>
<!-- Contratos já existentes na codebase — usar direto, sem exploração adicional. -->

From src/components/yzihub/LeadsClient.tsx (linha 14-19):
```typescript
export type Corretor = {
  id: string;       // UUID do corretor
  name: string;     // nome de exibição
  phone?: string;
  email?: string;
};
```

From src/app/api/brokers/route.ts (GET retorna):
```typescript
// Array JSON com linhas da tabela `corretores`:
{
  id: string;
  tenant_id: string;
  name: string;
  phone: string | null;
  email: string | null;
  role: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}[]
```
Ordenado por is_active DESC, created_at DESC. Filtrado por tenant_id via profiles (gatekeeper).

From src/components/yzihub/LeadDrawer.tsx (CorretorCard, linha 295-376):
- Já recebe `corretores: Corretor[]` e `lead: Lead`
- Já renderiza nome, phone, email do corretor selecionado
- Já tem modo "Trocar/Atribuir" com <select> por nome
- Já chama `onLeadSaved({ ...lead, assigned_to: selectedId || null })`
- Guard em LeadDrawer linha 1048: `{lead && corretores.length > 0 && <CorretorCard ... />}` — desaparece se corretores estiver vazio

From src/components/yzihub/LeadDrawer.tsx (TabDados, linha 548-551):
```tsx
<div>
  <label className={LABEL_CLS}>Corretor responsável (ID)</label>
  <input type="text" value={form.assigned_to} onChange={(e) => set("assigned_to", e.target.value)} className={INPUT_CLS} />
</div>
```
Este input cru é o que precisa virar <select> com nomes.

From src/components/yzihub/LeadDrawer.tsx (handleSave, linha 404-443):
- Já envia payload completo (incluindo assigned_to) via PATCH /api/leads/:id
- Não precisa mudar — o select só precisa atualizar `form.assigned_to` com UUID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch de corretores ativos em LeadsClient e propagar para LeadDrawer/LeadsDataTable</name>
  <files>src/components/yzihub/LeadsClient.tsx</files>
  <action>
Em `src/components/yzihub/LeadsClient.tsx`:

1. Adicionar `useEffect` ao componente `LeadsClient` (requer importar `useEffect` junto com `useState`, `useMemo`):
```tsx
import { useState, useMemo, useEffect } from "react";
```

2. Substituir a linha atual `const corretores: Corretor[] = [];` (linha ~57) por estado real:
```tsx
const [corretores, setCorretores] = useState<Corretor[]>([]);

useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const res = await fetch("/api/brokers", { cache: "no-store" });
      if (!res.ok) return;
      const data = await res.json();
      if (!Array.isArray(data) || cancelled) return;
      const active: Corretor[] = data
        .filter((b: { is_active?: boolean }) => b.is_active !== false)
        .map((b: { id: string; name: string; phone?: string | null; email?: string | null }) => ({
          id: b.id,
          name: b.name,
          phone: b.phone ?? undefined,
          email: b.email ?? undefined,
        }));
      setCorretores(active);
    } catch (err) {
      console.error("[LeadsClient] falha ao carregar corretores:", err);
    }
  })();
  return () => { cancelled = true; };
}, []);
```

3. Remover o comentário `// TODO: fetch from /api/corretores — currently empty; integration planned for future plan` (linha ~56).

**Não alterar:** JSX de `LeadsDataTable` (já recebe `corretores`), JSX de `LeadDrawer` (já recebe `corretores`), filtros, handlers, kanban. O array `corretores` agora passa a ser populado via estado.

**Por quê `/api/brokers` e não `/api/corretores`:** A rota existente é `src/app/api/brokers/route.ts` (GET) — já filtra por tenant via `profiles` e ordena `is_active DESC`. Reaproveitar.

**Por quê filtrar `is_active !== false` (não `=== true`):** Compatibilidade com linhas antigas onde `is_active` pode vir `null` antes da migration `dj5` — tratar como ativo por default.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
- `corretores` em `LeadsClient` é `useState` populado via fetch `/api/brokers` no mount
- Apenas corretores com `is_active !== false` são mapeados
- Mapeamento preserva `id`, `name`, `phone`, `email` (phone/email viram `undefined` se null)
- TypeScript compila sem erros novos
- Nenhuma regressão em `LeadsDataTable`, `LeadsKanban`, `LeadDrawer` (todos ainda recebem `corretores` como prop)
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir input de UUID por select de corretor no TabDados</name>
  <files>src/components/yzihub/LeadDrawer.tsx</files>
  <action>
Em `src/components/yzihub/LeadDrawer.tsx`:

1. Adicionar `corretores` como prop opcional no componente interno `TabDados` (linha ~380-386). Mudar assinatura de:
```tsx
function TabDados({
  lead,
  onLeadSaved,
}: {
  lead: Lead | null;
  onLeadSaved?: (lead: Lead) => void;
}) {
```
para:
```tsx
function TabDados({
  lead,
  onLeadSaved,
  corretores = [],
}: {
  lead: Lead | null;
  onLeadSaved?: (lead: Lead) => void;
  corretores?: Corretor[];
}) {
```

2. Substituir o bloco do input de UUID (linha ~548-551) — atualmente:
```tsx
<div>
  <label className={LABEL_CLS}>Corretor responsável (ID)</label>
  <input type="text" value={form.assigned_to} onChange={(e) => set("assigned_to", e.target.value)} className={INPUT_CLS} />
</div>
```
por um select com nomes:
```tsx
<div>
  <label className={LABEL_CLS}>Corretor responsável</label>
  <select
    value={form.assigned_to}
    onChange={(e) => set("assigned_to", e.target.value)}
    className={SELECT_CLS}
  >
    <option value="">— Sem corretor —</option>
    {corretores.map((c) => (
      <option key={c.id} value={c.id}>{c.name}</option>
    ))}
  </select>
</div>
```

3. Propagar a prop `corretores` nas chamadas de `<TabDados ... />` dentro do render do `LeadDrawer` (linhas ~1075 e ~1079):
```tsx
{!lead && (
  <TabDados lead={null} onLeadSaved={onLeadSaved} corretores={corretores} />
)}
...
{lead && activeTab === "dados" && <TabDados lead={lead} onLeadSaved={onLeadSaved} corretores={corretores} />}
```

**Não alterar:**
- `CorretorCard` (já funciona, já aparece acima das tabs via guard `corretores.length > 0`)
- Fluxo `handleSave` / PATCH `/api/leads/:id` (já envia `assigned_to` no payload)
- Outras tabs (Conversas, Atividades, etc.)
- Assinatura pública de `LeadDrawerProps` (já tem `corretores?: Corretor[]`)

**Por quê manter o valor = UUID:** `form.assigned_to` continua sendo UUID para satisfazer o schema de `leads.assigned_to` (FK para `corretores.id`). O select apenas troca a **apresentação** (nome) mas o `<option value>` é o UUID. Zero mudança no backend.

**Edge case:** Se um lead tiver `assigned_to` apontando para um corretor agora `is_active=false` (não está na lista filtrada), o select mostrará "— Sem corretor —" e salvar o form vai sobrescrever o valor. Isso é aceitável para handoff — operador deve escolher um corretor ativo.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
- `TabDados` aceita prop opcional `corretores?: Corretor[]`
- Campo "Corretor responsável" é um `<select>` com `<option value={UUID}>{name}</option>`
- Primeira opção é `"— Sem corretor —"` com `value=""`
- Ambas chamadas de `<TabDados />` no render do Drawer passam `corretores`
- `handleSave` continua enviando `assigned_to` (UUID) no PATCH — nenhuma mudança
- `CorretorCard` continua funcionando como antes (exibe nome/phone/email do corretor atribuído)
- TypeScript compila sem novos erros
  </done>
</task>

</tasks>

<verification>
Smoke test manual (após implementação):
1. `rtk next build` ou `rtk tsc --noEmit` — zero erros de tipo
2. Abrir `/cockpit/crm` com usuário que tem ao menos 1 corretor ativo
3. Abrir um lead → confirmar que `CorretorCard` aparece (antes estava escondido por `corretores.length > 0`)
4. Abrir tab Dados → confirmar que campo "Corretor responsável" é um dropdown com nomes (não input de UUID)
5. Selecionar um corretor → clicar "Salvar alterações" → recarregar → `lead.assigned_to` persistido com UUID correto
6. Abrir console do browser → nenhum erro no fetch `/api/brokers`
</verification>

<success_criteria>
- [x] `LeadsClient` popula `corretores` via `/api/brokers` no mount
- [x] Apenas corretores `is_active !== false` são exibidos
- [x] TabDados mostra select com nomes, grava UUID em `assigned_to`
- [x] `CorretorCard` existente continua funcionando (exibe nome do corretor atribuído, permite trocar)
- [x] Save persiste `assigned_to` via PATCH existente — zero mudanças de backend
- [x] Nenhuma regressão em Kanban, DataTable, outras tabs do drawer
- [x] `rtk tsc --noEmit` sem novos erros
</success_criteria>

<output>
After completion, create `.planning/quick/260416-s24-conectar-handoff-de-lead-com-corretores-/260416-s24-SUMMARY.md` summarizing:
- Files changed (2)
- Key decisions (reaproveitar `/api/brokers`, filtrar `is_active !== false`, manter UUID como value)
- Edge cases tratados (corretor inativo referenciado por lead)
- Commit hash
</output>
