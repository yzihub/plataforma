---
phase: quick
plan: 260407-l5s
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/n8n-payloads.ts
  - src/app/api/leads/route.ts
  - src/app/api/imoveis/route.ts
  - src/app/api/contracts/route.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /api/leads retorna envelope N8nEnvelope<N8nLead> com entity, tenant_id, count, fetched_at, data[]"
    - "GET /api/imoveis retorna envelope N8nEnvelope<N8nProperty> com campos essenciais de imóvel"
    - "GET /api/contracts retorna envelope N8nEnvelope<N8nContract> — mantendo POST inalterado"
    - "Tipos TypeScript documentam exatamente o contrato de cada payload para consumo n8n"
    - "Zero regressão: PATCH /api/leads/[id] e POST/PATCH /api/contracts/[id] não alterados"
  artifacts:
    - path: "src/types/n8n-payloads.ts"
      provides: "Tipos N8nEnvelope, N8nLead, N8nProperty, N8nContract e helpers buildN8nEnvelope + mappers"
      exports: ["N8nEnvelope", "N8nLead", "N8nProperty", "N8nContract", "buildN8nEnvelope"]
    - path: "src/app/api/leads/route.ts"
      provides: "GET /api/leads com payload padronizado"
      exports: ["GET"]
    - path: "src/app/api/imoveis/route.ts"
      provides: "GET /api/imoveis com payload padronizado"
      exports: ["GET"]
    - path: "src/app/api/contracts/route.ts"
      provides: "GET /api/contracts com payload padronizado (POST mantido)"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/leads/route.ts"
      to: "supabase.from('leads')"
      via: "select explícito de campos N8nLead"
      pattern: "from.*leads.*select"
    - from: "src/app/api/imoveis/route.ts"
      to: "supabase.from('properties')"
      via: "select explícito de campos N8nProperty"
      pattern: "from.*properties.*select"
    - from: "src/app/api/contracts/route.ts"
      to: "buildN8nEnvelope"
      via: "import de @/types/n8n-payloads"
      pattern: "buildN8nEnvelope.*contracts"
---

<objective>
Padronizar os payloads de retorno das APIs de leads, imóveis e contratos para consumo consistente futuro pelo n8n.

Purpose: Antes de construir qualquer automação no n8n, os contratos de API precisam estar estáveis. Um envelope uniforme com entity + tenant_id + count + fetched_at + data[] garante que webhooks n8n processem qualquer entidade com o mesmo parser. Sem isso, cada automação precisaria de lógica de normalização ad-hoc.
Output: Tipos TypeScript N8n + 3 GET endpoints retornando envelope padronizado. Nenhuma lógica de negócio ou autenticação alterada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/api/contracts/route.ts

<!-- Schema real da tabela leads (001_initial_schema.sql):
  id UUID, tenant_id UUID, stage_id UUID|null, name TEXT, email TEXT|null,
  phone TEXT|null, company TEXT|null, source TEXT|null,
  status TEXT ('new'|'contacted'|'qualified'|'proposal'|'negotiation'|'won'|'lost'),
  score INTEGER, value NUMERIC(12,2), notes TEXT|null, metadata JSONB,
  assigned_to UUID|null, last_action_at TIMESTAMPTZ|null,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
-->

<!-- Schema real da tabela properties (008 + 009_properties_extend.sql):
  id UUID, tenant_id UUID, title TEXT, photo_url TEXT|null,
  price NUMERIC(14,2), location TEXT, area_sqm NUMERIC(8,2)|null,
  status TEXT ('available'|'sold'|'reserved'), link TEXT|null,
  notes TEXT|null, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
  (campos estendidos: property_type, neighborhood, purpose, external_id, source, etc.)
-->

<!-- Schema real da tabela contracts (011_contracts_table.sql):
  id UUID, tenant_id UUID, lead_id UUID|null, lead_name TEXT,
  project_id UUID|null, project_name TEXT|null,
  corretor_id UUID|null, corretor_name TEXT|null,
  title TEXT|null, type TEXT ('venda'|'locacao'|'servico'|'parceria'),
  status TEXT ('rascunho'|'pendente'|'assinado'|'cancelado'|'expirado'),
  value NUMERIC(14,2), notes TEXT|null, file_url TEXT|null, file_name TEXT|null,
  signed_at TIMESTAMPTZ|null, expires_at TIMESTAMPTZ|null,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
-->

<!-- Padrão de autenticação (usado em contracts/route.ts e leads/[id]/route.ts):
  const supabase = await createClient()  // de @/lib/supabase/server
  const { data: { user } } = await supabase.auth.getUser()
  const { data: profile } = await supabase.from("profiles").select("tenant_id").eq("id", user.id).single()
  const tenantId = profile.tenant_id
  // toda query usa .eq("tenant_id", tenantId)
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tipos e helpers N8n em src/types/n8n-payloads.ts</name>
  <files>src/types/n8n-payloads.ts</files>
  <action>
Criar arquivo `src/types/n8n-payloads.ts` com envelope genérico, tipos por entidade, helper de construção e mappers.

**Envelope genérico:**
```typescript
export interface N8nEnvelope<T> {
  entity: string;      // "leads" | "imoveis" | "contracts"
  tenant_id: string;
  count: number;
  fetched_at: string;  // ISO 8601
  data: T[];
}
```

**Tipo N8nLead** (campos essenciais para automação — sem notes, metadata):
```typescript
export interface N8nLead {
  id: string;
  tenant_id: string;
  stage_id: string | null;
  name: string;
  email: string | null;
  phone: string | null;
  company: string | null;
  source: string | null;
  status: string;
  score: number;
  value: number;
  assigned_to: string | null;
  last_action_at: string | null;
  created_at: string;
  updated_at: string;
}
```

**Tipo N8nProperty** (campos essenciais — sem notes, photo_url, features):
```typescript
export interface N8nProperty {
  id: string;
  tenant_id: string;
  title: string;
  price: number;
  location: string;
  area_sqm: number | null;
  status: string;
  link: string | null;
  created_at: string;
  updated_at: string;
}
```

**Tipo N8nContract** (campos essenciais — sem notes, file_url, file_name, corretor_id):
```typescript
export interface N8nContract {
  id: string;
  tenant_id: string;
  lead_id: string | null;
  lead_name: string;
  project_name: string | null;
  corretor_name: string | null;
  title: string | null;
  type: string;
  status: string;
  value: number;
  signed_at: string | null;
  expires_at: string | null;
  created_at: string;
  updated_at: string;
}
```

**Helper buildN8nEnvelope** (evita repetição nos handlers):
```typescript
export function buildN8nEnvelope<T>(
  entity: string,
  tenantId: string,
  data: T[]
): N8nEnvelope<T> {
  return {
    entity,
    tenant_id: tenantId,
    count: data.length,
    fetched_at: new Date().toISOString(),
    data,
  };
}
```

**Mappers** — extraem apenas campos da interface, ignorando extras do Supabase:
```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function toN8nLead(row: any): N8nLead {
  return {
    id: row.id,
    tenant_id: row.tenant_id,
    stage_id: row.stage_id ?? null,
    name: row.name,
    email: row.email ?? null,
    phone: row.phone ?? null,
    company: row.company ?? null,
    source: row.source ?? null,
    status: row.status,
    score: row.score ?? 0,
    value: row.value ?? 0,
    assigned_to: row.assigned_to ?? null,
    last_action_at: row.last_action_at ?? null,
    created_at: row.created_at,
    updated_at: row.updated_at,
  };
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function toN8nProperty(row: any): N8nProperty {
  return {
    id: row.id,
    tenant_id: row.tenant_id,
    title: row.title,
    price: row.price ?? 0,
    location: row.location,
    area_sqm: row.area_sqm ?? null,
    status: row.status,
    link: row.link ?? null,
    created_at: row.created_at,
    updated_at: row.updated_at,
  };
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function toN8nContract(row: any): N8nContract {
  return {
    id: row.id,
    tenant_id: row.tenant_id,
    lead_id: row.lead_id ?? null,
    lead_name: row.lead_name,
    project_name: row.project_name ?? null,
    corretor_name: row.corretor_name ?? null,
    title: row.title ?? null,
    type: row.type,
    status: row.status,
    value: row.value ?? 0,
    signed_at: row.signed_at ?? null,
    expires_at: row.expires_at ?? null,
    created_at: row.created_at,
    updated_at: row.updated_at,
  };
}
```
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && npx tsc --noEmit 2>&1 | grep "n8n-payloads" || echo "NO_TS_ERRORS_IN_N8N_PAYLOADS"</automated>
  </verify>
  <done>Arquivo src/types/n8n-payloads.ts existe com N8nEnvelope, N8nLead, N8nProperty, N8nContract, buildN8nEnvelope e os 3 mappers exportados. Compila sem erros TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Criar GET /api/leads e GET /api/imoveis + padronizar GET /api/contracts</name>
  <files>src/app/api/leads/route.ts, src/app/api/imoveis/route.ts, src/app/api/contracts/route.ts</files>
  <action>
**IMPORTANTE:** Não existe `src/app/api/leads/route.ts` nem `src/app/api/imoveis/route.ts` — criar ambos. O arquivo `src/app/api/leads/[id]/route.ts` já existe e NÃO deve ser tocado.

---

**src/app/api/leads/route.ts** — criar do zero:
```typescript
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { buildN8nEnvelope, toN8nLead } from "@/types/n8n-payloads";

// ─── GET /api/leads ───────────────────────────────────────────────────────────
// Retorna leads do tenant autenticado em formato padronizado para n8n

export async function GET() {
  try {
    const supabase = await createClient();

    const { data: { user }, error: authError } = await supabase.auth.getUser();
    if (authError || !user) {
      return NextResponse.json({ error: "Nao autenticado" }, { status: 401 });
    }

    const { data: profile, error: profileError } = await supabase
      .from("profiles")
      .select("tenant_id")
      .eq("id", user.id)
      .single();

    if (profileError || !profile?.tenant_id) {
      return NextResponse.json({ error: "Perfil nao encontrado" }, { status: 401 });
    }

    const tenantId = profile.tenant_id as string;

    const { data: leads, error: leadsError } = await supabase
      .from("leads")
      .select("id, tenant_id, stage_id, name, email, phone, company, source, status, score, value, assigned_to, last_action_at, created_at, updated_at")
      .eq("tenant_id", tenantId)
      .order("updated_at", { ascending: false });

    if (leadsError) {
      console.error("[GET /api/leads] query error:", leadsError);
      return NextResponse.json({ error: "Erro ao buscar leads" }, { status: 500 });
    }

    const payload = buildN8nEnvelope("leads", tenantId, (leads ?? []).map(toN8nLead));
    return NextResponse.json(payload, { status: 200 });
  } catch (err) {
    console.error("[GET /api/leads] unexpected error:", err);
    return NextResponse.json({ error: "Erro interno do servidor" }, { status: 500 });
  }
}
```

---

**src/app/api/imoveis/route.ts** — criar do zero:
```typescript
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { buildN8nEnvelope, toN8nProperty } from "@/types/n8n-payloads";

// ─── GET /api/imoveis ─────────────────────────────────────────────────────────
// Retorna imóveis do tenant autenticado em formato padronizado para n8n

export async function GET() {
  try {
    const supabase = await createClient();

    const { data: { user }, error: authError } = await supabase.auth.getUser();
    if (authError || !user) {
      return NextResponse.json({ error: "Nao autenticado" }, { status: 401 });
    }

    const { data: profile, error: profileError } = await supabase
      .from("profiles")
      .select("tenant_id")
      .eq("id", user.id)
      .single();

    if (profileError || !profile?.tenant_id) {
      return NextResponse.json({ error: "Perfil nao encontrado" }, { status: 401 });
    }

    const tenantId = profile.tenant_id as string;

    const { data: properties, error: propertiesError } = await supabase
      .from("properties")
      .select("id, tenant_id, title, price, location, area_sqm, status, link, created_at, updated_at")
      .eq("tenant_id", tenantId)
      .order("updated_at", { ascending: false });

    if (propertiesError) {
      console.error("[GET /api/imoveis] query error:", propertiesError);
      return NextResponse.json({ error: "Erro ao buscar imoveis" }, { status: 500 });
    }

    const payload = buildN8nEnvelope("imoveis", tenantId, (properties ?? []).map(toN8nProperty));
    return NextResponse.json(payload, { status: 200 });
  } catch (err) {
    console.error("[GET /api/imoveis] unexpected error:", err);
    return NextResponse.json({ error: "Erro interno do servidor" }, { status: 500 });
  }
}
```

---

**src/app/api/contracts/route.ts** — modificar APENAS o GET handler:

Ler o arquivo atual e fazer apenas estas 3 mudanças no handler GET (POST fica idêntico):
1. Adicionar import no topo: `import { buildN8nEnvelope, toN8nContract } from "@/types/n8n-payloads";`
2. Trocar `.select("*")` por `.select("id, tenant_id, lead_id, lead_name, project_name, corretor_name, title, type, status, value, signed_at, expires_at, created_at, updated_at")`
3. Trocar `return NextResponse.json(contracts ?? [], { status: 200 });` por:
   ```typescript
   const payload = buildN8nEnvelope("contracts", tenantId, (contracts ?? []).map(toN8nContract));
   return NextResponse.json(payload, { status: 200 });
   ```
O handler POST retorna objeto direto com status 201 — não alterar.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && npx tsc --noEmit 2>&1 | grep -E "api/leads/route|api/imoveis/route|api/contracts/route" || echo "NO_TS_ERRORS_IN_ROUTES"</automated>
  </verify>
  <done>
- src/app/api/leads/route.ts criado — GET retorna N8nEnvelope com entity="leads"
- src/app/api/imoveis/route.ts criado — GET retorna N8nEnvelope com entity="imoveis"
- src/app/api/contracts/route.ts modificado — GET retorna N8nEnvelope, POST inalterado
- Zero erros de TypeScript nos 3 arquivos
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passa sem erros (todos os 4 arquivos)
2. Os 3 GETs retornam shape consistente: `{ entity, tenant_id, count, fetched_at, data: [...] }`
3. `npm run build` completa sem erros de rota Next.js
4. POST /api/contracts continua retornando objeto contract direto com status 201
5. PATCH /api/leads/[id] e PATCH+POST /api/contracts/[id] não foram tocados
</verification>

<success_criteria>
- GET /api/leads: `{ entity: "leads", tenant_id, count, fetched_at, data: N8nLead[] }`
- GET /api/imoveis: `{ entity: "imoveis", tenant_id, count, fetched_at, data: N8nProperty[] }`
- GET /api/contracts: `{ entity: "contracts", tenant_id, count, fetched_at, data: N8nContract[] }`
- Tipos TypeScript em src/types/n8n-payloads.ts documentam contrato exato de cada payload
- Campos sensíveis/verbosos excluídos: notes, metadata, file_url, file_name, features, images
- Build Next.js passa sem erros
- Zero regressão em handlers existentes (POST, PATCH)
</success_criteria>

<output>
After completion, create `.planning/quick/260407-l5s-padronizar-payload-para-n8n/260407-l5s-SUMMARY.md`
</output>
