---
phase: quick
plan: 260409-61o
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VALIDATE-LUANA-E2E]

must_haves:
  truths:
    - "Lead e criado apenas uma vez ao receber primeira mensagem"
    - "Segunda mensagem faz GET e retorna lead existente (sem duplicar)"
    - "atualizar_qualificacao faz merge correto de metadata (preserva campos antigos)"
    - "consultar_imoveis retorna array com dados completos (15+ campos)"
    - "setar_lead_quente atualiza status/score no mesmo lead (sem criar novo)"
    - "metadata acumula campos ao longo do fluxo sem perda"
    - "status evolui no mesmo registro sem duplicacao"
  artifacts: []
  key_links:
    - from: "main workflow (lead UPSERT)"
      to: "leads table"
      via: "on_conflict=tenant_id,phone"
      pattern: "COUNT.*leads.*phone.*= 1"
    - from: "atualizar_qualificacao"
      to: "leads table metadata"
      via: "GET + spread merge + UPSERT"
      pattern: "metadata.*antigo.*novo"
    - from: "setar_lead_quente"
      to: "leads table status/score"
      via: "GET + PATCH by tenant_id+phone"
      pattern: "status.*lead_quente.*score.*3"
---

<objective>
Validar o fluxo completo da agente Luana (Jurema Brokers) ponta a ponta usando chamadas HTTP diretas ao Supabase REST API.

Purpose: Confirmar que todas as migracoes Airtable->Supabase dos workflows (260408-sow, 260408-rzc, 260409-06k, 260409-5w2) funcionam corretamente em conjunto — lead unico, metadata acumulativo, status evoluindo, imoveis retornados.

Output: Script de validacao executavel + relatorio de resultados (pass/fail por criterio)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.env.local

Jurema Brokers tenant_id: aaaaaaaa-0002-0002-0002-000000000002
Supabase URL: https://dwmbklfkrtumfaxrbxio.supabase.co
Service Role Key: from .env.local SUPABASE_SERVICE_ROLE_KEY

Workflows migrados (referencia):
- Main Luana: lead lookup/create via Supabase GET + UPSERT on_conflict=tenant_id,phone (260408-sow)
- atualizar_qualificacao: GET Lead -> Build Context {..antigo, ...novo} -> UPSERT (260408-rzc)
- consultar_imoveis: HTTP GET /imoveis com 16 campos + Code normalize (260409-06k)
- setar_lead_quente: GET Lead -> Build Metadata merge -> PATCH (260409-5w2)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar e executar script de validacao E2E do fluxo Luana</name>
  <files>.planning/quick/260409-61o-validar-fluxo-completo-da-luana-ponta-a-/validate-luana-e2e.js</files>
  <action>
Criar um script Node.js que valida o fluxo completo via chamadas HTTP diretas ao Supabase REST API (service_role key para bypass de RLS). NAO alterar nenhum workflow ou codigo.

O script deve usar um telefone de teste unico (ex: 5500000000001) para nao interferir com dados reais.

**Sequencia de validacao:**

1. **SETUP — Limpar lead de teste anterior (se existir)**
   - DELETE /rest/v1/leads?tenant_id=eq.aaaaaaaa-0002-0002-0002-000000000002&phone=eq.5500000000001
   - Garantir estado limpo

2. **TEST 1 — Simular primeira mensagem (lead creation)**
   - POST /rest/v1/leads com body: { tenant_id, phone: "5500000000001", name: "Teste E2E Luana", status: "novo", metadata: {} }
   - Header: Prefer: return=representation
   - Assert: resposta tem 1 item, id eh UUID, phone e name corretos
   - GET /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001
   - Assert: COUNT = 1 (exatamente um lead)

3. **TEST 2 — Simular segunda mensagem (lead lookup, sem duplicar)**
   - POST /rest/v1/leads?on_conflict=tenant_id,phone com mesmo phone+tenant_id mas name="Teste E2E Luana Update"
   - Header: Prefer: resolution=merge-duplicates,return=representation
   - GET /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001
   - Assert: COUNT = 1 (ainda apenas um lead, NAO duplicou)
   - Assert: retornou o mesmo UUID id

4. **TEST 3 — Simular atualizar_qualificacao (merge de metadata)**
   - Primeiro GET para obter metadata atual
   - PATCH /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001 com:
     body: { status: "qualificado", metadata: { objetivo: "comprar", faixa_valor: "500k-1M", bairro_interesse: "Manaira" } }
   - Header: Prefer: return=representation
   - Assert: metadata contem objetivo, faixa_valor, bairro_interesse
   - Segundo PATCH simulando nova qualificacao parcial:
     body: { metadata: { ...metadata_atual, tipo_imovel: "apartamento" } } (merge manual no script)
   - Assert: metadata final contem TODOS os 4 campos (objetivo + faixa_valor + bairro_interesse + tipo_imovel)
   - Assert: campos anteriores NAO foram apagados

5. **TEST 4 — Consultar imoveis (dados completos)**
   - GET /rest/v1/imoveis?tenant_id=eq.aaaaaaaa-0002-0002-0002-000000000002&status_publicacao=eq.Publicado&select=id_imovel,titulo_comercial,titulo_seo,descricao_imovel,bairro,tipo_de_imovel,finalidade,valor,metragem,quartos,suites,vagas,foto_principal,link_do_imovel,link_redes_sociais,status_publicacao
   - Assert: resposta eh array com length > 0
   - Assert: primeiro item tem campos obrigatorios: id_imovel, titulo_comercial, bairro, valor, tipo_de_imovel
   - Assert: nenhum campo obrigatorio eh null/undefined
   - Imprimir total de imoveis encontrados e sample do primeiro

6. **TEST 5 — Simular setar_lead_quente (update status + merge metadata)**
   - GET lead atual para obter metadata
   - Build merged metadata: { ...metadata_atual, localizacao_visita: "Rua das Flores, 123" }
   - PATCH /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001 com:
     body: { status: "lead_quente", score: 3, metadata: merged }
   - Header: Prefer: return=representation
   - Assert: status == "lead_quente"
   - Assert: score == 3
   - Assert: metadata.localizacao_visita == "Rua das Flores, 123"
   - Assert: metadata ainda contem objetivo, faixa_valor, bairro_interesse, tipo_imovel (merge preservou)

7. **TEST 6 — Validacao final de integridade**
   - GET /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001
   - Assert: COUNT = 1 (nenhum duplicado ao longo de todo o fluxo)
   - Assert: status = "lead_quente" (evolucao final)
   - Assert: metadata tem 5 campos acumulados (objetivo, faixa_valor, bairro_interesse, tipo_imovel, localizacao_visita)
   - Imprimir lead completo para inspecao visual

8. **CLEANUP — Remover lead de teste**
   - DELETE /rest/v1/leads?tenant_id=eq.X&phone=eq.5500000000001
   - Assert: DELETE retorna 200/204

**Formato de output do script:**
```
=== LUANA E2E VALIDATION ===
[PASS/FAIL] Test 1: Lead creation (single insert)
[PASS/FAIL] Test 2: Second message (no duplicate)
[PASS/FAIL] Test 3: atualizar_qualificacao (metadata merge)
[PASS/FAIL] Test 4: consultar_imoveis (complete data)
[PASS/FAIL] Test 5: setar_lead_quente (status + merge)
[PASS/FAIL] Test 6: Final integrity check

RESULT: X/6 passed
```

Usar apenas `fetch` nativo do Node.js (v18+), sem dependencias externas. Ler SUPABASE_SERVICE_ROLE_KEY e NEXT_PUBLIC_SUPABASE_URL de .env.local via fs.readFileSync + parse manual.

REGRA CRITICA: Este script NAO modifica workflows — apenas valida o comportamento esperado via chamadas diretas ao Supabase REST API, simulando o que os workflows fariam.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && node .planning/quick/260409-61o-validar-fluxo-completo-da-luana-ponta-a-/validate-luana-e2e.js</automated>
  </verify>
  <done>Script executa com sucesso e imprime resultado de cada teste (PASS/FAIL). Todos os 6 testes passam: lead unico, sem duplicados, metadata acumulativo, imoveis completos, status evolui, integridade final.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Script de validacao E2E do fluxo Luana executado com sucesso contra o Supabase real. Os 6 testes cobrem: criacao de lead, idempotencia, merge de metadata, consulta de imoveis, setar_lead_quente, e integridade final.</what-built>
  <how-to-verify>
    1. Revisar o output do script — todos os 6 testes devem mostrar [PASS]
    2. Verificar se o lead de teste foi removido no cleanup (nao poluiu dados reais)
    3. Se algum teste falhou, o output mostrara detalhes do que era esperado vs recebido
    4. Opcionalmente, verificar no Supabase Dashboard que nao existe lead com phone=5500000000001
  </how-to-verify>
  <resume-signal>Type "approved" se todos os testes passaram, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- Script executa sem erros de sintaxe ou runtime
- Todos os 6 testes reportam PASS
- Lead de teste eh criado e removido sem deixar residuo
- Nenhum workflow ou codigo do projeto foi alterado
</verification>

<success_criteria>
- 6/6 testes PASS confirmando fluxo completo da Luana
- metadata acumula 5 campos ao longo do fluxo sem perda
- Apenas 1 lead criado durante todo o fluxo (zero duplicados)
- Status evolui: novo -> qualificado -> lead_quente no mesmo registro
- consultar_imoveis retorna dados com campos obrigatorios preenchidos
</success_criteria>

<output>
After completion, create `.planning/quick/260409-61o-validar-fluxo-completo-da-luana-ponta-a-/260409-61o-SUMMARY.md`
</output>
