---
phase: quick
plan: 260408-2i3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260408-2i3-migrar-workflow-ler-im-veis-jetengine-pa/workflow-patch-ler-imoveis-jetengine.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "Workflow 'Ler Imoveis JetEngine' no n8n grava na tabela imoveis do Supabase em vez do Airtable"
    - "Upsert usa chave composta tenant_id + id_imovel — sem duplicatas ao re-executar"
    - "Leitura do WordPress JetEngine permanece inalterada"
    - "Todos os 18 campos sao gravados corretamente"
  artifacts:
    - path: ".planning/quick/260408-2i3-migrar-workflow-ler-im-veis-jetengine-pa/workflow-patch-ler-imoveis-jetengine.json"
      provides: "Patch JSON completo para aplicacao no n8n"
  key_links:
    - from: "n8n workflow Set node"
      to: "Supabase node upsert"
      via: "node connection replacing Airtable"
      pattern: "operation.*upsert"
---

<objective>
Migrar o destino do workflow "Ler Imoveis JetEngine" no n8n de Airtable para Supabase, fazendo upsert na tabela `imoveis` com 18 campos e chave composta tenant_id + id_imovel para Jurema Brokers.

Purpose: Centralizar dados de imoveis no Supabase (fonte unica de verdade) em vez do Airtable, permitindo que cockpit e agente WhatsApp consumam da mesma tabela.
Output: Patch JSON aplicavel no n8n + workflow atualizado (se MCP n8n disponivel).

NOTA: A task 260407-wba ja gerou um patch identico. Esta task deve tentar APLICAR o patch via MCP n8n. Se MCP n8n indisponivel, gerar patch atualizado e fornecer instrucoes de aplicacao manual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260407-wba-migrar-workflow-ler-im-veis-jetengine-pa/workflow-patch-ler-imoveis-jetengine.json
@.planning/quick/260407-wba-migrar-workflow-ler-im-veis-jetengine-pa/260407-wba-SUMMARY.md

Referencia anterior: task 260407-wba ja gerou patch completo com os mesmos 18 campos.
tenant_id Jurema Brokers: aaaaaaaa-0002-0002-0002-000000000002
Tabela destino: imoveis (NAO properties)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar MCP n8n e tentar aplicar patch no workflow</name>
  <files>.planning/quick/260408-2i3-migrar-workflow-ler-im-veis-jetengine-pa/workflow-patch-ler-imoveis-jetengine.json</files>
  <action>
1. Verificar se MCP n8n esta disponivel (tentar listar workflows ou buscar workflow com nome contendo "JetEngine" ou "Imoveis").

SE MCP n8n DISPONIVEL:
  a. Buscar o workflow "Ler Imoveis JetEngine" por nome.
  b. Ler o workflow atual e identificar o node Airtable (destino).
  c. Substituir o node Airtable por um node Supabase configurado como:
     - type: n8n-nodes-base.supabase
     - operation: upsert
     - tableId: imoveis
     - conflictColumns: ["tenant_id", "id_imovel"]
     - 18 campos conforme patch 260407-wba (ver context)
     - tenant_id hardcoded: "aaaaaaaa-0002-0002-0002-000000000002"
  d. Reconectar: saida do Set node (ou HTTP se nao houver Set) -> entrada do node Supabase.
  e. Salvar workflow atualizado.
  f. Gerar o patch JSON final em workflow-patch-ler-imoveis-jetengine.json com o que foi aplicado.

SE MCP n8n INDISPONIVEL:
  a. Copiar o patch de 260407-wba como base.
  b. Verificar que todos os 18 campos estao presentes: tenant_id, id_imovel, titulo_comercial, titulo_seo, descricao_imovel, bairro, tipo_de_imovel, finalidade, valor, condominio, metragem, quartos, suites, vagas, foto_principal, link_do_imovel, link_redes_sociais, status_publicacao.
  c. Salvar como workflow-patch-ler-imoveis-jetengine.json neste diretorio.
  d. Incluir instrucoes claras de aplicacao manual (mesmo formato do 260407-wba).

REGRAS:
- NAO alterar logica de leitura do WordPress JetEngine.
- NAO mexer na Luana (agente).
- Usar tenant_id Jurema: aaaaaaaa-0002-0002-0002-000000000002.
- Upsert por tenant_id + id_imovel.
  </action>
  <verify>
    <automated>node -e "const p=require('./.planning/quick/260408-2i3-migrar-workflow-ler-im-veis-jetengine-pa/workflow-patch-ler-imoveis-jetengine.json'); const cols=p.NODE_SUPABASE_UPSERT.columns.map(c=>c.column); const required=['tenant_id','id_imovel','titulo_comercial','titulo_seo','descricao_imovel','bairro','tipo_de_imovel','finalidade','valor','condominio','metragem','quartos','suites','vagas','foto_principal','link_do_imovel','link_redes_sociais','status_publicacao']; const ok=required.every(r=>cols.includes(r)); console.log(JSON.stringify({upsert:p.NODE_SUPABASE_UPSERT.operation==='upsert',table:p.NODE_SUPABASE_UPSERT.tableId==='imoveis',tenant:cols.includes('tenant_id'),conflict:JSON.stringify(p.NODE_SUPABASE_UPSERT.conflictColumns)==='[\"tenant_id\",\"id_imovel\"]',all_18_fields:ok,count:cols.length}));"</automated>
  </verify>
  <done>Patch JSON com 18 campos, upsert na tabela imoveis, chave composta tenant_id + id_imovel. Se MCP disponivel, workflow ja aplicado no n8n.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Patch JSON para migrar workflow "Ler Imoveis JetEngine" de Airtable para Supabase upsert com 18 campos na tabela imoveis (Jurema Brokers).</what-built>
  <how-to-verify>
    1. Se MCP n8n foi usado: abrir n8n, localizar workflow "Ler Imoveis JetEngine", verificar que node Supabase existe com operacao upsert na tabela imoveis, executar manualmente e verificar dados no Supabase.
    2. Se patch manual: abrir o arquivo workflow-patch-ler-imoveis-jetengine.json, seguir instrucoes em _meta.instructions e WIRING.steps para aplicar no n8n manualmente.
    3. Apos aplicar, executar workflow e rodar no Supabase:
       SELECT count(*) FROM imoveis WHERE tenant_id = 'aaaaaaaa-0002-0002-0002-000000000002';
    4. Verificar que registros aparecem e re-execucao nao duplica (upsert funcionando).
  </how-to-verify>
  <resume-signal>Type "approved" se workflow esta gravando no Supabase corretamente, ou descreva problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- Patch JSON existe com 18 campos mapeados
- operation = "upsert", tableId = "imoveis"
- conflictColumns = ["tenant_id", "id_imovel"]
- tenant_id Jurema hardcoded: aaaaaaaa-0002-0002-0002-000000000002
- Nenhuma referencia a Airtable no destino
- Instrucoes de leitura WP JetEngine marcadas como inalteradas
</verification>

<success_criteria>
Workflow "Ler Imoveis JetEngine" grava todos 18 campos na tabela imoveis do Supabase via upsert, sem duplicatas, para tenant Jurema Brokers.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-2i3-migrar-workflow-ler-im-veis-jetengine-pa/260408-2i3-SUMMARY.md`
</output>
