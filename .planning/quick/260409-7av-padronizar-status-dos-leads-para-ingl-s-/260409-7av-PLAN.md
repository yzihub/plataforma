---
phase: quick
plan: 260409-7av
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json
  - .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js
  - .planning/quick/260408-sub-refactor-luana-airtable-supabase/atualizar_qualificacao.json
  - .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "All lead status values written to Supabase DB use only the English enum: new, contacted, qualified, proposal, won, lost"
    - "No old non-standard slugs (start, qualifying, hot_lead, visit_requested) remain as status values in any workflow"
    - "No Portuguese status slugs (novo, contatado, qualificando, lead_quente) are written as DB values"
    - "Workflow logic, nodes, connections, and positions are unchanged"
  artifacts:
    - path: ".planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json"
      provides: "Main Luana workflow with all status fallbacks corrected"
      contains: "'new'"
    - path: ".planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js"
      provides: "Fix script with corrected status values"
      contains: "'new'"
  key_links:
    - from: "workflow JSON status values"
      to: "Supabase leads.status column"
      via: "HTTP PATCH/POST body"
      pattern: "status.*'(new|contacted|qualified|proposal)'"
---

<objective>
Padronizar todos os valores de status nos workflows n8n da Luana para usar apenas o enum ingles do Supabase (new, contacted, qualified, proposal).

Purpose: Garantir consistencia entre workflows e o banco de dados. O Supabase rejeita valores fora do enum.
Output: Arquivos de workflow atualizados com status padronizado, sem alteracao de logica.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

## Auditoria Completa dos Workflows (pre-analise do planner)

### Mapeamento de status (valores antigos -> valores DB validos):
- 'start' -> 'new'
- 'qualifying' -> 'contacted'
- 'hot_lead' -> 'qualified'
- 'visit_requested' -> 'proposal'
- 'novo' -> 'new'
- 'contatado' -> 'contacted'
- 'qualificando' -> 'contacted'
- 'lead_quente' -> 'qualified'

### Estado atual de cada arquivo:

**1. setar_lead_quente.json** — JA CORRETO
- Linha 67: `status: 'qualified'` (ja esta correto)
- SEM ALTERACAO NECESSARIA. Apenas auditar e confirmar.

**2. atualizar_qualificacao.json** — JA CORRETO
- NAO tem campo status sendo escrito no PATCH. Apenas faz merge de metadata.
- SEM ALTERACAO NECESSARIA. Apenas auditar e confirmar.

**3. luana-agent-workflow-fixed.json** — PRECISA CORRECAO (1 ponto)
- Linha ~2670 (Create a record1 body): `status: 'new'` — JA CORRETO
- Linhas ~2748 e ~9075 (Contexto Inicial): `_status_slug: 'new'` — JA CORRETO
- Linha ~3765 (Dados do Lead, branch Supabase): `lead.status || 'new'` — JA CORRETO
- Linha ~3765 urgencyMap: usa `qualified`, `proposal`, `new` como keys — JA CORRETO
- **Linha ~10092 (legacy Code node, branch Airtable):**
  - statusMap values: `'new'`, `'contacted'`, `'qualified'`, `'proposal'` — JA CORRETO
  - urgencyMap keys: `qualified`, `proposal`, `new` — JA CORRETO
  - **FALTA CORRIGIR: fallback `statusMap[statusRaw] || 'start'` -> deve ser `|| 'new'`**
- Linhas 3283 e 9610: `"start": "={{ $fromAI('start'..."` — NAO E STATUS, e parametro de AI tool. NAO ALTERAR.

**4. fix-workflow.js** — PRECISA CORRECAO (2 pontos)
- **Linha 97: `status: 'start'` -> `status: 'new'`**
- **Linha 135: `lead.status || 'start'` -> `lead.status || 'new'`**
- urgencyMap: verificar se usa keys antigos (hot_lead, etc)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir status residuais e auditar todos os workflows da Luana</name>
  <files>
    .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json
    .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js
    .planning/quick/260408-sub-refactor-luana-airtable-supabase/atualizar_qualificacao.json
    .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
  </files>
  <action>
    REGRA ABSOLUTA: Nao alterar logica, nodes, connections, positions. APENAS substituir valores de status string.

    **A. luana-agent-workflow-fixed.json:**
    No legacy Code node (linha ~10092), dentro do jsCode (string JSON escapada), localizar:
    - `statusMap[statusRaw] || 'start'` -> substituir por `statusMap[statusRaw] || 'new'`
    CUIDADO: O jsCode esta numa string JSON com \\n escapes. Editar apenas o valor 'start' nesse ponto especifico.
    NAO TOCAR nas linhas 3283 e 9610 que contem `"start": "={{ $fromAI('start'..."` — esses sao parametros de AI tool, nao status.

    **B. fix-workflow.js:**
    - Linha 97: `status: 'start'` -> `status: 'new'`
    - Linha 135: `lead.status || 'start'` -> `lead.status || 'new'`
    - Verificar urgencyMap neste arquivo — se usar keys `hot_lead`, `visit_requested`, `start`, substituir por `qualified`, `proposal`, `new` respectivamente.

    **C. setar_lead_quente.json:**
    - Auditar: confirmar que linha ~67 tem `status: 'qualified'`. Se sim, nenhuma alteracao. Se nao, corrigir.

    **D. atualizar_qualificacao.json:**
    - Auditar: confirmar que NAO escreve campo status em nenhum lugar. Se sim, nenhuma alteracao. Se escrever status em portugues, corrigir.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && node -e "const fs=require('fs'); const files=['.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json','.planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json','.planning/quick/260408-sub-refactor-luana-airtable-supabase/atualizar_qualificacao.json']; let ok=true; files.forEach(f=>{try{JSON.parse(fs.readFileSync(f,'utf8'));console.log('VALID JSON:',f)}catch(e){console.error('INVALID JSON:',f,e.message);ok=false}}); const all=files.map(f=>fs.readFileSync(f,'utf8')).join(' '); const js=fs.readFileSync('.planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js','utf8'); const combined=all+js; const bad=[/status:\s*'start'/g,/status:\s*'qualifying'/g,/status:\s*'hot_lead'/g,/status:\s*'visit_requested'/g,/status:\s*'novo'/g,/status:\s*'contatado'/g,/status:\s*'qualificando'/g,/status:\s*'lead_quente'/g,/\|\|\s*'start'/g]; bad.forEach(rx=>{const m=combined.match(rx);if(m){console.error('FAIL:',rx.source,'found',m.length,'times');ok=false}}); if(ok){console.log('PASS: all status values standardized to English enum')}else{process.exit(1)}"</automated>
  </verify>
  <done>
    - Todos os 4 workflows auditados
    - luana-agent-workflow-fixed.json: fallback 'start' corrigido para 'new' no legacy code node
    - fix-workflow.js: 'start' substituido por 'new' nas linhas 97 e 135
    - setar_lead_quente.json: confirmado status='qualified' (sem alteracao)
    - atualizar_qualificacao.json: confirmado que nao escreve status (sem alteracao)
    - Todos os JSONs continuam validos (parseable)
    - Zero ocorrencias de status antigos em qualquer arquivo
  </done>
</task>

</tasks>

<verification>
Verificacao global:
1. Todos os 3 arquivos JSON sao parseaveis (JSON.parse sem erro)
2. fix-workflow.js nao tem syntax error (node --check)
3. Busca por status antigos retorna zero resultados em todos os 4 arquivos
4. Status validos (new, contacted, qualified, proposal) estao presentes nos pontos corretos
</verification>

<success_criteria>
- Zero ocorrencias de status fora do enum ingles nos 4 arquivos de workflow
- Todos os status escritos no DB usam: new, contacted, qualified, proposal
- Nenhuma alteracao em logica, nodes, connections ou positions
- Todos os arquivos continuam validos
</success_criteria>

<output>
After completion, create `.planning/quick/260409-7av-padronizar-status-dos-leads-para-ingl-s-/260409-7av-SUMMARY.md`
</output>
