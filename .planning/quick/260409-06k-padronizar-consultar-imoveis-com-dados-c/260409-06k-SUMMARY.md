---
phase: quick
plan: 260409-06k
subsystem: n8n/consultar_imoveis
tags: [n8n, imoveis, luana, jurema-brokers, supabase, migration]
dependency_graph:
  requires: []
  provides: [consultar_imoveis workflow migrado para Supabase, array normalizado com 15 campos]
  affects: [agente Luana, tool consultar_imoveis]
key_files:
  modified:
    - .planning/quick/260408-sub-refactor-luana-airtable-supabase/consultar_imoveis.json
decisions:
  - Mantida lógica Prepara Busca inalterada (tipo_busca direta|filtro)
  - Filtros client-side no Code node (sem ramificação extra no fluxo)
  - cidade hardcoded como "João Pessoa" (campo não existe na tabela imoveis)
  - fotos mapeia para foto_principal (string única, não array)
  - Retorno: { imoveis: [...], total: N } em vez de texto formatado
metrics:
  completed_date: "2026-04-09"
  tasks_completed: 2
---

# Quick 260409-06k: Padronizar consultar_imoveis com dados completos — Summary

**One-liner:** Workflow `consultar_imoveis` migrado de Airtable para Supabase REST API via HTTP Request GET, com 16 campos selecionados e Code node normalizando nomes para padrão do agente Luana (`titulo`, `preco`, `area`, `tipo`, `fotos`, `cidade`).

## Tasks Completed

| # | Task | Resultado |
|---|------|-----------|
| 1 | Atualizar workflow n8n `0udn6N4YelE6F2Ws` via MCP | Airtable substituído por HTTP GET Supabase; Code node retorna `{ imoveis: [...], total: N }` |
| 2 | Atualizar `consultar_imoveis.json` local | JSON sincronizado com workflow real |

## O Que Mudou

### Node substituído: `Search records` (Airtable) → `Buscar Imóveis` (HTTP Request Supabase)

- **URL:** `{{ $vars.SUPABASE_URL }}/rest/v1/imoveis`
- **Headers:** apikey + Authorization Bearer + Content-Type
- **SELECT:** `id_imovel, titulo_comercial, titulo_seo, descricao_imovel, bairro, tipo_de_imovel, finalidade, valor, metragem, quartos, suites, vagas, foto_principal, link_do_imovel, link_redes_sociais, status_publicacao`
- **Filtros fixos:** `tenant_id=eq.aaaaaaaa-0002-0002-0002-000000000002`, `status_publicacao=eq.Publicado`
- **Ordem:** `valor.asc`

### Node substituído: `Code in JavaScript` (texto formatado) → `Formatar Imóveis` (array normalizado)

**Mapeamento de campos:**

| Campo output | Coluna Supabase         |
|--------------|-------------------------|
| titulo       | titulo_comercial        |
| descricao    | descricao_imovel        |
| preco        | valor                   |
| quartos      | quartos                 |
| suites       | suites                  |
| vagas        | vagas                   |
| area         | metragem                |
| bairro       | bairro                  |
| cidade       | "João Pessoa" (static)  |
| tipo         | tipo_de_imovel          |
| finalidade   | finalidade              |
| fotos        | foto_principal          |
| link         | link_do_imovel \|\| link_redes_sociais |

**Filtros client-side preservados:**
- `tipo_busca === "direta"`: filtra por `id_imovel`
- `tipo_busca === "filtro"`: filtra por `bairro`, `quartos >= N`, `preco <= valor_maximo`

**Output:** `{ imoveis: [...], total: N }` — array completo sem truncamento.

## Nodes Preservados (inalterados)

- `When Executed by Another Workflow` — trigger e inputs unchanged
- `Prepara Busca` — lógica de normalização de input unchanged

## Self-Check: PASSED

- [x] Workflow `0udn6N4YelE6F2Ws` atualizado via MCP (success: true, nodeCount: 4)
- [x] Airtable removido — sem dependência de credencial Airtable
- [x] Todos os campos solicitados presentes: titulo, descricao, preco, quartos, suites, vagas, area, bairro, cidade, tipo, status_publicacao, fotos
- [x] Array completo retornado (sem truncamento de texto)
- [x] Filtros do Prepara Busca preservados
- [x] consultar_imoveis.json local sincronizado
