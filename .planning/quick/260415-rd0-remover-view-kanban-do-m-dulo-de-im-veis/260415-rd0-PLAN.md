---
phase: quick-260415-rd0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/ImoveisClient.tsx
autonomous: true
requirements:
  - RD0-01
must_haves:
  truths:
    - "Página /cockpit/imoveis renderiza apenas os toggles Tabela e Grade — botão Kanban não existe mais"
    - "Estado view aceita apenas 'table' | 'grid' — 'kanban' não compila"
    - "Bloco {view === 'kanban'} foi removido do JSX"
    - "Import de PropertyKanban removido (sem warning de unused import)"
    - "Build (rtk next build) e tsc (rtk tsc --noEmit) passam sem erros"
  artifacts:
    - path: "src/components/yzihub/ImoveisClient.tsx"
      provides: "Cliente Imoveis com apenas duas views (Table/Grid)"
      contains: "view === \"table\""
  key_links:
    - from: "src/components/yzihub/ImoveisClient.tsx"
      to: "PropertyTable / PropertyCard"
      via: "renderização condicional por view"
      pattern: "view === \"(table|grid)\""
---

<objective>
Remover cirurgicamente a view Kanban do módulo de Imóveis, mantendo apenas Table e Grid. Patch mínimo, sem refatoração.

Purpose: A view Kanban por bairro não está agregando valor operacional — a Tabela e a Grade já cobrem o uso real. Reduzir superfície de UI para diminuir manutenção e ruído visual.
Output: ImoveisClient.tsx sem qualquer referência a Kanban (estado, botão, ícone, import, render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/yzihub/ImoveisClient.tsx

<interfaces>
<!-- Estado atual relevante de ImoveisClient.tsx (linhas-chave) -->

Linha 9:  import PropertyKanban from "@/components/yzihub/PropertyKanban";
Linha 113-121: function KanbanIcon() { ... }
Linha 169: const [view, setView] = useState<"table" | "grid" | "kanban">("table");
Linha 377-388: <button onClick={() => setView("kanban")}> ... <KanbanIcon /> Kanban </button>
Linha 440-442: {view === "kanban" && (<PropertyKanban properties={filtered} />)}

PropertyKanban.tsx NÃO deve ser deletado — apenas desconectado do ImoveisClient.
Mantém-se no repo para eventual reutilização / referência futura.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover Kanban de ImoveisClient.tsx (5 deleções pontuais)</name>
  <files>src/components/yzihub/ImoveisClient.tsx</files>
  <action>
Editar APENAS src/components/yzihub/ImoveisClient.tsx fazendo cinco remoções cirúrgicas. Não tocar em PropertyKanban.tsx (mantém no repo, apenas desconectado).

1. **Linha 9** — Remover import:
   - DELETAR: `import PropertyKanban from "@/components/yzihub/PropertyKanban";`

2. **Linhas 113-121** — Remover componente KanbanIcon inteiro:
   - DELETAR a function `KanbanIcon()` completa (inclusive a linha em branco anterior se virar ruído).

3. **Linha 169** — Restringir tipo do estado view:
   - DE:    `const [view, setView] = useState<"table" | "grid" | "kanban">("table");`
   - PARA:  `const [view, setView] = useState<"table" | "grid">("table");`

4. **Linhas ~377-388** — Remover o botão Kanban do view toggle:
   - DELETAR todo o `<button onClick={() => setView("kanban")} ...> ... <KanbanIcon /> ... Kanban </button>`
   - Manter os botões "Tabela" e "Grade" intactos (incluindo classes Tailwind, ordem e wrapper `<div className="flex rounded-xl border ... overflow-hidden">`).

5. **Linhas ~439-442** — Remover bloco de render do Kanban:
   - DELETAR o comentário `{/* Kanban view */}` e o bloco `{view === "kanban" && (<PropertyKanban properties={filtered} />)}`.

Não alterar mais nada: filtros, métricas, drawer, fetch, mapImoveisToProperty, MetricCard, TableIcon, GridIcon — tudo permanece igual.

Após editar, verificar que o arquivo não tem mais nenhuma ocorrência das strings: `Kanban`, `kanban`, `PropertyKanban`, `KanbanIcon`.
  </action>
  <verify>
    <automated>rtk grep -n "[Kk]anban" src/components/yzihub/ImoveisClient.tsx ; rtk tsc --noEmit</automated>
  </verify>
  <done>
- `grep [Kk]anban src/components/yzihub/ImoveisClient.tsx` retorna ZERO matches
- `tsc --noEmit` passa sem erros relacionados a ImoveisClient.tsx
- Estado view tipado como `"table" | "grid"`
- View toggle mostra apenas dois botões (Tabela, Grade)
- Arquivo PropertyKanban.tsx continua existindo no repo (não deletado)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
ImoveisClient.tsx sem a view Kanban — toggle reduzido a Tabela/Grade, import e render do PropertyKanban removidos. PropertyKanban.tsx mantido no repo (desconectado).
  </what-built>
  <how-to-verify>
1. Rodar `rtk pnpm dev` (ou similar) e abrir http://localhost:3000/cockpit/imoveis
2. Confirmar visualmente que o seletor de view mostra APENAS dois botões: "Tabela" e "Grade" (sem o terceiro botão Kanban com 3 colunas)
3. Clicar em "Tabela" — deve renderizar PropertyTable normalmente
4. Clicar em "Grade" — deve renderizar grid de PropertyCards normalmente
5. Filtros (Tipo, Bairro, Publicação, Preço) continuam funcionando em ambas as views
6. Faixa de 6 métricas no topo continua intacta
7. Console do navegador sem erros / warnings novos
  </how-to-verify>
  <resume-signal>Digite "approved" se UI estiver correta, ou descreva qualquer problema visual / funcional encontrado.</resume-signal>
</task>

</tasks>

<verification>
- `rtk grep -rn "PropertyKanban\|KanbanIcon" src/components/yzihub/ImoveisClient.tsx` → vazio
- `rtk tsc --noEmit` passa
- `rtk next build` passa (sem erros novos em /cockpit/imoveis)
- Visual: apenas 2 botões no view toggle de /cockpit/imoveis
</verification>

<success_criteria>
- Zero referências a Kanban em ImoveisClient.tsx
- Página /cockpit/imoveis funcional com Table e Grid
- Build e type-check verdes
- PropertyKanban.tsx preservado no repositório (não deletado)
- Nenhum outro arquivo modificado além de ImoveisClient.tsx
</success_criteria>

<output>
After completion, create `.planning/quick/260415-rd0-remover-view-kanban-do-m-dulo-de-im-veis/260415-rd0-SUMMARY.md`
</output>
