---
phase: quick-260414-o6s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cockpit/corretores/page.tsx
  - src/components/yzihub/CorretoresClient.tsx
  - src/components/yzihub/CorretorDrawer.tsx
  - src/types/brokers.ts
autonomous: false
requirements:
  - BROKER-01
  - BROKER-02
  - BROKER-03

must_haves:
  truths:
    - "Usuário logado vê lista de corretores filtrada por tenant_id ativo"
    - "Usuário pode criar novo corretor e ver o item aparecer na lista"
    - "Usuário pode editar full_name, phone, email e role de um corretor existente"
    - "Nenhum mock — todos os dados vêm da tabela Supabase verificada (brokers ou profiles)"
  artifacts:
    - path: "src/app/cockpit/corretores/page.tsx"
      provides: "Rota /cockpit/corretores renderizando o client component"
      min_lines: 10
    - path: "src/components/yzihub/CorretoresClient.tsx"
      provides: "Listagem + form inline/handlers para CRUD de corretores"
      min_lines: 120
    - path: "src/components/yzihub/CorretorDrawer.tsx"
      provides: "Drawer de criação/edição de corretor (padrão LeadDrawer/PropertyDrawer)"
      min_lines: 80
    - path: "src/types/brokers.ts"
      provides: "Tipo Broker e BrokerInput"
      min_lines: 15
  key_links:
    - from: "src/components/yzihub/CorretoresClient.tsx"
      to: "supabase brokers/profiles table"
      via: "createClient().from(TABLE).select/insert/update with .eq('tenant_id', tenant.id)"
      pattern: "\\.eq\\(['\"]tenant_id['\"]"
    - from: "src/components/yzihub/CorretoresClient.tsx"
      to: "useTenant hook"
      via: "import { useTenant } from '@/hooks/useTenant'"
      pattern: "useTenant\\(\\)"
    - from: "src/app/cockpit/corretores/page.tsx"
      to: "CorretoresClient"
      via: "dynamic/client import"
      pattern: "CorretoresClient"
---

<objective>
Criar módulo /cockpit/corretores com CRUD (listar/criar/editar) de corretores, integrado a Supabase real e filtrado por tenant_id via useTenant.

Purpose: Hoje não existe UI para gestão de corretores do tenant. Jurema Brokers precisa cadastrar e manter corretores vinculados ao tenant para futuros handoffs de leads quentes (série 260409-7j3).
Output: Página funcional em /cockpit/corretores listando corretores do tenant ativo e permitindo create/edit com persistência Supabase, seguindo padrão TailAdmin dark de `src/components/yzihub/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/app/cockpit/page.tsx
@src/components/yzihub/ImoveisClient.tsx
@src/components/yzihub/LeadDrawer.tsx
@src/components/yzihub/PropertyDrawer.tsx
@src/hooks/useTenant.ts
@src/lib/supabase/client.ts

<interfaces>
Padrão estabelecido no projeto (extraído de ImoveisClient.tsx):

```typescript
// Supabase client (browser)
import { createClient } from "@/lib/supabase/client";
const supabase = createClient();

// Tenant context
import { useTenant } from "@/hooks/useTenant";
const { tenant, loading: tenantLoading } = useTenant();
// tenant.id é usado em .eq("tenant_id", tenant.id)

// Padrão de fetch filtrado por tenant
await supabase.from("TABLE")
  .select("...")
  .eq("tenant_id", tenant.id)
  .order("created_at", { ascending: false });
```

Padrão Drawer: ver `src/components/yzihub/LeadDrawer.tsx` e `PropertyDrawer.tsx` para UI TailAdmin dark (lateral slide-over com overlay, rounded-2xl, dark:bg-white/[0.03], dark:border-gray-800).

Páginas cockpit são Next.js 15 App Router. `src/app/cockpit/layout.tsx` já injeta sidebar/layout global.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decidir tabela Supabase — brokers vs profiles(role=broker)</name>
  <decision>Qual tabela/modelo usar para persistir corretores?</decision>
  <context>
CLAUDE.md e STATE.md não confirmam existência de `brokers`. A tabela `profiles` existe (usada no gatekeeper). Precisamos decidir ANTES de escrever código para evitar retrabalho.

Antes de apresentar ao usuário, o executor DEVE investigar:
1. `rtk grep -r "from.*brokers" src/` e `rtk grep -r "table.*brokers\|brokers.*schema" .planning/` para ver se há referência.
2. `rtk grep -rn "role.*broker\|'broker'" src/ .planning/` para ver se role=broker já está mapeado em profiles.
3. Listar migrations existentes: `rtk ls supabase/migrations/ 2>/dev/null || rtk ls .planning/migrations/ 2>/dev/null`.
4. Reportar achados no checkpoint.
  </context>
  <options>
    <option id="option-a">
      <name>Usar tabela `brokers` dedicada</name>
      <pros>Schema claro, separa corretores de usuários do sistema, permite campos específicos (CRECI, comissão futura)</pros>
      <cons>Requer migration nova; duplica conceito se broker também tiver login</cons>
    </option>
    <option id="option-b">
      <name>Usar `profiles` com `role='broker'` e filtro por tenant_id</name>
      <pros>Reaproveita tabela existente (gatekeeper); sem migration; broker pode logar no Cockpit</pros>
      <cons>Mistura corretor-usuário com corretor-cadastro; pode exigir coluna tenant_id/role em profiles</cons>
    </option>
    <option id="option-c">
      <name>Criar tabela `brokers` simples + migration</name>
      <pros>Escopo isolado, segue convenção de tabela por domínio (leads, imoveis, contracts)</pros>
      <cons>Executor deve criar migration SQL inline; validar RLS por tenant_id</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c — e confirmar schema final (colunas: id, tenant_id, full_name, phone, email, role, created_at, updated_at)</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar tipos, drawer e client component do módulo Corretores</name>
  <files>
    src/types/brokers.ts
    src/components/yzihub/CorretorDrawer.tsx
    src/components/yzihub/CorretoresClient.tsx
  </files>
  <action>
Seguir EXATAMENTE o padrão de `ImoveisClient.tsx` + `PropertyDrawer.tsx` (TailAdmin dark, useTenant, createClient browser client).

1) `src/types/brokers.ts`:
```ts
export interface Broker {
  id: string;
  tenant_id: string;
  full_name: string;
  phone: string | null;
  email: string | null;
  role: string | null; // ex: "senior", "junior", "manager"
  created_at: string;
  updated_at: string | null;
}
export type BrokerInput = Pick<Broker, "full_name" | "phone" | "email" | "role">;
```

2) `CorretorDrawer.tsx` (client component):
- Props: `{ open: boolean; broker: Broker | null; onClose: () => void; onSave: (input: BrokerInput, id?: string) => Promise<void>; }`
- Slide-over drawer à direita (fixed inset-y-0 right-0, w-full max-w-md), overlay preto/40, `dark:bg-gray-900 dark:border-gray-800`.
- Form com inputs full_name (required), phone, email (type=email), role (select: Sênior/Júnior/Gerente ou input text — decidir simples).
- Título: "Novo Corretor" se `broker === null` else "Editar Corretor".
- Botão "Salvar" chama onSave (com id se edição); estado `saving` com disabled + label "Salvando...".
- Classes: `rounded-lg border border-gray-300 bg-transparent px-3 py-2 text-sm dark:border-gray-700 dark:bg-white/[0.03] dark:text-white/90` nos inputs.

3) `CorretoresClient.tsx` (client component):
- `"use client"`.
- Importar `useTenant`, `createClient`, `Broker`, `BrokerInput`, `CorretorDrawer`.
- Estado: `brokers: Broker[]`, `loading`, `drawerOpen`, `editingBroker: Broker | null`.
- useEffect: fetch corretores da TABELA DECIDIDA no Task 1 filtrando `.eq("tenant_id", tenant.id)` e `.order("created_at", { ascending: false })`. Padrão cancelled-flag como em ImoveisClient.
- Handler `handleSave(input, id?)`:
  - Se id: `supabase.from(TABLE).update({ ...input, updated_at: new Date().toISOString() }).eq("id", id).eq("tenant_id", tenant.id)`.
  - Senão: `supabase.from(TABLE).insert({ ...input, tenant_id: tenant.id }).select().single()`.
  - Após sucesso, refetch lista (ou otimista) e fechar drawer.
- UI: header "Corretores" + subtítulo "Equipe comercial do tenant"; botão "+ Novo Corretor" (abre drawer com broker=null); tabela com colunas Nome | Telefone | Email | Função | Ações (Editar).
- Tabela no padrão `rounded-2xl border bg-white dark:bg-white/[0.03] dark:border-gray-800`, thead `text-xs uppercase text-gray-500`, rows hover.
- Empty state: "Nenhum corretor cadastrado ainda. Clique em + Novo Corretor para começar."
- Loading skeleton (3 linhas pulse).
- IMPORTANTE: nome da tabela deve ser constante no topo do arquivo (`const BROKERS_TABLE = "brokers"` ou `"profiles"`) — definido pela decisão do Task 1.
- Se a decisão foi `profiles` com role='broker', o filtro `select` deve incluir `.eq("role", "broker")` e insert deve setar `role: "broker"` automaticamente (campo role do formulário mapeia para subcampo tipo `broker_role` ou outro — esclarecer no checkpoint).

Reutilizar padrões já presentes em `LeadDrawer.tsx`/`ImoveisClient.tsx` — NÃO inventar estilo novo.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
- Arquivos existem e compilam sem erro TypeScript.
- `CorretoresClient` usa `useTenant().tenant.id` (não hardcode como ImoveisClient faz na linha 150 — esta é a correção a seguir).
- Drawer abre/fecha e form submete via onSave.
- Sem imports de mock data.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar rota /cockpit/corretores e validar com checkpoint humano</name>
  <files>
    src/app/cockpit/corretores/page.tsx
  </files>
  <action>
Criar `src/app/cockpit/corretores/page.tsx`:

```tsx
import CorretoresClient from "@/components/yzihub/CorretoresClient";

export default function CorretoresPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-bold text-gray-800 dark:text-white/90">Corretores</h1>
        <p className="mt-1 text-sm text-gray-500 dark:text-gray-400">
          Gestão da equipe comercial do tenant
        </p>
      </div>
      <CorretoresClient />
    </div>
  );
}
```

Notas:
- NÃO criar entrada de sidebar nesta task (fora de escopo — quick atômico). Usuário acessa digitando URL; entrada de menu é follow-up.
- Validar build: `rtk next build` deve passar. Se falhar por outro motivo não relacionado a corretores (erro preexistente), reportar e seguir.
  </action>
  <verify>
    <automated>rtk tsc --noEmit && rtk next build</automated>
  </verify>
  <done>
- Rota /cockpit/corretores renderiza sem erro.
- `rtk tsc --noEmit` passa.
- `rtk next build` passa (ou falhas são preexistentes e documentadas).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificar fluxo CRUD real no navegador</name>
  <what-built>
Página /cockpit/corretores com listagem, criação e edição de corretores via Supabase real, filtrada por tenant ativo do usuário logado.
  </what-built>
  <how-to-verify>
1. `rtk pnpm dev` (ou equivalente já configurado) e abrir http://localhost:3000/cockpit/corretores logado como um usuário do tenant Jurema.
2. Confirmar: página carrega sem erro de console, mostra empty state OU corretores existentes do tenant.
3. Clicar "+ Novo Corretor". Preencher full_name="Teste Eric", phone="85999999999", email="teste@yzi.dev", role="Sênior". Salvar.
4. Confirmar: drawer fecha, item aparece na tabela. Recarregar página → item persiste (prova Supabase real).
5. Verificar no Supabase Studio (tabela decidida no Task 1) que a row existe com `tenant_id` correto.
6. Clicar "Editar" na row criada. Alterar phone para "85888888888". Salvar.
7. Confirmar: tabela mostra novo telefone; refresh mantém.
8. Logar como outro tenant (Café com Pam) — confirmar que o corretor de Jurema NÃO aparece (isolamento por tenant_id).
  </how-to-verify>
  <resume-signal>Type "approved" se todos os passos passarem, ou descrever o problema encontrado</resume-signal>
</task>

</tasks>

<verification>
- TypeScript sem erros: `rtk tsc --noEmit`
- Build Next.js passa: `rtk next build`
- CRUD funcional no navegador contra Supabase real (checkpoint humano)
- Isolamento por tenant_id confirmado visualmente
</verification>

<success_criteria>
- /cockpit/corretores lista, cria e edita corretores do tenant ativo
- Zero mock data — toda persistência via Supabase client
- Componentes em `src/components/yzihub/` seguindo padrão TailAdmin dark existente
- `useTenant()` usado corretamente (sem hardcode de tenant_id)
- Decisão da tabela (brokers vs profiles) documentada no SUMMARY
</success_criteria>

<output>
Ao completar, criar `.planning/quick/260414-o6s-criar-m-dulo-de-corretores/260414-o6s-SUMMARY.md` registrando:
- Tabela escolhida e por quê (Task 1)
- Se criou migration (e link)
- Arquivos criados
- Known follow-ups: adicionar entrada no sidebar, adicionar soft-delete, vincular corretor a leads (handoff 260409-7j3)
</output>
