---
phase: quick-260416-ln6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cockpit/pipeline/page.tsx
  - src/components/yzihub/CorretoresClient.tsx
  - src/components/yzihub/CorretorDrawer.tsx
autonomous: true
requirements:
  - LN6-01  # CorretorDrawer/form persiste broker em Supabase com tenant_id
  - LN6-02  # is_active honrado — apenas corretores ativos disponiveis para handoff
must_haves:
  truths:
    - "Criar novo corretor no CorretorDrawer insere row em brokers com tenant_id do tenant atual"
    - "Editar corretor atualiza row em brokers restrito por tenant_id"
    - "Toggle is_active=false no drawer persiste e remove broker da lista de handoff (AssignBrokerModal/PipelineHeader filter)"
    - "Pagina /cockpit/pipeline consulta brokers (nao corretores) filtrando is_active=true"
  artifacts:
    - path: "src/app/cockpit/pipeline/page.tsx"
      provides: "Server-side fetch de brokers ativos (tabela brokers + is_active=true)"
      contains: ".from(\"brokers\")"
    - path: "src/components/yzihub/CorretoresClient.tsx"
      provides: "CRUD brokers via supabase client (insert/update/delete escopado por tenant_id)"
      contains: "BROKERS_TABLE"
    - path: "src/components/yzihub/CorretorDrawer.tsx"
      provides: "Form controlado com toggle is_active + validacao full_name"
      contains: "is_active"
  key_links:
    - from: "src/components/yzihub/CorretorDrawer.tsx"
      to: "CorretoresClient.handleSave"
      via: "onSave(input, id?) prop"
      pattern: "onSave\\("
    - from: "src/components/yzihub/CorretoresClient.tsx"
      to: "brokers table (supabase)"
      via: "createClient().from(BROKERS_TABLE)"
      pattern: "\\.from\\(BROKERS_TABLE\\)"
    - from: "src/app/cockpit/pipeline/page.tsx"
      to: "brokers table (supabase) filtered by is_active"
      via: "supabase.from('brokers').eq('is_active', true)"
      pattern: "\\.eq\\(\"is_active\", true\\)"
---

<objective>
Fechar a ponta solta entre CorretorDrawer e Supabase garantindo que (1) o form do CorretorDrawer salva/atualiza na tabela `brokers` escopado por `tenant_id`, (2) apenas corretores com `is_active=true` ficam disponiveis para handoff no pipeline.

Purpose: Hoje o drawer ja persiste corretamente via CorretoresClient, mas o fluxo de handoff em `/cockpit/pipeline` consulta uma tabela errada (`corretores`, que nao existe no schema) e nao filtra por `is_active`. Resultado: dropdown de atribuicao fica vazio (ou mostra corretores inativos quando a query cair pra fallback). Precisamos padronizar tudo em `brokers` + `is_active=true` no canal de handoff, e adicionar validacao final no drawer.

Output:
- `src/app/cockpit/pipeline/page.tsx` lendo `brokers` (tabela correta) com `is_active=true`.
- `CorretorDrawer` com validacao defensiva (full_name nao-vazio, trim, telefone opcional normalizado).
- `CorretoresClient.handleSave` com trim + default `is_active=true` no insert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/types/brokers.ts
@src/app/api/brokers/route.ts

<interfaces>
<!-- Contratos ja existentes — executor usa direto, sem explorar codebase -->

From src/types/brokers.ts:
```typescript
export interface Broker {
  id: string;
  tenant_id: string;
  full_name: string;
  phone: string | null;
  email: string | null;
  role: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string | null;
}
export type BrokerInput = Pick<Broker, "full_name" | "phone" | "email" | "role" | "is_active">;
```

From src/components/yzihub/CorretorDrawer.tsx:
```typescript
interface CorretorDrawerProps {
  open: boolean;
  broker: Broker | null;
  onClose: () => void;
  onSave: (input: BrokerInput, id?: string) => Promise<void>;
  onDelete?: (id: string) => Promise<void> | void;
}
```

Tabela no Supabase: `brokers` (NAO `corretores`). Schema relevante: id, tenant_id, full_name, phone, email, role, is_active (boolean default true), created_at, updated_at.

Pattern estabelecido (quick-260416-lac/dj5): GET /api/brokers ordena `is_active desc, created_at desc`. Insert via client deve sempre incluir `tenant_id: tenant.id`. Update sempre filtra por `.eq("tenant_id", tenant.id)` alem do id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir fetch de brokers em /cockpit/pipeline (tabela correta + filtro is_active)</name>
  <files>src/app/cockpit/pipeline/page.tsx</files>
  <action>
  Atualmente o server component consulta `.from("corretores")` (tabela que nao existe no schema YZI — a tabela real e `brokers`). Alterar na funcao `fetchPipelineData`:

  1. Trocar `supabase.from("corretores").select("id, full_name").eq("tenant_id", tenantId)` por:
     ```ts
     supabase
       .from("brokers")
       .select("id, full_name")
       .eq("tenant_id", tenantId)
       .eq("is_active", true)
       .order("full_name", { ascending: true }),
     ```
  2. Manter o mapeamento existente `brokersRes.data.map((b) => ({ id: b.id, name: b.full_name }))`.
  3. Nao mudar mais nada no arquivo — so essa query.

  Motivo: garante LN6-02 (apenas ativos no handoff) e alinha com a tabela unica `brokers` usada pelo resto do sistema (CorretoresClient, /api/brokers).
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
  - Query usa `.from("brokers")` com `.eq("is_active", true)`.
  - `rtk tsc --noEmit` passa sem erros.
  - Manual: abrir `/cockpit/pipeline` como Jurema e confirmar que o filtro de corretor (PipelineHeader) e o AssignBrokerModal mostram apenas corretores ativos da Jurema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hardening CorretorDrawer + CorretoresClient.handleSave (validacao + defaults)</name>
  <files>src/components/yzihub/CorretorDrawer.tsx, src/components/yzihub/CorretoresClient.tsx</files>
  <action>
  O drawer ja chama `onSave(input, id)` e o CorretoresClient ja faz insert/update escopado por `tenant_id`. Adicionar hardening defensivo para LN6-01:

  **Em `src/components/yzihub/CorretorDrawer.tsx` (handleSubmit):**
  - Antes de chamar `onSave`, validar: se `full_name.trim().length < 2`, setar `saveError=true` e retornar sem chamar onSave (hoje so checa `!form.full_name.trim()` mas aceita 1 char).
  - Normalizar phone: manter apenas digitos. Substituir:
    ```ts
    phone: form.phone?.trim() || null,
    ```
    por:
    ```ts
    phone: form.phone?.replace(/\D/g, "") || null,
    ```
  - Nao mexer em mais nada do drawer.

  **Em `src/components/yzihub/CorretoresClient.tsx` (handleSave):**
  - No branch de insert (quando `id` e undefined), garantir default `is_active=true` caso input venha como undefined (defesa contra futuras mudancas de shape). Trocar:
    ```ts
    .insert({ ...input, tenant_id: tenant.id })
    ```
    por:
    ```ts
    .insert({
      ...input,
      is_active: input.is_active ?? true,
      tenant_id: tenant.id,
    })
    ```
  - Tambem adicionar guard early-return no comeco de `handleSave`: se `!input.full_name || input.full_name.trim().length < 2`, fazer `throw new Error("Nome invalido")` antes de chamar supabase (defesa em profundidade — drawer ja valida, mas isso protege chamadas futuras da funcao).

  Nenhuma outra mudanca. Nao refatorar, nao adicionar logging extra.
  </action>
  <verify>
    <automated>rtk tsc --noEmit && rtk next build</automated>
  </verify>
  <done>
  - CorretorDrawer rejeita `full_name` com menos de 2 chars (exibe mensagem de erro, nao chama onSave).
  - Phone e normalizado para apenas digitos antes do save.
  - Insert em brokers sempre carrega `is_active` explicito (default true) e `tenant_id`.
  - `rtk tsc --noEmit` passa.
  - `rtk next build` passa.
  - Manual: criar corretor novo com nome vazio/1-char -> bloqueio no drawer. Criar com nome valido -> aparece na lista com status Ativo. Editar e desativar -> broker desaparece do AssignBrokerModal em /cockpit/pipeline (apos refresh).
  </done>
</task>

</tasks>

<verification>
1. `rtk tsc --noEmit` passa limpo.
2. `rtk next build` passa.
3. Smoke manual (Jurema dev):
   - `/cockpit/corretores` -> criar corretor "Teste QA" ativo -> aparece na tabela.
   - `/cockpit/pipeline` -> filtro brokers inclui "Teste QA".
   - Voltar em corretores -> editar "Teste QA" e desativar (toggle is_active).
   - `/cockpit/pipeline` (refresh) -> filtro brokers NAO lista mais "Teste QA".
4. Verificar no Supabase que row em `brokers` tem `tenant_id = b179ae75-3d56-4de8-8840-fc9c4d9ec21e` (Jurema).
</verification>

<success_criteria>
- [ ] Pipeline page consulta tabela `brokers` com `is_active=true` (nao `corretores`).
- [ ] CorretorDrawer valida full_name >= 2 chars antes de salvar.
- [ ] Phone salva normalizado (apenas digitos).
- [ ] Insert em brokers sempre inclui tenant_id + is_active explicito.
- [ ] `rtk tsc --noEmit` e `rtk next build` passam.
- [ ] Smoke manual confirma que corretor inativo some do handoff.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-ln6-integra-o-do-corretordrawer-form-com-sup/260416-ln6-SUMMARY.md` documentando:
- Diff aplicado em cada arquivo (1-line por arquivo).
- Resultado do smoke manual (criar/editar/desativar + verificar no pipeline).
- Commit hash.
</output>
