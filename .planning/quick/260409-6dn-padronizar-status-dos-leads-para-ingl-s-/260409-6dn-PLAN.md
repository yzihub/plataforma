---
phase: quick
plan: 260409-6dn
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
  - .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json
  - .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "All workflow JSON files use only DB-valid English status values: new, contacted, qualified, proposal, won, lost"
    - "No Portuguese status values (novo, contatado, qualificando, lead_quente) remain in any workflow file"
    - "No invented slug values (start, hot_lead, qualifying, visit_requested) are written to DB"
    - "Internal urgencyMap keys are updated to match new English status values"
  artifacts:
    - path: ".planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json"
      provides: "setar_lead_quente workflow with status='qualified' instead of 'lead_quente'"
      contains: "qualified"
    - path: ".planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json"
      provides: "Main Luana workflow with all status values mapped to English DB enum"
      contains: "new"
    - path: ".planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js"
      provides: "Fix script with corrected status values"
      contains: "new"
  key_links:
    - from: "workflow JSON status values"
      to: "Supabase leads.status column"
      via: "HTTP PATCH/POST body"
      pattern: "status.*'(new|contacted|qualified|proposal|won|lost)'"
---

<objective>
Padronizar todos os valores de status nos workflows n8n da Luana (Jurema Brokers) para usar apenas valores validos no DB Supabase (new, contacted, qualified, proposal, won, lost).

Purpose: O DB rejeita com HTTP 400 valores como 'start', 'lead_quente', 'qualifying', 'hot_lead', 'visit_requested' que estao presentes nos workflows. Precisamos substituir por valores do enum ingles.
Output: 3 arquivos JSON/JS atualizados com status padronizado.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Mapeamento de status (valores atuais -> valores DB validos):
- 'start' -> 'new'
- 'qualifying' -> 'contacted'
- 'lead_quente' -> 'qualified'
- 'hot_lead' -> 'qualified'
- 'visit_requested' -> 'proposal'

Valores DB validos (enum na tabela leads): new, contacted, qualified, proposal, won, lost

Arquivos afetados e localizacao dos valores:

1. setar_lead_quente.json (Build Metadata node, linha ~67):
   - `status: 'lead_quente'` -> `status: 'qualified'`

2. luana-agent-workflow-fixed.json:
   - Create a record1 node (linha ~2670): `status: 'start'` -> `status: 'new'`
   - Contexto Inicial (2 ocorrencias, linhas ~2748 e ~9075): `STATUS: start` e `_status_slug: 'start'` -> `'new'`
   - Dados do Lead node (linha ~3765): `lead.status || 'start'` -> `lead.status || 'new'`
   - urgencyMap keys no Dados do Lead: `hot_lead` -> `qualified`, `visit_requested` -> `proposal`, `start` -> `new`
   - statusMap no legacy Code node (linha ~10092): `'start'` -> `'new'`, `'qualifying'` -> `'contacted'`, `'hot_lead'` -> `'qualified'`, `'visit_requested'` -> `'proposal'`
   - urgencyMap no legacy Code node: mesmas substituicoes

3. fix-workflow.js:
   - Linha 97: `status: 'start'` -> `status: 'new'`
   - Linha 135: `lead.status || 'start'` -> `lead.status || 'new'`
   - urgencyMap (linhas 174-177): mesmas substituicoes de keys
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar status no setar_lead_quente.json e luana-agent-workflow-fixed.json</name>
  <files>
    .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
    .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json
  </files>
  <action>
    REGRA: Nao alterar logica, nao alterar nodes, nao alterar connections, nao alterar positions. APENAS substituir valores de status string.

    **setar_lead_quente.json:**
    No node "Build Metadata" (jsCode), substituir:
    - `status: 'lead_quente'` -> `status: 'qualified'`

    **luana-agent-workflow-fixed.json:**

    A. Node "Create a record1" (body JSON.stringify):
    - `status: 'start'` -> `status: 'new'`

    B. Nodes "Contexto Inicial" (existem 2 ocorrencias identicas no workflow):
    - `STATUS: start` -> `STATUS: new` (dentro da template string ctx)
    - `_status_slug: 'start'` -> `_status_slug: 'new'`

    C. Node "Dados do Lead" (primeira ocorrencia, branch Supabase):
    - `lead.status || 'start'` -> `lead.status || 'new'`
    - urgencyMap keys: `hot_lead:` -> `qualified:`, `visit_requested:` -> `proposal:`, `start:` -> `new:`

    D. Node legacy Code (segunda ocorrencia do "Dados do Lead", branch Airtable):
    - statusMap values: `'start'` -> `'new'`, `'qualifying'` -> `'contacted'`, `'hot_lead'` -> `'qualified'`, `'visit_requested'` -> `'proposal'`
    - urgencyMap keys: `hot_lead:` -> `qualified:`, `visit_requested:` -> `proposal:`, `start:` -> `new:`
    - `lead.status || 'start'` -> `lead.status || 'new'` (se existir neste bloco)

    CUIDADO: Os jsCode estao em strings JSON (dentro de aspas, com escapes \\n). Editar com precisao para nao quebrar o JSON. Usar ferramentas de busca e substituicao textual precisa.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && node -e "const fs=require('fs'); const f1=JSON.parse(fs.readFileSync('.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json','utf8')); const f2=JSON.parse(fs.readFileSync('.planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-fixed.json','utf8')); const all=JSON.stringify(f1)+JSON.stringify(f2); const bad=['lead_quente','start','qualifying','hot_lead','visit_requested']; const found=bad.filter(b=>all.includes(\"'\"+b+\"'\")||all.includes('\"'+b+'\"')); if(found.length>0){console.error('FAIL: old status values found:',found);process.exit(1)}else{console.log('PASS: no old status values found');} const good=['new','contacted','qualified','proposal']; const missing=good.filter(g=>!all.includes(g)); if(missing.length>0){console.error('WARN: expected values not found:',missing)} else{console.log('PASS: all expected status values present')}"</automated>
  </verify>
  <done>
    - setar_lead_quente.json usa status='qualified' em vez de 'lead_quente'
    - luana-agent-workflow-fixed.json usa apenas valores do enum ingles (new, contacted, qualified, proposal)
    - Nenhum valor antigo (start, qualifying, hot_lead, visit_requested, lead_quente) permanece nos arquivos
    - Ambos os JSONs continuam validos (parseable)
  </done>
</task>

<task type="auto">
  <name>Task 2: Padronizar status no fix-workflow.js</name>
  <files>
    .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js
  </files>
  <action>
    REGRA: Nao alterar logica, apenas substituir valores de status string.

    No fix-workflow.js, substituir:
    - Linha 97 (Create a record1 body): `status: 'start'` -> `status: 'new'`
    - Linha 135 (Dados do Lead jsCode): `lead.status || 'start'` -> `lead.status || 'new'`
    - urgencyMap keys (linhas 174-177):
      - `hot_lead:` -> `qualified:`
      - `visit_requested:` -> `proposal:`
      - `start:` -> `new:`

    NOTA: Este arquivo e um script JS (nao JSON), entao a edicao e mais simples — substituicoes diretas em strings JS.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && grep -c "'start'" .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js | xargs -I{} test {} -eq 0 && echo "PASS: no 'start' found" || echo "FAIL: 'start' still present" && grep -c "'hot_lead'" .planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/fix-workflow.js | xargs -I{} test {} -eq 0 && echo "PASS: no 'hot_lead' found" || echo "FAIL: 'hot_lead' still present"</automated>
  </verify>
  <done>
    - fix-workflow.js usa apenas valores do enum ingles (new, contacted, qualified, proposal)
    - Nenhum valor antigo (start, hot_lead, visit_requested) permanece
    - Script continua sintaticamente valido (node fix-workflow.js nao da syntax error)
  </done>
</task>

</tasks>

<verification>
Verificacao global:
1. Todos os 3 arquivos sao parseaveis (JSON valido para .json, JS valido para .js)
2. grep -rn "lead_quente\|'start'\|'qualifying'\|'hot_lead'\|'visit_requested'" nos 3 arquivos retorna zero resultados
3. grep -rn "'new'\|'contacted'\|'qualified'\|'proposal'" confirma presenca dos valores corretos
</verification>

<success_criteria>
- Zero ocorrencias de status em portugues ou slugs inventados nos 3 arquivos de workflow
- Todos os status escritos no DB usam valores do enum: new, contacted, qualified, proposal, won, lost
- Nenhuma alteracao em logica, nodes, connections ou positions dos workflows
- Todos os arquivos continuam validos (JSON parseable, JS sem syntax error)
</success_criteria>

<output>
After completion, create `.planning/quick/260409-6dn-padronizar-status-dos-leads-para-ingl-s-/260409-6dn-SUMMARY.md`
</output>
