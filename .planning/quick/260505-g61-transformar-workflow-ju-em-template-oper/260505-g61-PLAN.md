---
phase: quick-260505-g61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md
  - .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/TEMPLATE_GUIDE.md
  - .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json
autonomous: false
requirements:
  - WF-TEMPLATE-01  # Workflow operacional Ju transformado em template clonável YZI IMOB
  - WF-TEMPLATE-02  # Isolamento por tenant/client_slug, sem mistura de RAGs
  - WF-TEMPLATE-03  # System Prompt do AI parametrizado (agent_name, business_name, contexto, histórico, regras, RAG)
  - WF-TEMPLATE-04  # Persistência inbound/outbound no Supabase + respeito a ai_paused
  - WF-TEMPLATE-05  # Remoção de nodes Airtable e referências hardcoded (Luana/PAM)
user_setup:
  - service: n8n
    why: "Acesso ao workflow JzEtJ1MpAXx6EMTp via MCP n8n para refatoração"
    dashboard_config:
      - task: "Validar visualmente o workflow refatorado em https://app.yzihub.com/workflow/JzEtJ1MpAXx6EMTp após mudanças via MCP"
        location: "n8n editor — app.yzihub.com"

must_haves:
  truths:
    - "Workflow Ju não contém referências hardcoded a 'Luana', 'PAM' ou nomes específicos de cliente"
    - "Workflow Ju não contém nodes Airtable (apenas Supabase + Evolution + AI)"
    - "System Prompt do AI usa variáveis parametrizadas ({{agent_name}}, {{business_name}}, contexto, histórico, RAG)"
    - "Workflow salva mensagem inbound no Supabase ANTES de chamar a IA"
    - "Workflow respeita lead.ai_paused — se true, não chama IA"
    - "Workflow salva mensagem outbound no Supabase e atualiza last_message/last_message_at"
    - "Cada node tem nome descritivo (sem emojis aleatórios, sem nomes de cliente)"
    - "Configuração centralizada em Set node ou sticky note no topo (tenant_id, client_slug, agent_name, business_name)"
    - "Existe documentação operacional explicando como clonar o template para um novo cliente IMOB"
  artifacts:
    - path: ".planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md"
      provides: "Mapa completo do workflow atual: nodes, conexões, hardcodes, débitos técnicos"
      min_lines: 50
    - path: ".planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/TEMPLATE_GUIDE.md"
      provides: "Arquitetura do template + guia de clonagem por cliente"
      min_lines: 80
      contains: "client_slug"
    - path: ".planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json"
      provides: "JSON do workflow refatorado (snapshot pós-MCP update)"
  key_links:
    - from: "n8n workflow JzEtJ1MpAXx6EMTp"
      to: "Supabase leads.ai_paused"
      via: "node 'Verificar ai_paused' (IF node lendo lead antes da IA)"
      pattern: "ai_paused"
    - from: "n8n workflow JzEtJ1MpAXx6EMTp"
      to: "AI Agent System Prompt"
      via: "Set node 'Configuração Cliente' centralizando agent_name/business_name/tenant_id"
      pattern: "\\{\\{.*agent_name.*\\}\\}"
    - from: "TEMPLATE_GUIDE.md"
      to: "AUDIT.md"
      via: "referência cruzada: cada node renomeado documentado em ambos"
      pattern: "Set node.*Configuração"
---

<objective>
Transformar o workflow atual da Ju (n8n ID `JzEtJ1MpAXx6EMTp`) em um **template operacional reutilizável YZI IMOB**, isolado por tenant/client_slug e clonável para qualquer cliente IMOB apenas trocando variáveis de configuração.

Purpose: Hoje o workflow é um caso isolado da Jurema com nomes hardcoded ("Luana", "PAM"), nodes Airtable legados e prompt monolítico. Para escalar YZI IMOB para múltiplos clientes (vertical imobiliária), o workflow precisa virar template — base clonável com variáveis no topo, prompt parametrizado e isolamento garantido entre RAGs.

Output:
1. `AUDIT.md` — diagnóstico completo do workflow atual (nodes, hardcodes, débitos)
2. Workflow refatorado no n8n (via MCP `n8n_update_full_workflow`)
3. `workflow-template.json` — snapshot do workflow pós-refatoração
4. `TEMPLATE_GUIDE.md` — guia operacional de clonagem por cliente
</objective>

<execution_context>
@C:\Users\Eric\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Eric\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<!-- Contexto crítico do CLAUDE.md (resumo): -->
<!-- - Backend Agno/YZI OS é fonte da verdade — n8n faz INTEGRAÇÃO operacional, não regra de negócio -->
<!-- - Nunca usar service_role no frontend; n8n pode usar Supabase com chave própria -->
<!-- - Tabela leads tem campos: tenant_id, phone, ai_paused, ai_status, ai_temperature, ai_last_summary, ai_last_intent -->
<!-- - Tabela jurema_deals: tenant_id, lead_id, deal_stage, qualification_status, lead_score, missing_fields, broker_status -->
<!-- - Workflow atual (JzEtJ1MpAXx6EMTp) é da Ju/Jurema — tenant 82cc7aa9-fc6e-4f37-8d8e-8a71c1691361 -->
<!-- - Tarefas similares completadas: 260408-rqi (UNIQUE leads), 260408-sow (migrar Luana Airtable→Supabase) -->

<interfaces>
<!-- Schema relevante das tabelas Supabase que o workflow usa -->

Tabela `leads` (campos consumidos/atualizados pelo workflow):
```
id            UUID
tenant_id     UUID         # FILTRO obrigatório por cliente
phone         TEXT         # phone_normalized para match
phone_normalized TEXT
name          TEXT
ai_paused     BOOLEAN      # SE true, NÃO chamar IA (modo humano)
ai_status     TEXT         # idle | thinking | replied
ai_last_summary TEXT
ai_last_intent  TEXT
ai_temperature  TEXT       # frio | morno | quente
metadata      JSONB
created_at    TIMESTAMPTZ
updated_at    TIMESTAMPTZ
```

Tabela `conversations` (uma por lead, atualizar last_message):
```
id              UUID
tenant_id       UUID
lead_id         UUID
last_message    TEXT
last_message_at TIMESTAMPTZ
metadata        JSONB
```

Tabela `messages` (histórico — recuperar últimas N para contexto da IA):
```
id              UUID
tenant_id       UUID
conversation_id UUID
lead_id         UUID
direction       TEXT         # inbound | outbound
content         TEXT
sender          TEXT         # lead | agent | human
metadata        JSONB        # { agent_name, model, etc }
created_at      TIMESTAMPTZ
```

MCP n8n disponível para o executor:
- mcp__n8n__n8n_get_workflow (workflowId) — obter JSON completo
- mcp__n8n__n8n_update_full_workflow (workflowId, nodes, connections) — substituir workflow
- mcp__n8n__n8n_validate_workflow — validar antes de aplicar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar workflow atual da Ju via MCP n8n e mapear hardcodes</name>
  <files>.planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md</files>
  <action>
Use o MCP n8n para buscar o workflow atual:

```
mcp__n8n__n8n_get_workflow(workflowId="JzEtJ1MpAXx6EMTp")
```

A partir do JSON retornado, produza `AUDIT.md` com as seções:

1. **Visão geral**: nome do workflow, total de nodes, status (active/inactive), trigger principal
2. **Mapa de nodes** (tabela Markdown): para cada node listar `id | name | type | propósito atual | hardcodes encontrados`
3. **Hardcodes detectados**: listar TODAS as ocorrências de:
   - "Luana", "PAM", "Pam", "Jurema" (em nomes de nodes, prompts, mensagens estáticas)
   - tenant_id literal `82cc7aa9-fc6e-4f37-8d8e-8a71c1691361` em expressões/queries
   - URLs/credenciais hardcoded (Asaas, Evolution, Supabase URL específico)
   - Nomes de Airtable bases/tabelas
4. **Nodes Airtable a remover**: lista com `node id | substituto Supabase`
5. **System Prompt atual**: copiar o prompt do AI Agent INTEIRO (com indentação) em bloco de código
6. **Conexões críticas** (fluxo): diagrama em texto mostrando trigger → ... → response (ex: `Webhook Evolution → Normalizar phone → ...`)
7. **Débitos técnicos identificados**: o que precisa mudar para virar template (lista priorizada)
8. **Tabelas Supabase usadas**: listar quais tabelas o workflow lê/escreve hoje (leads, jurema_deals, etc)

NÃO modifique o workflow nesta task — apenas leitura e documentação.

Salve o JSON bruto retornado pelo MCP em `workflow-template.json` (no MESMO diretório) como baseline pré-refatoração — este arquivo será sobrescrito na Task 2 com a versão final.
  </action>
  <verify>
    <automated>test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md &amp;&amp; test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json &amp;&amp; grep -c "Luana\|Pam\|PAM\|hardcode" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md</automated>
  </verify>
  <done>
    - AUDIT.md existe com 8 seções preenchidas e tabela de nodes (mín 50 linhas)
    - workflow-template.json contém o JSON original do workflow JzEtJ1MpAXx6EMTp
    - Todos os hardcodes (Luana, Pam, tenant literal, Airtable) listados explicitamente
    - System Prompt do AI Agent copiado integralmente
  </done>
</task>

<task type="auto">
  <name>Task 2: Refatorar workflow no n8n para template parametrizado</name>
  <files>.planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json</files>
  <action>
Com base no `AUDIT.md` da Task 1, refatore o workflow JzEtJ1MpAXx6EMTp aplicando estas transformações OBRIGATÓRIAS:

**A. Adicionar Set node de configuração no topo (logo após o trigger)**
Nome: `Configuração Cliente` (sticky note opcional documentando)
Campos (todos string):
```
client_slug         = "jurema_brokers"
tenant_id           = "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"
agent_name          = "Ju"
business_name       = "Jurema Brokers"
business_segment    = "imobiliario"
rag_namespace       = "jurema_brokers_v1"     # ← isola RAG por cliente
history_limit       = 10                       # últimas N mensagens
supabase_url        = "{{ $env.SUPABASE_URL }}"      # NUNCA hardcodar
supabase_key        = "{{ $env.SUPABASE_KEY }}"
evolution_instance  = "{{ $env.EVOLUTION_INSTANCE_JUREMA }}"
```

Todos os nodes downstream DEVEM ler dessas variáveis via `{{ $('Configuração Cliente').item.json.X }}`.

**B. Renomear nodes** removendo TODAS as referências hardcoded encontradas no AUDIT:
- "Luana" → nome funcional do node (ex: "AI Agent IMOB", "Salvar Resposta")
- "PAM"/"Pam" → remover ou substituir pelo papel funcional
- Cada node deve ter nome descritivo do que FAZ, não de quem é
- Sugestões de nomes funcionais: `Webhook Evolution Inbound`, `Normalizar Telefone`, `Buscar Lead Supabase`, `Verificar ai_paused`, `Salvar Mensagem Inbound`, `Buscar Histórico Recente`, `Buscar RAG Cliente`, `AI Agent IMOB`, `Salvar Mensagem Outbound`, `Atualizar Conversation`, `Enviar via Evolution`

**C. Remover TODOS os nodes Airtable**
Para cada node Airtable identificado no AUDIT, substituir por equivalente Supabase via HTTP Request node (ou Supabase node nativo se já existe credencial). Conectar nas mesmas entradas/saídas.

**D. Adicionar fluxo operacional completo (se ainda não existir):**
Sequência obrigatória após webhook:
1. `Normalizar Telefone` (E.164)
2. `Buscar Lead Supabase` (filtrado por `tenant_id` + `phone_normalized`)
3. `Salvar Mensagem Inbound` (INSERT em `messages` com direction='inbound') — **ANTES da IA**
4. `Verificar ai_paused` (IF node) — se `lead.ai_paused === true`, ir para branch "Modo Humano" (apenas atualiza conversation, não chama IA)
5. Branch IA:
   - `Buscar Histórico Recente` (SELECT últimas N messages do conversation_id)
   - `Buscar RAG Cliente` (consulta vector store filtrada por `rag_namespace`)
   - `AI Agent IMOB` com System Prompt parametrizado (ver E)
   - `Salvar Mensagem Outbound` (INSERT em `messages` direction='outbound')
   - `Enviar via Evolution`
6. `Atualizar Conversation` (UPDATE last_message + last_message_at no conversation)

**E. Parametrizar System Prompt do AI Agent**
O System Prompt atual (do AUDIT) deve virar template com placeholders:
```
Você é {{agent_name}}, atendente virtual da {{business_name}} ({{business_segment}}).

Lead atual:
- Nome: {{lead.name}}
- Telefone: {{lead.phone}}
- Status: {{lead.ai_status}}
- Temperatura: {{lead.ai_temperature}}
- Último resumo: {{lead.ai_last_summary}}

Histórico recente (últimas {{history_limit}} mensagens):
{{historico_formatado}}

Base de conhecimento:
{{rag_context}}

Regras:
- Não duplicar regras do backend.
- Não inventar dados — usar apenas o RAG e histórico.
- ...
```
Use expressões n8n: `{{ $('Configuração Cliente').item.json.agent_name }}`, etc.

**F. Validar e aplicar via MCP**
1. Chamar `mcp__n8n__n8n_validate_workflow` para verificar consistência
2. Se válido, chamar `mcp__n8n__n8n_update_full_workflow` com nodes/connections refatorados
3. Re-baixar com `mcp__n8n__n8n_get_workflow` e salvar em `workflow-template.json` (sobrescrevendo o snapshot da Task 1)

**Restrições:**
- Não tocar em credenciais (referenciar via `$env`)
- Não criar pipeline novo do zero — refatorar o existente preservando lógica
- Não mexer no frontend nem no backend Python YZI OS
- Se algum hardcode dependente de lógica externa for arriscado de remover, deixar sticky note `# TODO TEMPLATE` documentando

Se o `n8n_update_full_workflow` falhar (validação ou permissão), pause como checkpoint e reporte o erro com diff JSON.
  </action>
  <verify>
    <automated>test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json &amp;&amp; ! grep -i "Luana\|airtable" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json &amp;&amp; grep -q "Configuração Cliente\|client_slug" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json &amp;&amp; grep -q "ai_paused" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json</automated>
  </verify>
  <done>
    - Workflow JzEtJ1MpAXx6EMTp atualizado no n8n via MCP (sem erros de validação)
    - workflow-template.json (snapshot final) NÃO contém: "Luana", "Pam"/"PAM", "airtable" (case-insensitive)
    - workflow-template.json contém Set node "Configuração Cliente" com client_slug, agent_name, business_name, rag_namespace
    - Nodes seguem fluxo: webhook → normalizar → buscar lead → salvar inbound → verificar ai_paused → (branch IA com histórico+RAG+prompt parametrizado) → salvar outbound → evolution → atualizar conversation
    - System Prompt usa expressões `{{ $('Configuração Cliente').item.json.* }}` (não strings literais "Ju", "Jurema Brokers")
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Documentar template + validação humana no n8n editor</name>
  <files>.planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/TEMPLATE_GUIDE.md</files>
  <what-built>
Workflow Ju refatorado para template YZI IMOB com:
- Set node `Configuração Cliente` centralizando 9+ variáveis
- Nodes renomeados sem referências a "Luana"/"Pam"
- Nodes Airtable substituídos por Supabase
- Fluxo operacional completo (inbound→ai_paused→IA→outbound)
- System Prompt parametrizado por cliente
- Snapshot JSON salvo em workflow-template.json
  </what-built>
  <action>
**Antes do checkpoint humano**, criar `TEMPLATE_GUIDE.md` com as seções:

1. **Visão geral do template YZI IMOB**
   - Propósito (workflow base clonável para qualquer cliente IMOB)
   - Garantias de isolamento (tenant_id, rag_namespace, client_slug)

2. **Arquitetura** (referenciar AUDIT.md para mapa de nodes)
   - Diagrama em texto do fluxo principal
   - Tabela: cada node | função | inputs | outputs

3. **Configuração Cliente — variáveis explicadas**
   - Tabela: variável | tipo | exemplo Jurema | obrigatória? | onde é usada
   - Listar TODAS as 9+ variáveis do Set node

4. **Como clonar para um novo cliente IMOB** (passo a passo)
   ```
   1. Duplicar workflow no n8n (UI: Save as)
   2. Renomear: "[CLIENT] Operacional IMOB"
   3. Editar Set node "Configuração Cliente":
      - client_slug      = "novo_cliente"
      - tenant_id        = "<UUID do tenant no Supabase>"
      - agent_name       = "<Nome do agente, ex: Ana>"
      - business_name    = "<Nome do cliente, ex: Imobiliária X>"
      - rag_namespace    = "novo_cliente_v1"
      - evolution_instance = "<instance do WhatsApp do cliente>"
   4. Configurar credentials Supabase (se diferente)
   5. Configurar Vector Store namespace = rag_namespace
   6. Ativar workflow
   7. Smoke test: enviar mensagem de teste no WhatsApp
   ```

5. **Garantias de isolamento entre clientes**
   - tenant_id em TODA query Supabase
   - rag_namespace em TODA consulta vector store
   - evolution_instance separada por cliente
   - Nunca compartilhar Set node entre workflows

6. **Pontos de extensão futura**
   - Handoff humano (já preparado via ai_paused)
   - Múltiplos agentes por cliente (variar agent_name por entrypoint)
   - Métricas (agent_metrics_events)

7. **Referências**
   - Link AUDIT.md
   - Link CLAUDE.md (regras de tenant/isolamento)
   - n8n workflow: https://app.yzihub.com/workflow/JzEtJ1MpAXx6EMTp

8. **Checklist de validação por cliente** (markdown checklist)
   - [ ] tenant_id correto no Set node
   - [ ] rag_namespace único e populado
   - [ ] evolution_instance configurada
   - [ ] credentials Supabase apontando para projeto certo
   - [ ] Smoke test executado
   - [ ] ai_paused funciona (toggle e verificar IA não responde)
  </action>
  <how-to-verify>
**Após Claude criar TEMPLATE_GUIDE.md, verificar humanamente no n8n editor:**

1. Abrir https://app.yzihub.com/workflow/JzEtJ1MpAXx6EMTp
2. Confirmar visualmente:
   - [ ] Existe Set node "Configuração Cliente" no início (após trigger)
   - [ ] Set node tem ao menos: client_slug, tenant_id, agent_name, business_name, rag_namespace
   - [ ] Nenhum node tem "Luana", "Pam" ou "PAM" no nome
   - [ ] Nenhum node Airtable presente (procurar ícone Airtable)
   - [ ] AI Agent System Prompt usa `{{ $('Configuração Cliente').item.json.agent_name }}` (não literal "Ju")
   - [ ] Existe IF node verificando `ai_paused`
   - [ ] Mensagem inbound salva ANTES do AI Agent (procurar node "Salvar Mensagem Inbound" ou similar antes do AI)
3. Executar teste manual: enviar mensagem WhatsApp de teste (nº dev)
   - [ ] Lead criado/atualizado em `leads` com tenant_id correto
   - [ ] Mensagem inbound em `messages` (direction='inbound')
   - [ ] Resposta da IA chega no WhatsApp
   - [ ] Mensagem outbound em `messages` (direction='outbound')
   - [ ] conversation.last_message atualizado
4. Teste ai_paused: marcar `lead.ai_paused = true`, enviar nova mensagem
   - [ ] IA NÃO responde (apenas inbound salvo)

5. Conferir TEMPLATE_GUIDE.md tem 8 seções e checklist final
  </how-to-verify>
  <resume-signal>Digite "approved" se workflow + guide estão corretos, ou descreva o que precisa ajustar (ex: "node X ainda tem hardcode Y", "checklist incompleto")</resume-signal>
</task>

</tasks>

<verification>
**Verificação automatizada (executável):**
```
test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/AUDIT.md
test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/TEMPLATE_GUIDE.md
test -f .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json
! grep -i "Luana\|airtable" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json
grep -q "client_slug" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/TEMPLATE_GUIDE.md
grep -q "ai_paused" .planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/workflow-template.json
```

**Verificação manual (n8n editor):** ver Task 3 how-to-verify.
</verification>

<success_criteria>
1. **Auditoria completa** — AUDIT.md mapeia todos os nodes, hardcodes e débitos
2. **Workflow refatorado no n8n** — JzEtJ1MpAXx6EMTp atualizado via MCP, sem Airtable, sem Luana/Pam, com Set node de config no topo
3. **Fluxo operacional completo** — inbound salvo antes da IA, ai_paused respeitado, outbound salvo, conversation atualizada
4. **System Prompt parametrizado** — usa expressões `{{ $('Configuração Cliente').item.json.* }}`
5. **Documentação operacional** — TEMPLATE_GUIDE.md com guia de clonagem por cliente + checklist
6. **Validação humana** — usuário confirmou no n8n editor que template está pronto para clonagem
</success_criteria>

<output>
Após completar, criar `.planning/quick/260505-g61-transformar-workflow-ju-em-template-oper/260505-g61-SUMMARY.md` documentando:
- O que foi auditado/refatorado/documentado
- Lista de nodes renomeados (antes → depois)
- Lista de nodes Airtable removidos
- Variáveis do Set node criadas
- Próximos passos (clonar para próximo cliente IMOB)
- Atualizar STATE.md com este quick task em `Quick Tasks Completed`
</output>
