---
phase: quick
plan: 260409-7j3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
  - .planning/quick/260409-7j3-criar-handoff-do-lead-quente-para-corret/handoff_corretor.json
autonomous: true
requirements: [HANDOFF-MVP]
must_haves:
  truths:
    - "Apos setar_lead_quente executar, o workflow dispara handoff com resumo do lead para o corretor"
    - "O resumo contem: nome, telefone, objetivo, faixa_valor, bairro_interesse, imovel_ref, localizacao_visita"
    - "Os dados vem de metadata (campos dinamicos) + colunas fixas (phone, name, status)"
    - "O fluxo da Luana nao foi alterado — apenas saida adicional apos PATCH Lead"
  artifacts:
    - path: ".planning/quick/260409-7j3-criar-handoff-do-lead-quente-para-corret/handoff_corretor.json"
      provides: "Workflow n8n handoff_corretor completo"
    - path: ".planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json"
      provides: "setar_lead_quente com node adicional que chama handoff_corretor"
  key_links:
    - from: "setar_lead_quente (PATCH Lead output)"
      to: "handoff_corretor (Execute Sub-Workflow)"
      via: "n8n executeWorkflow node encadeado apos PATCH"
---

<objective>
Criar workflow n8n `handoff_corretor` e encadear no `setar_lead_quente` para enviar resumo estruturado do lead quente ao corretor.

Purpose: Quando a Luana marca um lead como quente (qualified), o corretor recebe automaticamente um resumo com dados relevantes para dar continuidade ao atendimento.
Output: Workflow `handoff_corretor.json` + `setar_lead_quente.json` atualizado com node de chamada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
@.planning/quick/260408-sub-refactor-luana-airtable-supabase/atualizar_qualificacao.json

<interfaces>
<!-- setar_lead_quente PATCH Lead output (Prefer: return=representation) -->
<!-- Returns the updated lead row as JSON: -->
{
  "id": "uuid",
  "tenant_id": "uuid",
  "phone": "5511999999999",
  "name": "Nome do Lead",
  "status": "qualified",
  "score": 3,
  "metadata": {
    "objetivo": "comprar apartamento",
    "faixa_valor": "500k-800k",
    "bairro_interesse": "Moema",
    "imovel_ref": "IMV-123",
    "localizacao_visita": "Rua X, 123",
    "session_id": "...",
    "message_type": "text"
  }
}

<!-- Supabase lead status enum: new, contacted, qualified, proposal -->
<!-- setar_lead_quente sets status='qualified', score=3 -->
<!-- Metadata fields are dynamic — not all may be present -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar workflow handoff_corretor.json</name>
  <files>.planning/quick/260409-7j3-criar-handoff-do-lead-quente-para-corret/handoff_corretor.json</files>
  <action>
Criar workflow n8n `handoff_corretor` com arquitetura linear de 3 nodes:

**Node 1 — `Dados do Lead` (executeWorkflowTrigger)**
- workflowInputs: phone, tenant_id, lead_name, lead_status, lead_score, metadata_json (string)
- Recebe dados ja resolvidos do setar_lead_quente (nao precisa consultar Supabase de novo)

**Node 2 — `Montar Resumo` (Code node)**
- Parse metadata_json (JSON.parse) com try/catch (fallback: {})
- Montar objeto `resumo` com campos extraidos:
  ```
  {
    titulo: `Lead Quente: ${lead_name}`,
    telefone: phone,
    nome: lead_name,
    status: lead_status,
    score: lead_score,
    objetivo: metadata.objetivo || 'Nao informado',
    faixa_valor: metadata.faixa_valor || 'Nao informado',
    bairro_interesse: metadata.bairro_interesse || 'Nao informado',
    imovel_ref: metadata.imovel_ref || null,
    localizacao_visita: metadata.localizacao_visita || null,
    data_handoff: new Date().toISOString()
  }
  ```
- Montar `mensagem_texto` formatada para WhatsApp/log:
  ```
  LEAD QUENTE - HANDOFF CORRETOR
  Nome: {nome}
  Telefone: {telefone}
  Objetivo: {objetivo}
  Faixa de Valor: {faixa_valor}
  Bairro: {bairro_interesse}
  Imovel Ref: {imovel_ref || 'Nenhum'}
  Local Visita: {localizacao_visita || 'Nao definido'}
  Data: {data_handoff}
  ```
- Output: { resumo, mensagem_texto, phone, tenant_id }

**Node 3 — `Enviar Handoff` (HTTP Request POST)**
- MVP: POST para webhook generico configuravel via n8n variable
- URL: `{{ $vars.HANDOFF_WEBHOOK_URL || 'https://httpbin.org/post' }}`
- Fallback para httpbin.org/post permite testar sem webhook real configurado
- Body JSON: { type: 'lead_handoff', tenant_id, resumo, mensagem_texto }
- Headers: Content-Type: application/json
- Se HANDOFF_WEBHOOK_URL nao estiver definida, o httpbin loggara o payload (MVP funcional)

Connections: Node 1 -> Node 2 -> Node 3 (linear)

Settings: executionOrder v1, mesmo padrao dos outros workflows.
  </action>
  <verify>
    <automated>node -e "const w = require('./.planning/quick/260409-7j3-criar-handoff-do-lead-quente-para-corret/handoff_corretor.json'); console.log('nodes:', w.nodes.length, 'connections:', Object.keys(w.connections).length); if(w.nodes.length !== 3) throw 'Expected 3 nodes'; console.log('PASS')"</automated>
  </verify>
  <done>handoff_corretor.json existe com 3 nodes (Trigger, Montar Resumo, Enviar Handoff) e connections validas</done>
</task>

<task type="auto">
  <name>Task 2: Encadear handoff no setar_lead_quente</name>
  <files>.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json</files>
  <action>
Adicionar node 6 ao setar_lead_quente.json APOS o PATCH Lead (node 5):

**Node 6 — `Handoff Corretor` (executeWorkflow)**
- type: "n8n-nodes-base.executeWorkflow"
- typeVersion: 1.2
- position: [1456, 0] (proximo node na sequencia horizontal)
- parameters.mode: "each"
- parameters.workflowId: usar expressao `{{ $vars.HANDOFF_WORKFLOW_ID }}` para ser configuravel
  (Alternativa: se o executor preferir, pode usar workflowId fixo com comment explicando)
- parameters.workflowInputs:
  - phone: `{{ $json.phone || $('Normalize').first().json.phone }}`
  - tenant_id: `{{ $json.tenant_id || $('Normalize').first().json.tenant_id }}`
  - lead_name: `{{ $json.name || 'Sem nome' }}`
  - lead_status: `{{ $json.status || 'qualified' }}`
  - lead_score: `{{ $json.score || 3 }}`
  - metadata_json: `{{ JSON.stringify($json.metadata || {}) }}`

Nota: $json aqui vem do PATCH Lead que retorna representation do lead atualizado.

**Atualizar connections:**
- Manter connection existente: "Build Metadata" -> "PATCH Lead"
- Adicionar: "PATCH Lead" -> "Handoff Corretor"
  ```json
  "PATCH Lead": {
    "main": [[{"node": "Handoff Corretor", "type": "main", "index": 0}]]
  }
  ```

**NAO ALTERAR:** Nodes 1-5 devem permanecer identicos. Apenas adicionar node 6 e connection do PATCH Lead.

**Importante:** O _comment do workflow deve ser atualizado para refletir o novo node:
`"Workflow: Luana tool — marcar lead como quente. Arquitetura: ExecuteWorkflowTrigger → Normalize → Get Lead → Build Metadata → PATCH Lead → Handoff Corretor."`
  </action>
  <verify>
    <automated>node -e "const w = require('./.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json'); const names = w.nodes.map(n=>n.name); console.log('nodes:', names); if(!names.includes('Handoff Corretor')) throw 'Missing Handoff node'; if(w.nodes.length !== 6) throw 'Expected 6 nodes, got '+w.nodes.length; const patchConn = w.connections['PATCH Lead']; if(!patchConn) throw 'Missing PATCH Lead connection'; console.log('PASS')"</automated>
  </verify>
  <done>setar_lead_quente.json tem 6 nodes, node "Handoff Corretor" (executeWorkflow) conectado apos PATCH Lead, nodes 1-5 inalterados</done>
</task>

</tasks>

<verification>
- handoff_corretor.json parseia sem erro e tem 3 nodes com connections validas
- setar_lead_quente.json tem 6 nodes, novo node "Handoff Corretor" encadeado apos PATCH Lead
- Nodes 1-5 do setar_lead_quente inalterados (mesmos IDs, mesmos parametros)
- Resumo do handoff inclui: nome, telefone, objetivo, faixa_valor, bairro_interesse, imovel_ref, localizacao_visita
- Dados vem de metadata (merge nao-destrutivo) + colunas fixas (phone, name, status, score)
</verification>

<success_criteria>
1. Workflow handoff_corretor.json criado com 3 nodes funcionais
2. setar_lead_quente.json atualizado com node 6 sem quebrar nodes 1-5
3. Payload do handoff contem todos os campos do resumo especificados
4. MVP funcional: fallback para httpbin.org/post se webhook nao configurado
</success_criteria>

<output>
After completion, create `.planning/quick/260409-7j3-criar-handoff-do-lead-quente-para-corret/260409-7j3-SUMMARY.md`
</output>
