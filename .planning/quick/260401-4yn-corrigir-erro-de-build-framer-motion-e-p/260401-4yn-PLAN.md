---
phase: quick
plan: 260401-4yn
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_add_tenant_ai_columns.sql
autonomous: true
must_haves:
  truths:
    - "next build completes without DB errors on static page generation"
    - "tenants table has system_prompt and knowledge_rag_xml columns"
    - "Control dashboard loads tenant data including AI config fields"
  artifacts:
    - path: "supabase/migrations/007_add_tenant_ai_columns.sql"
      provides: "ALTER TABLE adding system_prompt and knowledge_rag_xml to tenants"
      contains: "ALTER TABLE tenants"
  key_links:
    - from: "src/lib/control/queries.ts"
      to: "supabase tenants table"
      via: "select system_prompt, knowledge_rag_xml"
      pattern: "system_prompt.*knowledge_rag_xml"
---

<objective>
Add missing `system_prompt` and `knowledge_rag_xml` columns to the `tenants` table so the Control dashboard query stops failing with column-not-found errors during build.

Purpose: The code in `src/lib/control/queries.ts` selects these columns but they were never added to the schema. This causes a DB error (code 42703) during `next build` static page generation for `/control/tenants`.
Output: A Supabase migration file that adds the two columns, and a verified clean build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@supabase/migrations/001_initial_schema.sql (tenants table definition — lines 13-22)
@src/lib/control/queries.ts (the query selecting the missing columns — line 15)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration to add AI config columns to tenants</name>
  <files>supabase/migrations/007_add_tenant_ai_columns.sql</files>
  <action>
Create `supabase/migrations/007_add_tenant_ai_columns.sql` with:

```sql
-- Add AI configuration columns to tenants table
-- Used by Control dashboard (src/lib/control/queries.ts)
ALTER TABLE tenants ADD COLUMN IF NOT EXISTS system_prompt TEXT;
ALTER TABLE tenants ADD COLUMN IF NOT EXISTS knowledge_rag_xml TEXT;
```

Both columns are TEXT and nullable (no NOT NULL constraint) — the queries.ts code already handles null with `?? null` fallback.

After creating the file, apply the migration to the live Supabase database:
```bash
npx supabase db push
```
If `supabase db push` is not configured or fails, note it for the user to run manually via the Supabase SQL editor.
  </action>
  <verify>
    <automated>npx next build 2>&1 | grep -E "(error|42703|does not exist|Compiled successfully)" | head -10</automated>
  </verify>
  <done>Migration file exists at supabase/migrations/007_add_tenant_ai_columns.sql. `next build` completes without the "column tenants.system_prompt does not exist" error.</done>
</task>

</tasks>

<verification>
- `next build` exits 0 with no DB column errors
- Migration file is sequential (007) after existing migrations (001-006)
</verification>

<success_criteria>
- Build is clean and ready for `vercel --prod` deployment
- tenants table schema matches what queries.ts expects
</success_criteria>

<output>
After completion, create `.planning/quick/260401-4yn-corrigir-erro-de-build-framer-motion-e-p/260401-4yn-SUMMARY.md`
</output>
