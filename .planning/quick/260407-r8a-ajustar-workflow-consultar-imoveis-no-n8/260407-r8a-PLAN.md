---
phase: quick
plan: 260407-r8a
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "Workflow consultar_imoveis busca dados da tabela real 'properties' no Supabase"
    - "Resultados filtrados por tenant_id do contexto"
    - "Campos mapeados corretamente para saida WhatsApp"
  artifacts: []
  key_links:
    - from: "n8n workflow consultar_imoveis"
      to: "Supabase tabela properties"
      via: "Supabase node com SELECT filtrado por tenant_id"
      pattern: "SELECT.*FROM properties.*WHERE tenant_id"
---

<objective>
Ajustar o workflow consultar_imoveis no n8n para usar a tabela real `properties` do Supabase, com filtro por tenant_id e mapeamento correto dos campos para saida WhatsApp.

Purpose: O workflow atual pode estar usando busca antiga/mock. Precisa apontar para a tabela real `properties` com os campos corretos do schema (migrations 008 + 009).
Output: Workflow n8n atualizado e funcional, consultando dados reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@supabase/migrations/008_properties_table.sql
@supabase/migrations/009_properties_extend.sql

Schema da tabela `properties` (colunas reais do Supabase):
- id (UUID) — mapear como id_imovel
- tenant_id (UUID) — filtro obrigatorio
- title (TEXT)
- photo_url (TEXT) — mapear como foto_principal
- price (NUMERIC) — mapear como valor
- location (TEXT)
- area_sqm (NUMERIC) — mapear como metragem
- status (TEXT: available/sold/reserved)
- link (TEXT) — mapear como link_redes_sociais
- neighborhood (TEXT) — mapear como bairro
- property_type (TEXT) — mapear como tipo_de_imovel
- publication_status (TEXT) — mapear como status_publicacao
- tags (TEXT[])
- purpose (TEXT)

IMPORTANTE: Nao existe coluna `quartos` na tabela. Se o workflow atual mapeia quartos, remover ou deixar como null/vazio. Nao inventar dados.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localizar e inspecionar workflow consultar_imoveis no n8n</name>
  <files>n/a (n8n MCP)</files>
  <action>
    Usar MCP n8n tools para:
    1. Listar workflows e encontrar o workflow "consultar_imoveis" (ou nome similar)
    2. Ler o workflow completo para entender a estrutura atual dos nodes
    3. Identificar qual node faz a busca de imoveis (Supabase node, HTTP node, ou Function node)
    4. Documentar o mapeamento atual de campos e a query usada
    5. Identificar onde o tenant_id e injetado (ou se esta faltando)
  </action>
  <verify>Workflow encontrado e estrutura documentada — nodes de busca e mapeamento identificados</verify>
  <done>Sabe-se exatamente quais nodes precisam ser alterados e qual o estado atual</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar nodes de busca e mapeamento de campos</name>
  <files>n/a (n8n MCP)</files>
  <action>
    Usar MCP n8n tools para editar o workflow consultar_imoveis:

    1. NO NODE DE BUSCA (Supabase node):
       - Apontar para tabela `properties` (nao imoveis, nao properties_old, etc.)
       - Adicionar filtro WHERE tenant_id = {{tenant_id do contexto}}
       - Garantir que filtra apenas publication_status = 'published' (ou conforme logica existente)

    2. NO NODE DE MAPEAMENTO (Set/Function node que formata saida):
       - Mapear campos da tabela para nomes esperados pela saida WhatsApp:
         * id -> id_imovel
         * neighborhood -> bairro
         * property_type -> tipo_de_imovel
         * price -> valor
         * area_sqm -> metragem
         * link -> link_redes_sociais
         * photo_url -> foto_principal
         * publication_status -> status_publicacao
       - Campo `quartos`: NAO existe na tabela. Se o mapeamento antigo referencia quartos, setar como null ou string vazia. Nao inventar valores.
       - Manter title e location como campos auxiliares se ja existiam

    3. NAO ALTERAR:
       - Logica de trigger/entrada do workflow
       - Nodes do agente de IA
       - Nodes de envio de mensagem WhatsApp
       - Estrutura geral do workflow (ordem dos nodes, conexoes entre nodes nao-afetados)

    4. Salvar o workflow via MCP (nao ativar/desativar — manter estado atual)
  </action>
  <verify>
    Usar MCP n8n tools para reler o workflow atualizado e confirmar:
    - Node de busca aponta para tabela `properties`
    - Filtro tenant_id esta presente
    - Mapeamento de campos esta correto conforme lista acima
    - Nenhum node fora do escopo foi alterado
  </verify>
  <done>
    Workflow consultar_imoveis busca da tabela real properties com filtro tenant_id, campos mapeados corretamente para WhatsApp, sem mock data, sem mudanca na logica do agente
  </done>
</task>

</tasks>

<verification>
- Reler workflow final via MCP e confirmar schema de campos
- Verificar que tenant_id e filtrado na query Supabase
- Confirmar que nenhum node de agente/whatsapp foi modificado
</verification>

<success_criteria>
- Workflow consultar_imoveis usa tabela `properties` real do Supabase
- Filtro por tenant_id ativo
- Campos mapeados: id_imovel, bairro, tipo_de_imovel, valor, metragem, link_redes_sociais, foto_principal, status_publicacao
- Campo quartos tratado como ausente (null/vazio)
- Saida formatada para WhatsApp mantida
- Nenhuma mudanca na logica do agente ou estrutura do workflow
</success_criteria>

<output>
After completion, create `.planning/quick/260407-r8a-ajustar-workflow-consultar-imoveis-no-n8/260407-r8a-SUMMARY.md`
</output>
