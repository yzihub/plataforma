---
phase: quick
plan: 260401-669
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/auth.ts
  - src/components/auth/SignInForm.tsx
  - .env.example
  - scripts/check-env.js
  - .planning/ROADMAP.md
autonomous: true
must_haves:
  truths:
    - "NEXT_PUBLIC_APP_URL is the single source of truth for all auth redirect URLs"
    - "Google OAuth, Magic Link, and any future auth flow all use NEXT_PUBLIC_APP_URL consistently"
    - "check-env.js fails loudly if NEXT_PUBLIC_APP_URL is missing or set to localhost in production"
    - "ROADMAP Phase 6 documents the NEXT_PUBLIC_APP_URL requirement for Vercel"
  artifacts:
    - path: "src/components/auth/SignInForm.tsx"
      provides: "Google OAuth redirect using NEXT_PUBLIC_APP_URL env var"
      contains: "NEXT_PUBLIC_APP_URL"
    - path: "src/lib/supabase/auth.ts"
      provides: "Magic link redirect using NEXT_PUBLIC_APP_URL (already correct)"
    - path: ".env.example"
      provides: "Template showing all required env vars including NEXT_PUBLIC_APP_URL"
    - path: "scripts/check-env.js"
      provides: "Pre-deploy validation script"
  key_links:
    - from: "src/components/auth/SignInForm.tsx"
      to: "NEXT_PUBLIC_APP_URL"
      via: "process.env lookup"
      pattern: "process\\.env\\.NEXT_PUBLIC_APP_URL"
    - from: "scripts/check-env.js"
      to: "NEXT_PUBLIC_APP_URL"
      via: "env validation"
      pattern: "NEXT_PUBLIC_APP_URL"
---

<objective>
Consolidate all auth redirect URLs to use NEXT_PUBLIC_APP_URL, create a pre-deploy env validation script, and document the requirement for Phase 6.

Purpose: Magic links and Google OAuth currently have inconsistent URL resolution. SignInForm.tsx uses window.location.origin directly (which works by accident in production but is fragile and inconsistent with auth.ts). We need a single, explicit env-var-driven approach plus a safety net script.
Output: Consistent auth redirects, .env.example, check-env.js script, updated ROADMAP.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key existing code:

From src/lib/supabase/auth.ts (line 12 — already correct):
```typescript
`${process.env.NEXT_PUBLIC_APP_URL || (typeof window !== 'undefined' ? window.location.origin : '')}/auth/callback`
```

From src/components/auth/SignInForm.tsx (line 30 — BUG: hardcoded window.location.origin):
```typescript
redirectTo: `${window.location.origin}/auth/callback`,
```

From .env.local:
```
NEXT_PUBLIC_APP_URL=http://localhost:3000
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SignInForm Google OAuth redirect and create .env.example</name>
  <files>src/components/auth/SignInForm.tsx, .env.example</files>
  <action>
1. In `src/components/auth/SignInForm.tsx`, line 30, replace:
   ```
   redirectTo: `${window.location.origin}/auth/callback`,
   ```
   with:
   ```
   redirectTo: `${process.env.NEXT_PUBLIC_APP_URL || window.location.origin}/auth/callback`,
   ```
   This matches the pattern already used in `src/lib/supabase/auth.ts` and ensures the env var takes priority when set (Vercel production) while falling back to window.location.origin for local dev if the var is somehow missing.

2. Create `.env.example` in project root with ALL env vars from `.env.local` but with placeholder values:
   ```
   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

   # App — REQUIRED for auth redirects (magic link, Google OAuth)
   # In production (Vercel): set to your production URL (e.g. https://plataforma-xxx.vercel.app)
   # In local dev: http://localhost:3000
   NEXT_PUBLIC_APP_URL=http://localhost:3000

   # n8n (YZI FACTORY)
   FACTORY_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/factory-activate
   ```
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && grep -q "NEXT_PUBLIC_APP_URL" src/components/auth/SignInForm.tsx && grep -q "NEXT_PUBLIC_APP_URL" .env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SignInForm.tsx Google OAuth redirect uses NEXT_PUBLIC_APP_URL with window.location.origin fallback. .env.example exists documenting all required env vars with comments.</done>
</task>

<task type="auto">
  <name>Task 2: Create check-env.js pre-deploy validation script</name>
  <files>scripts/check-env.js</files>
  <action>
Create `scripts/check-env.js` (Node.js, no dependencies) that:

1. Reads `.env.local` if it exists (for local runs) OR uses `process.env` directly (for CI/Vercel).
2. Checks these REQUIRED vars exist and are non-empty:
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY
   - NEXT_PUBLIC_APP_URL
3. Warns (yellow) if NEXT_PUBLIC_APP_URL contains "localhost" and NODE_ENV === "production".
4. Warns (yellow) if NEXT_PUBLIC_APP_URL does NOT start with "https://" and NODE_ENV === "production".
5. Exits with code 0 on success, code 1 on any missing required var.
6. Prints a clear summary: green checkmarks for present vars, red X for missing.

Use only `fs`, `path`, `process` — no npm packages. Use ANSI color codes for terminal output.

Make the script executable: add `#!/usr/bin/env node` shebang.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && node scripts/check-env.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Running `node scripts/check-env.js` locally exits 0 with green checkmarks for all required vars. Would exit 1 if any required var is missing.</done>
</task>

<task type="auto">
  <name>Task 3: Document NEXT_PUBLIC_APP_URL requirement in ROADMAP Phase 6</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In `.planning/ROADMAP.md`, under "### Phase 6: Deploy & Infra", add a note after the Success Criteria block:

```
**Deploy Checklist:**
  - Set `NEXT_PUBLIC_APP_URL` to the production Vercel URL in Vercel Environment Variables
  - Run `node scripts/check-env.js` before each deploy (validates all required env vars)
  - Ensure Supabase Auth > URL Configuration > Site URL matches NEXT_PUBLIC_APP_URL
  - Ensure Supabase Auth > URL Configuration > Redirect URLs includes `{NEXT_PUBLIC_APP_URL}/auth/callback`
```

Do NOT modify any other part of the ROADMAP.
  </action>
  <verify>
    <automated>cd D:/dev/plataforma && grep -q "NEXT_PUBLIC_APP_URL" .planning/ROADMAP.md && grep -q "check-env.js" .planning/ROADMAP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ROADMAP Phase 6 has a Deploy Checklist section documenting the NEXT_PUBLIC_APP_URL requirement and check-env.js usage.</done>
</task>

</tasks>

<verification>
1. `grep -rn "window.location.origin" src/components/auth/SignInForm.tsx` should show the fallback inside NEXT_PUBLIC_APP_URL pattern, NOT a standalone usage
2. `node scripts/check-env.js` exits 0 locally
3. `.env.example` exists and documents NEXT_PUBLIC_APP_URL
4. ROADMAP.md Phase 6 mentions NEXT_PUBLIC_APP_URL and check-env.js
</verification>

<success_criteria>
- All auth redirect URLs (magic link + Google OAuth) use NEXT_PUBLIC_APP_URL as primary source
- Pre-deploy script validates required env vars and warns about localhost in production
- Phase 6 documentation captures the deployment requirement
- No regressions: local dev still works with .env.local values
</success_criteria>

<output>
After completion, create `.planning/quick/260401-669-configurar-url-de-produ-o-para-magic-lin/260401-669-SUMMARY.md`
</output>
