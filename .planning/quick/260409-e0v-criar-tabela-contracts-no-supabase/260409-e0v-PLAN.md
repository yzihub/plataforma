---
phase: quick
plan: 260409-e0v
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/013_contracts_add_imovel_conteudo.sql
autonomous: true
requirements: [CONTRACTS-EXTEND]

must_haves:
  truths:
    - "Tabela contracts possui coluna imovel_id (uuid, FK para properties)"
    - "Tabela contracts possui coluna conteudo (text)"
    - "Index composto (tenant_id, lead_id) existe na tabela contracts"
  artifacts:
    - path: "supabase/migrations/013_contracts_add_imovel_conteudo.sql"
      provides: "ALTER TABLE migration adding imovel_id, conteudo, and composite index"
      contains: "ALTER TABLE contracts"
  key_links:
    - from: "contracts.imovel_id"
      to: "properties.id"
      via: "FOREIGN KEY"
      pattern: "REFERENCES properties"
---

<objective>
Estender tabela contracts com colunas imovel_id e conteudo, e criar index composto (tenant_id, lead_id).

Purpose: A tabela contracts ja existe (migration 011) com schema rico (tenant_id, lead_id, project_id, status, file_url, etc). Porem faltam duas colunas solicitadas: imovel_id (uuid FK para properties) e conteudo (text). Tambem falta o index composto (tenant_id, lead_id). Esta migration adiciona apenas o que falta.
Output: Migration SQL 013 pronta para aplicar no Supabase.
</objective>

<context>
@CLAUDE.md
@supabase/migrations/011_contracts_table.sql
@supabase/migrations/008_properties_table.sql
@supabase/migrations/001_initial_schema.sql

NOTA IMPORTANTE: A tabela contracts JA EXISTE em 011_contracts_table.sql com:
- id, tenant_id, lead_id, lead_name, project_id, project_name, corretor_id, corretor_name
- title, type, status (rascunho/pendente/assinado/cancelado/expirado), value, notes
- file_url, file_name, signed_at, expires_at, created_at, updated_at
- RLS habilitado, indexes, trigger updated_at, storage bucket

O que FALTA do pedido do usuario:
- imovel_id (uuid FK para properties) -- 011 tem project_id que referencia properties, mas usuario quer imovel_id explicitamente
- conteudo (text) -- nao existe
- index composto (tenant_id, lead_id) -- 011 tem indexes separados, nao composto
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 013 para estender contracts</name>
  <files>supabase/migrations/013_contracts_add_imovel_conteudo.sql</files>
  <action>
Criar migration SQL seguindo o padrao do projeto (header com comentario, secoes separadas por linhas).

Conteudo da migration:

1. ADD COLUMN imovel_id UUID com FK para properties(id) ON DELETE SET NULL -- permite referencia direta ao imovel separada de project_id
2. ADD COLUMN conteudo TEXT -- corpo/template do contrato
3. CREATE INDEX idx_contracts_tenant_lead ON contracts(tenant_id, lead_id) -- index composto solicitado

NAO alterar: outras tabelas, RLS existente, triggers existentes, colunas existentes.
Seguir padrao de 011_contracts_table.sql para estilo de SQL (comentarios, spacing).
  </action>
  <verify>
    <automated>cat supabase/migrations/013_contracts_add_imovel_conteudo.sql | grep -c "ALTER TABLE\|CREATE INDEX" | grep -q "3" && echo "PASS: 3 statements found" || echo "FAIL"</automated>
  </verify>
  <done>
  - Migration 013 existe com ALTER TABLE ADD COLUMN imovel_id, ALTER TABLE ADD COLUMN conteudo, CREATE INDEX composto
  - Nenhuma outra tabela alterada
  - SQL segue padrao das migrations existentes
  </done>
</task>

</tasks>

<verification>
- Arquivo 013_contracts_add_imovel_conteudo.sql existe em supabase/migrations/
- Contem ADD COLUMN imovel_id UUID REFERENCES properties(id)
- Contem ADD COLUMN conteudo TEXT
- Contem CREATE INDEX idx_contracts_tenant_lead ON contracts(tenant_id, lead_id)
- Nenhum CREATE TABLE (tabela ja existe)
- Nenhuma alteracao em outras tabelas
</verification>

<success_criteria>
Migration SQL criada, pronta para ser aplicada no Supabase. Adiciona imovel_id (FK properties), conteudo (text) e index composto (tenant_id, lead_id) a tabela contracts existente.
</success_criteria>

<output>
After completion, create `.planning/quick/260409-e0v-criar-tabela-contracts-no-supabase/260409-e0v-SUMMARY.md`
</output>
