---
phase: quick-260416-dj5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/015_brokers_add_is_active.sql
  - src/types/brokers.ts
  - src/components/yzihub/CorretoresKpiStrip.tsx
  - src/components/yzihub/CorretoresClient.tsx
  - src/components/yzihub/CorretorDrawer.tsx
autonomous: true
requirements: [CORR-KPI, CORR-ACTIVE, CORR-RANKING]
must_haves:
  truths:
    - "KPI strip no topo mostra Total, Ativos, Inativos, e top corretor por leads"
    - "Toggle ativo/inativo no CorretorDrawer persiste is_active no Supabase"
    - "Tabela mostra badge de status ativo/inativo por corretor"
    - "Ranking de corretores por leads atribuidos visivel na pagina"
  artifacts:
    - path: "supabase/migrations/015_brokers_add_is_active.sql"
      provides: "Coluna is_active na tabela brokers"
      contains: "is_active"
    - path: "src/components/yzihub/CorretoresKpiStrip.tsx"
      provides: "KPI strip para corretores"
      min_lines: 40
    - path: "src/types/brokers.ts"
      provides: "Campo is_active no tipo Broker"
      contains: "is_active"
  key_links:
    - from: "src/components/yzihub/CorretoresClient.tsx"
      to: "CorretoresKpiStrip"
      via: "import e render no topo"
      pattern: "CorretoresKpiStrip"
    - from: "src/components/yzihub/CorretorDrawer.tsx"
      to: "supabase brokers.is_active"
      via: "toggle no form"
      pattern: "is_active"
---

<objective>
Incrementar pagina de Corretores com KPI strip no topo, campo is_active na tabela/tipo, toggle ativo/inativo no drawer, e ranking de corretores por leads atribuidos.

Purpose: Dar visibilidade operacional sobre a equipe comercial — quem esta ativo, quem tem mais leads, metricas rapidas.
Output: KPI strip estilo TailAdmin, coluna is_active persistida, drawer com toggle, ranking por leads.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/types/brokers.ts
@src/components/yzihub/CorretoresClient.tsx
@src/components/yzihub/CorretorDrawer.tsx
@src/components/yzihub/LeadsKpiStrip.tsx
@src/lib/crm/types.ts
@supabase/migrations/014_brokers_table.sql

<interfaces>
<!-- Tipo Broker atual (src/types/brokers.ts) -->
```typescript
export interface Broker {
  id: string;
  tenant_id: string;
  full_name: string;
  phone: string | null;
  email: string | null;
  role: string | null;
  created_at: string;
  updated_at: string | null;
}
export type BrokerInput = Pick<Broker, "full_name" | "phone" | "email" | "role">;
```

<!-- Lead.assigned_to correlaciona com broker.id (src/lib/crm/types.ts) -->
```typescript
export interface Lead {
  id: string;
  tenant_id: string;
  assigned_to: string | null; // broker ID
  // ... demais campos
}
```

<!-- Padrao visual: LeadsKpiStrip (src/components/yzihub/LeadsKpiStrip.tsx) -->
<!-- Grid: grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 xl:grid-cols-N -->
<!-- Cards: rounded-2xl border p-4, icon 10x10 rounded-xl, label text-xs + count text-xl font-bold -->
<!-- Active state: border-brand-500 bg-brand-50 dark:bg-brand-500/10 -->
<!-- Icons importados de @/icons -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration is_active + tipo Broker atualizado</name>
  <files>supabase/migrations/015_brokers_add_is_active.sql, src/types/brokers.ts</files>
  <action>
1. Criar migration `supabase/migrations/015_brokers_add_is_active.sql`:
   ```sql
   ALTER TABLE brokers ADD COLUMN IF NOT EXISTS is_active BOOLEAN NOT NULL DEFAULT true;
   ```
   Manter simples — sem index (tabela pequena por tenant).

2. Atualizar `src/types/brokers.ts`:
   - Adicionar `is_active: boolean` ao interface Broker (antes de created_at).
   - Adicionar `is_active` ao BrokerInput: `export type BrokerInput = Pick<Broker, "full_name" | "phone" | "email" | "role" | "is_active">;`

NAO alterar nenhum outro arquivo nesta task.
  </action>
  <verify>
    <automated>rtk tsc --noEmit --project D:/dev/plataforma/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Migration SQL criada, tipo Broker tem is_active: boolean, BrokerInput inclui is_active. Type errors esperados nos consumers (serao corrigidos na Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: CorretoresKpiStrip + ranking + integrar no CorretoresClient</name>
  <files>src/components/yzihub/CorretoresKpiStrip.tsx, src/components/yzihub/CorretoresClient.tsx</files>
  <action>
1. **Criar `src/components/yzihub/CorretoresKpiStrip.tsx`** — replicar padrao visual do LeadsKpiStrip:
   - Props: `{ brokers: Broker[]; leadCounts: Record<string, number> }` onde leadCounts e mapa broker.id -> numero de leads.
   - 4 KPI cards em grid (grid-cols-2 sm:grid-cols-4 gap-3):
     - **Total Corretores** (icon: GroupIcon de @/icons) — brokers.length
     - **Ativos** (icon: CheckCircleIcon) — brokers.filter(b => b.is_active).length — accent text-emerald-500
     - **Inativos** (icon: CloseLineIcon) — brokers.filter(b => !b.is_active).length — accent text-gray-500
     - **Top Corretor** (icon: ShootingStarIcon de @/icons, mapeado de shooting-star.svg) — nome do broker com mais leads (usar leadCounts). Se empate, primeiro alfabeticamente. Se nenhum lead, mostrar "-".
   - Visual identico ao LeadsKpiStrip: rounded-2xl border p-4, icon container w-10 h-10 rounded-xl bg-gray-100 dark:bg-gray-800, label text-xs font-medium, count text-xl font-bold.
   - SEM estado de clique/active (nao e filtro, e apenas metricas).
   - Import icons: `import { GroupIcon, CheckCircleIcon, CloseLineIcon, ShootingStarIcon } from "@/icons"`. Se ShootingStarIcon nao existir no barrel, verificar nome no index.tsx de icons e usar o correto (shooting-star.svg provavelmente exporta como ShootingStarIcon).

2. **Criar secao de Ranking abaixo dos KPIs** (dentro de CorretoresClient, NAO componente separado):
   - Titulo: "Ranking por Leads" em h3, text-sm font-semibold, mb-3.
   - Lista dos top 5 corretores por leads (ordenar leadCounts desc, pegar top 5).
   - Cada item: flex row com posicao (#1, #2...), nome do broker, e contagem de leads.
   - Usar rounded-2xl border card igual ao padrao TailAdmin (border-gray-200 dark:border-gray-800 bg-white dark:bg-white/[0.03]).
   - Se nenhum corretor tem leads, mostrar texto "Nenhum lead atribuido ainda" em text-sm text-gray-400.

3. **Atualizar `src/components/yzihub/CorretoresClient.tsx`**:
   - Importar CorretoresKpiStrip.
   - Importar `createClient` para buscar leads (ja importado).
   - Adicionar state `leads` (tipo simplificado: `Array<{id: string; assigned_to: string | null}>`).
   - No useEffect existente de fetchBrokers, adicionar fetch paralelo de leads do mesmo tenant: `supabase.from("leads").select("id, assigned_to").eq("tenant_id", tenant.id)`.
   - Computar `leadCounts`: `Record<string, number>` mapeando broker.id -> count de leads onde assigned_to === broker.id.
   - Renderizar `<CorretoresKpiStrip brokers={brokers} leadCounts={leadCounts} />` ACIMA do header "Equipe comercial".
   - Renderizar secao de ranking ABAIXO da tabela de corretores e ACIMA do drawer.
   - Na tabela, adicionar coluna "Status" apos "Funcao": badge com texto "Ativo" (bg-emerald-100 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400 rounded-full px-2.5 py-0.5 text-xs font-medium) ou "Inativo" (bg-gray-100 text-gray-500 dark:bg-gray-700 dark:text-gray-400).
   - Na tabela, adicionar coluna "Leads" apos "Status": mostrar leadCounts[broker.id] || 0.
   - Atualizar skeleton row e header para incluir as 2 colunas novas (7 colunas total).
   - No handleSave, garantir que is_active e passado no input (default true para novos corretores).
   - Atualizar select do fetchBrokers para incluir is_active: `"id, tenant_id, full_name, phone, email, role, is_active, created_at, updated_at"`.
  </action>
  <verify>
    <automated>rtk tsc --noEmit --project D:/dev/plataforma/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>KPI strip com 4 cards renderiza no topo da pagina. Tabela mostra colunas Status e Leads. Ranking top 5 visivel abaixo da tabela. Zero type errors.</done>
</task>

<task type="auto">
  <name>Task 3: Toggle ativo/inativo no CorretorDrawer</name>
  <files>src/components/yzihub/CorretorDrawer.tsx</files>
  <action>
1. Adicionar campo `is_active` ao form state (emptyForm: `is_active: true`).
2. No useEffect de sync, ler `broker.is_active` (default true se undefined para backwards compat).
3. Adicionar toggle visual ACIMA do campo "Funcao" no formulario:
   - Label: "Status do corretor" (labelCls).
   - Toggle switch estilo TailAdmin: botao com div relativa, circulo absoluto que desliza. Quando ativo: bg-brand-500 + circulo translate-x. Quando inativo: bg-gray-300 dark:bg-gray-600.
   - Texto ao lado do toggle: "Ativo" (text-emerald-500) ou "Inativo" (text-gray-400).
   - onClick: `setField("is_active", !form.is_active)`.
4. No handleSubmit, incluir is_active no input BrokerInput enviado ao onSave.
5. Manter consistencia visual com o resto do drawer — espaçamento space-y-4.

Toggle CSS pattern (inline, sem dependencia externa):
```
<button type="button" onClick={...} className="relative inline-flex h-6 w-11 items-center rounded-full transition-colors {active ? 'bg-brand-500' : 'bg-gray-300 dark:bg-gray-600'}">
  <span className="inline-block h-4 w-4 rounded-full bg-white shadow-sm transform transition-transform {active ? 'translate-x-6' : 'translate-x-1'}" />
</button>
```
  </action>
  <verify>
    <automated>rtk tsc --noEmit --project D:/dev/plataforma/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Toggle ativo/inativo visivel no drawer. Persistencia via onSave com is_active. Novo corretor default ativo. Editar corretor reflete estado atual.</done>
</task>

</tasks>

<verification>
1. `rtk tsc --noEmit` — zero errors
2. `rtk next build` — build completa sem erros
3. Pagina /cockpit/corretores renderiza: KPI strip (4 cards) + tabela com Status/Leads + ranking + drawer com toggle
</verification>

<success_criteria>
- KPI strip mostra Total, Ativos, Inativos, Top Corretor com contagem de leads
- Tabela de corretores tem colunas Status (badge) e Leads (contagem)
- Drawer tem toggle ativo/inativo que persiste no Supabase
- Ranking top 5 corretores por leads visivel abaixo da tabela
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/quick/260416-dj5-incrementar-p-gina-corretores-kpi-strip-/260416-dj5-SUMMARY.md`
</output>
