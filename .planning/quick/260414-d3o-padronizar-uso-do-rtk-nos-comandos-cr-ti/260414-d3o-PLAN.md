---
phase: quick
plan: 260414-d3o
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md do projeto menciona RTK como regra de uso CLI"
    - "Comandos criticos do YZIHUB listados com prefixo rtk"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Regra RTK na secao Otimizacao de Tokens"
      contains: "rtk"
  key_links: []
---

<objective>
Adicionar regra de uso do RTK (Rust Token Killer) ao CLAUDE.md do projeto, padronizando que todos os comandos CLI criticos devem usar o prefixo `rtk` para comprimir output e economizar tokens.

Purpose: RTK ja esta instalado globalmente e configurado em ~/.claude/CLAUDE.md, mas o CLAUDE.md do projeto nao referencia essa ferramenta. Sem essa referencia, sessoes futuras podem ignorar o RTK.
Output: CLAUDE.md atualizado com regra RTK na secao de Otimizacao de Tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar regra RTK ao CLAUDE.md do projeto</name>
  <files>CLAUDE.md</files>
  <action>
Editar a secao "## ⚡ Otimização de Tokens e Contexto" do CLAUDE.md (linhas 34-40) para adicionar regras de RTK APOS as linhas existentes (antes da linha em branco que separa da proxima secao).

Adicionar os seguintes bullet points ao final da secao:

- Sempre prefixar comandos CLI com `rtk` para comprimir output (RTK v0.36.0+ instalado globalmente)
- Comandos criticos YZIHUB com RTK:
  - `rtk git status`, `rtk git diff`, `rtk git log` (git)
  - `rtk next build` (build Next.js)
  - `rtk tsc --noEmit` (type-check)
  - `rtk grep`, `rtk find` (busca em arquivos)
  - `rtk pnpm install`, `rtk pnpm list` (gerenciador de pacotes)
- Config global RTK em `~/.claude/CLAUDE.md` — regras detalhadas la; aqui apenas reforco de uso

REGRAS DE EDICAO:
- Manter o estilo existente: bullets com "-", backticks para codigo, PT-BR
- NAO alterar nenhuma outra secao do arquivo
- NAO duplicar conteudo do ~/.claude/CLAUDE.md global — apenas referencia breve
- Inserir DEPOIS da linha do context7 (linha 40) e ANTES do bloco GSD:project-start (linha 42)
  </action>
  <verify>
    <automated>grep -c "rtk" CLAUDE.md</automated>
  </verify>
  <done>Secao "Otimizacao de Tokens e Contexto" contem regra RTK com lista de comandos criticos e referencia ao config global. Nenhuma outra secao alterada.</done>
</task>

</tasks>

<verification>
- grep "rtk" CLAUDE.md retorna pelo menos 5 matches (rtk git, rtk next, rtk tsc, rtk grep, rtk pnpm)
- Secao "Otimizacao de Tokens" contem bullets sobre RTK
- Nenhum outro arquivo foi modificado
</verification>

<success_criteria>
CLAUDE.md do projeto padroniza uso de RTK como prefixo obrigatorio para comandos CLI criticos do YZIHUB.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-d3o-padronizar-uso-do-rtk-nos-comandos-cr-ti/260414-d3o-SUMMARY.md`
</output>
