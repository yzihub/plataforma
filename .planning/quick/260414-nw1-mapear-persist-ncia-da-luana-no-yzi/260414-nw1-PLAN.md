---
phase: quick-260414-nw1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md
autonomous: true
requirements:
  - LUANA-MAP-01
  - LUANA-MAP-02
  - LUANA-MAP-03

must_haves:
  truths:
    - "Documento lista quais campos do webhook vão para colunas fixas da tabela `leads`"
    - "Documento lista quais campos vão para `metadata` (JSONB)"
    - "Documento deixa explícito que a segunda mensagem deve fazer GET do lead antes do build_context"
    - "Documento confirma que `atualizar_qualificacao` é a tool de merge para o mesmo lead"
    - "Documento reafirma arquitetura preservada: webhook → normaliza → get/create lead → build_context → agent tool update"
  artifacts:
    - path: ".planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md"
      provides: "Mapeamento de persistência da Luana (colunas fixas vs metadata JSONB)"
      contains: "## Colunas fixas (leads)"
  key_links:
    - from: "webhook Evolution (entrada Luana)"
      to: "tabela leads (Supabase)"
      via: "normaliza → get_or_create lead → build_context → atualizar_qualificacao"
      pattern: "tenant_id + phone (UNIQUE) → merge via atualizar_qualificacao"
---

<objective>
Produzir um documento único de mapeamento (`MAP.md`) que descreve como a Luana persiste dados do lead no Supabase, alinhando com a arquitetura já existente (NÃO é refactor, é documentação).

Purpose: Servir de referência canônica para quem mexer no workflow da Luana, evitando regressões de persistência (ex.: sobrescrever metadata, perder lead existente, duplicar por telefone).
Output: Arquivo `.planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md`
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@clientes/jurema-brokers/automations.md
@clientes/jurema-brokers/knowledge.xml

<!-- Workflows e decisões já tomadas sobre persistência da Luana (ler para mapear, não para mudar) -->
@.planning/quick/260408-rqi-padronizar-entrada-do-lead-no-supabase/260408-rqi-SUMMARY.md
@.planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/260408-sow-SUMMARY.md
@.planning/quick/260408-sow-padronizar-entrada-do-lead-no-supabase/luana-agent-workflow-audit.md
@.planning/quick/260408-rzc-garantir-merge-de-metadata-no-atualizar-/260408-rzc-SUMMARY.md
@.planning/quick/260409-5w2-padronizar-setar-lead-quente-com-merge-e/260409-5w2-SUMMARY.md
@.planning/quick/260408-sub-refactor-luana-airtable-supabase/atualizar_qualificacao.json
@.planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json

<interfaces>
<!-- Arquitetura a PRESERVAR - não alterar, apenas documentar -->
Fluxo canônico da Luana (cada mensagem WhatsApp):
  webhook Evolution
    → normaliza (tenant_id, phone E.164, nome, raw_message)
    → get_or_create lead (UPSERT por UNIQUE(tenant_id, phone))
    → build_context (usa lead existente + knowledge.xml + histórico)
    → agent response
    → agent tool call: atualizar_qualificacao (merge em metadata JSONB)

Regra crítica: a 2ª+ mensagem NUNCA deve recriar o lead; deve SEMPRE fazer GET
pelo par (tenant_id, phone) ANTES do build_context, para que o agente tenha
o histórico de qualificação acumulado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever MAP.md com o mapeamento de persistência da Luana</name>
  <files>.planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md</files>
  <action>
Criar um único arquivo Markdown curto e objetivo (alvo: 120-200 linhas) com a estrutura abaixo. Usar como fontes primárias os SUMMARYs e JSONs listados em `<context>` — NÃO inspecionar código além do necessário, NÃO propor refactor.

Estrutura obrigatória do MAP.md:

1. `# Mapeamento de Persistência — Luana (Jurema Brokers)`
   - 2-3 linhas de contexto: tenant, agente, objetivo do doc.

2. `## Arquitetura (preservar — não mudar)`
   - Diagrama em texto do fluxo: `webhook → normaliza → get/create lead → build_context → agent → tool atualizar_qualificacao`.
   - Frase explícita: "Esta arquitetura NÃO está em discussão neste documento. O objetivo é alinhar persistência ao que já existe."

3. `## Colunas fixas (tabela `leads`)`
   - Tabela markdown com 3 colunas: `Coluna | Origem (webhook field) | Observação`.
   - Listar no mínimo: `tenant_id`, `phone`, `name`, `status`, `source`, `created_at`, `updated_at`.
   - `phone` + `tenant_id` = UNIQUE (citar a migration que criou isso — ver SUMMARY 260408-rqi).

4. `## Campos em `metadata` (JSONB)`
   - Tabela markdown: `Campo | Tipo | Origem | Quando é preenchido`.
   - Incluir os campos de qualificação que a Luana coleta: `budget`, `bairros_interesse[]`, `tipo_imovel`, `quartos`, `finalidade` (morar/investir), `urgencia`, `score`, `last_message_at`, `conversation_id`, `raw_webhook` (opcional).
   - Nota: metadata é MERGE (não substituição) — referenciar quick 260408-rzc.

5. `## Regras de persistência`
   - Regra 1 — Idempotência: toda mensagem faz `UPSERT on conflict(tenant_id, phone)`.
   - Regra 2 — 2ª mensagem em diante: SEMPRE `SELECT` do lead ANTES do `build_context`, para que o contexto do agente inclua metadata acumulado. Destacar em bloco de aviso.
   - Regra 3 — `atualizar_qualificacao` é a ÚNICA tool responsável por escrever em metadata; ela faz merge (`metadata = metadata || $new`), nunca overwrite.
   - Regra 4 — `setar_lead_quente` muda `status` (coluna fixa) e dispara handoff; não mexe em metadata de qualificação.

6. `## O que NÃO fazer`
   - Lista curta de anti-patterns: criar lead novo a cada mensagem, sobrescrever metadata, chamar n8n direto do frontend, usar Airtable (já migrado).

7. `## Referências`
   - Links relativos para os SUMMARYs consultados (260408-rqi, 260408-sow, 260408-rzc, 260409-5w2) e JSONs das tools.

Regras de redação:
- Português, tom direto, zero enfeite.
- Sem propor mudanças de arquitetura.
- Sem código novo — apenas referenciar arquivos existentes.
- Se um campo não estiver 100% confirmado nas fontes, marcar `(a confirmar)` ao invés de inventar.
  </action>
  <verify>
    <automated>test -f .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md && grep -q "## Colunas fixas" .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md && grep -q "## Campos em \`metadata\`" .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md && grep -q "atualizar_qualificacao" .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md && grep -q "GET" .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/MAP.md</automated>
  </verify>
  <done>
    MAP.md existe, contém as 7 seções obrigatórias, lista pelo menos 7 colunas fixas e 6 campos de metadata, deixa explícita a regra de GET antes do build_context na 2ª mensagem, e confirma `atualizar_qualificacao` como tool de merge.
  </done>
</task>

</tasks>

<verification>
- `rtk ls .planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/` mostra `MAP.md` criado.
- Leitura rápida do MAP.md confirma: arquitetura preservada + tabela de colunas fixas + tabela de metadata + regra de GET + papel do `atualizar_qualificacao`.
- Nenhum arquivo de código foi modificado (apenas `.planning/quick/.../MAP.md`).
</verification>

<success_criteria>
- MAP.md é auto-suficiente: alguém novo consegue, só lendo esse arquivo, entender o que vai para coluna fixa vs metadata.
- Zero ambiguidade sobre a regra "GET antes do build_context na 2ª mensagem".
- Zero proposta de refactor — apenas mapeamento do estado atual.
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260414-nw1-mapear-persist-ncia-da-luana-no-yzi/260414-nw1-SUMMARY.md` com links para o MAP.md e resumo de 3-5 linhas.
</output>
