---
phase: quick
plan: 260416-t9f
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/LeadCard.tsx
  - src/components/yzihub/LeadDrawer.tsx
autonomous: true
requirements: [t9f]
must_haves:
  truths:
    - "LeadCard exibe imóvel (imovel_ref ou interesse_principal) com fallback 'Nenhum imóvel associado'"
    - "LeadCard exibe corretor por nome (não UUID) com fallback 'Sem corretor'"
    - "LeadCard tem botão 'Enviar para Corretor' visível"
    - "LeadDrawer (TabDados) exibe seção Imóvel Associado com imovel_ref + faixa_valor + interesse"
    - "LeadDrawer (CorretorCard) exibe nome+telefone+email do corretor ou 'Nenhum corretor atribuído'"
  artifacts:
    - path: "src/components/yzihub/LeadCard.tsx"
      provides: "Card exibe imóvel e corretor"
    - path: "src/components/yzihub/LeadDrawer.tsx"
      provides: "Drawer exibe seções claras com imóvel e corretor"
  key_links:
    - from: "LeadCard"
      to: "corretores prop"
      via: "findCorretorName(lead.assigned_to, corretores)"
      pattern: "assigned_to.*corretores"
---

<objective>
Exibir imóvel e corretor responsável no card e drawer de leads.

Purpose: Corretores e gestores precisam ver de imediato qual imóvel está vinculado ao lead e quem é o corretor responsável, sem abrir o drawer para isso.
Output: LeadCard atualizado com linha de imóvel + linha de corretor + botão handoff. LeadDrawer com seções claras de imóvel associado e corretor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/crm/types.ts
</context>

<interfaces>
<!-- Lead type (src/lib/crm/types.ts) — campos relevantes -->
```typescript
interface Lead {
  id: string
  tenant_id: string
  name: string
  phone: string | null
  status: LeadStatus
  score: number
  value: number
  assigned_to: string | null       // UUID do corretor
  imovel_ref?: string | null       // referência do imóvel (id_imovel)
  interesse_principal?: string | null  // 'apartamento' | 'casa' | 'terreno' | ...
  faixa_valor?: string | null      // ex: "R$ 700k – R$ 1M"
  regiao_interesse?: string | null
  notes: string | null
}

// LeadCard recebe corretores via prop:
interface LeadCardProps {
  lead: Lead;
  onActionSuccess?: (leadId: string, jobId: string, action: string) => void;
  onLeadSelect?: (lead: Lead) => void;
  corretores?: Corretor[];  // adicionar este prop
}

// Corretor type (de LeadsClient.tsx):
export type Corretor = {
  id: string;
  name: string;
  phone?: string;
  email?: string;
}
```

<!-- LeadDrawer já tem CorretorCard e TabDados com seção "Imóvel Associado" -->
<!-- CorretorCard (linha 295-376 em LeadDrawer.tsx): mostra nome+telefone+email do corretor, fallback "Nenhum corretor atribuído" — JÁ FUNCIONA CORRETAMENTE quando corretores passados -->
<!-- TabDados seção "Imóvel Associado" (linha 466-497): exibe imovel_ref, fallback "Nenhum imóvel associado" — JÁ FUNCIONA -->
<!-- O drawer está OK — só precisamos enriquecer a seção de Imóvel Associado para mostrar também interesse_principal + faixa_valor + regiao_interesse -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar imóvel e corretor ao LeadCard + botão handoff</name>
  <files>src/components/yzihub/LeadCard.tsx</files>
  <action>
Adicionar prop `corretores?: Corretor[]` ao `LeadCardProps`. Importar o tipo `Corretor` de `@/components/yzihub/LeadsClient`.

Adicionar helper `findCorretorName` (igual ao de LeadsDataTable.tsx) que resolve assigned_to → nome ou "Sem corretor".

Adicionar helper `getImovelLabel(lead: Lead): string` que retorna:
1. `lead.imovel_ref` se existir
2. `lead.interesse_principal` (capitalizado) + ` · ` + `lead.regiao_interesse` se existirem
3. `lead.interesse_principal` se existir
4. `""` se nada existir

No JSX do card, após a seção "Contact Info" (Row 3) e antes da seção "Score + Value" (Row 4), inserir **Row 3b** com dois itens inline:

```tsx
{/* ── Row 3b: Imóvel + Corretor ── */}
{(imovelLabel || corretorName !== "Sem corretor") && (
  <div className="flex flex-col gap-1">
    <span className="flex items-center gap-1.5 text-theme-xs text-gray-500 dark:text-gray-400">
      <HouseIcon />
      <span className="truncate">{imovelLabel || "Nenhum imóvel associado"}</span>
    </span>
    <span className="flex items-center gap-1.5 text-theme-xs text-gray-500 dark:text-gray-400">
      <PersonIcon />
      <span className="truncate">{corretorName}</span>
    </span>
  </div>
)}
```

Definir `HouseIcon` e `PersonIcon` como inline SVGs (estilo dos ícones já existentes no arquivo: width/height 12, stroke currentColor).

No Row 5 (Action Buttons), adicionar o botão "Enviar para Corretor" APÓS os CommandButtons existentes:

```tsx
{lead && (
  <button
    type="button"
    onClick={(e) => { e.stopPropagation(); enviarParaCorretor(lead); }}
    className="inline-flex items-center gap-1.5 rounded-lg bg-brand-50 px-2.5 py-1 text-xs font-medium text-brand-600 hover:bg-brand-100 dark:bg-brand-500/10 dark:text-brand-400 dark:hover:bg-brand-500/20 transition-colors"
  >
    Enviar para Corretor
  </button>
)}
```

Adicionar função `enviarParaCorretor(lead: Lead)` dentro do componente:
```tsx
async function enviarParaCorretor(lead: Lead) {
  try {
    await fetch("/api/actions/execute", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ action: "send_to_broker", lead_id: lead.id, tenant_id: lead.tenant_id }),
    });
  } catch (err) {
    console.error("[LeadCard] enviarParaCorretor error:", err);
  }
}
```

O botão deve aparecer sempre (não só quando há ações no STATUS_ACTIONS), para que o usuário sempre possa acionar o handoff.
  </action>
  <verify>rtk tsc --noEmit 2>&1 | head -30</verify>
  <done>LeadCard compila sem erros de tipo; exibe imóvel e corretor na listagem; botão "Enviar para Corretor" visível em todos os cards</done>
</task>

<task type="auto">
  <name>Task 2: Enriquecer seção Imóvel Associado no LeadDrawer (TabDados)</name>
  <files>src/components/yzihub/LeadDrawer.tsx</files>
  <action>
Na função `TabDados`, localizar a seção "Imóvel Associado" (linhas ~466-497). A seção atual só exibe `form.imovel_ref`. Expandir para mostrar um card com todos os campos de contexto imobiliário disponíveis.

Substituir o bloco atual do card de imóvel encontrado (`form.imovel_ref ? (...)`) por:

```tsx
{(form.imovel_ref || form.interesse_principal || form.faixa_valor || form.regiao_interesse) ? (
  <div className="rounded-xl border border-gray-200 dark:border-gray-700 p-4 space-y-2">
    {form.imovel_ref && (
      <div className="flex items-center justify-between">
        <div>
          <p className="text-xs text-gray-400 uppercase tracking-wider">Referência</p>
          <p className="text-sm font-medium text-gray-800 dark:text-white/90">{form.imovel_ref}</p>
        </div>
        <a
          href={`/cockpit/imoveis?ref=${encodeURIComponent(form.imovel_ref)}`}
          className="text-xs text-brand-500 hover:underline whitespace-nowrap"
        >
          Ver imóvel →
        </a>
      </div>
    )}
    {form.interesse_principal && (
      <div>
        <p className="text-xs text-gray-400 uppercase tracking-wider">Tipo</p>
        <p className="text-sm text-gray-700 dark:text-gray-200 capitalize">{form.interesse_principal}</p>
      </div>
    )}
    {form.faixa_valor && (
      <div>
        <p className="text-xs text-gray-400 uppercase tracking-wider">Faixa de valor</p>
        <p className="text-sm text-gray-700 dark:text-gray-200">{form.faixa_valor}</p>
      </div>
    )}
    {form.regiao_interesse && (
      <div>
        <p className="text-xs text-gray-400 uppercase tracking-wider">Região</p>
        <p className="text-sm text-gray-700 dark:text-gray-200">{form.regiao_interesse}</p>
      </div>
    )}
  </div>
) : (
  <div className="rounded-xl border border-dashed border-gray-200 dark:border-gray-700 p-4 flex flex-col items-center gap-2 text-center">
    <span className="text-2xl">🏠</span>
    <p className="text-sm text-gray-400">Nenhum imóvel associado</p>
    <button
      type="button"
      className="text-xs text-brand-500 hover:underline"
      onClick={() => { /* TODO: modal de seleção de imóvel */ }}
    >
      Associar imóvel
    </button>
  </div>
)}
```

O `CorretorCard` já está funcional (exibe nome, telefone, email do corretor ou "Nenhum corretor atribuído") — não alterar.

A seção de título "Imóvel Associado" no topo da seção já existe — manter intacta.
  </action>
  <verify>rtk tsc --noEmit 2>&1 | head -30</verify>
  <done>TabDados no drawer mostra card rico com referência, tipo, faixa de valor e região quando existentes; fallback "Nenhum imóvel associado" quando nenhum campo preenchido; CorretorCard continua funcional</done>
</task>

</tasks>

<verification>
Após ambas as tasks:
1. `rtk tsc --noEmit` — zero erros de tipo
2. `rtk next build` — build sem erros
3. Abrir `/cockpit/leads` — cards exibem linha de imóvel e corretor
4. Botão "Enviar para Corretor" aparece em cada card
5. Abrir drawer de um lead com imóvel_ref — seção Imóvel Associado mostra o card rico
6. Abrir drawer de um lead sem imóvel — exibe "Nenhum imóvel associado"
7. Seção Corretor Responsável no drawer mostra nome (não UUID) ou "Nenhum corretor atribuído"
</verification>

<success_criteria>
- LeadCard aceita prop `corretores?: Corretor[]` e resolve `assigned_to` para nome
- LeadCard exibe imóvel (ref ou interesse) e corretor como linhas abaixo das informações de contato
- Botão "Enviar para Corretor" visível em todos os cards, dispara POST /api/actions/execute action=send_to_broker
- TabDados no drawer exibe card de imóvel com até 4 campos (ref, tipo, faixa, região) ou fallback vazio
- Build TypeScript sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260416-t9f-exibir-imovel-e-corretor-no-card-e-drawe/260416-t9f-SUMMARY.md`
</output>
