---
phase: quick-260415-1zp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/KanbanBoard.tsx
autonomous: true
requirements:
  - KANBAN-LEADS-01
must_haves:
  truths:
    - "Kanban exibe 5 colunas: Novo Lead, Lead Quente, Em Qualificação, Qualificando, Agendamento"
    - "Cada coluna tem header com titulo e badge de contagem (pill arredondado)"
    - "Cards mostram: nome, telefone, interesse, faixa_valor"
    - "Visual segue o padrao TailAdmin dark/light ja estabelecido em PipelineClient.tsx"
    - "Sem emojis em nenhum ponto do componente"
  artifacts:
    - path: "src/components/yzihub/KanbanBoard.tsx"
      provides: "Kanban de leads no padrao TailAdmin"
      exports: ["KanbanBoard"]
  key_links:
    - from: "src/components/yzihub/KanbanBoard.tsx"
      to: "leads prop"
      via: "filtro por status === stage"
      pattern: "leads\\.filter"
---

<objective>
Reescrever KanbanBoard.tsx substituindo o visual antigo (bg-boxdark, bg-meta-4, strokedark) pelo padrao TailAdmin ja estabelecido em PipelineClient.tsx. Colunas e cards passam a usar border-gray-200, bg-gray-50, rounded-xl, shadow-sm — identico ao PipelineClient.

Purpose: Consistencia visual entre o Pipeline (PipelineClient.tsx) e o Kanban de Leads.
Output: KanbanBoard.tsx refatorado, sem quebrar a interface publica do componente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Interface atual que sera substituida — manter compatibilidade de props -->
<!-- src/components/yzihub/KanbanBoard.tsx — export KanbanBoard, props: leads[], onMoveLead?, onSelectLead? -->

<!-- Padrao visual de referencia (PipelineClient.tsx): -->
<!-- Coluna: rounded-xl border border-gray-200 bg-gray-50 dark:border-gray-700 dark:bg-gray-900/50 -->
<!-- Card:   rounded-xl border border-gray-200 bg-white p-3.5 shadow-sm dark:border-gray-700 dark:bg-gray-800 hover:border-brand-300 hover:shadow-md transition-all -->
<!-- Count badge: text-xs font-medium text-gray-400 bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded-full -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever KanbanBoard.tsx com visual TailAdmin e novas colunas/campos</name>
  <files>src/components/yzihub/KanbanBoard.tsx</files>
  <action>
Reescrever o arquivo completo seguindo as regras abaixo. NAO usar emojis em nenhum lugar.

**STAGES (substitui as colunas antigas):**
```ts
const STAGES = ['Novo Lead', 'Lead Quente', 'Em Qualificação', 'Qualificando', 'Agendamento'];
```

**Tipo Lead (adicionar campos novos, manter retrocompatibilidade):**
```ts
type Lead = {
  id: string;
  nome: string;
  telefone?: string;
  interesse?: string;
  faixa_valor?: string;
  valor_imovel?: number;   // mantido para compat
  status: string;
  origem?: string;
};
```

**Props (sem alteracao):**
```ts
interface KanbanBoardProps {
  leads: Lead[];
  onMoveLead?: (leadId: string, newStatus: string) => void;
  onSelectLead?: (lead: Lead) => void;
}
```

**Layout do container:**
```tsx
<div className="flex gap-4 overflow-x-auto pb-4">
```

**Coluna — espelhar PipelineClient:**
```tsx
<div className="flex flex-col w-72 shrink-0 rounded-xl border border-gray-200 bg-gray-50 dark:border-gray-700 dark:bg-gray-900/50">
  {/* Header */}
  <div className="px-3.5 pt-3.5 pb-2">
    <div className="flex items-center gap-2">
      <span className="text-sm font-semibold text-gray-700 dark:text-gray-200 truncate">
        {stage}
      </span>
      <span className="text-xs font-medium text-gray-400 bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded-full shrink-0">
        {stageLeads.length}
      </span>
    </div>
  </div>
  {/* Cards */}
  <div className="flex flex-col gap-2.5 p-2.5 flex-1 overflow-y-auto max-h-[calc(100vh-260px)] min-h-[80px]">
    {/* ... cards ... */}
  </div>
</div>
```

**Card — espelhar PipelineClient:**
```tsx
<div
  onClick={() => onSelectLead?.(lead)}
  className="rounded-xl border border-gray-200 bg-white p-3.5 shadow-sm dark:border-gray-700 dark:bg-gray-800 cursor-pointer hover:border-brand-300 hover:shadow-md transition-all space-y-2"
>
  {/* Nome */}
  <p className="text-sm font-medium text-gray-800 dark:text-white/90 truncate">{lead.nome}</p>

  {/* Telefone (so se presente) */}
  {lead.telefone && (
    <p className="text-xs text-gray-400 truncate">{lead.telefone}</p>
  )}

  {/* Interesse como tag */}
  {lead.interesse && (
    <span className="inline-block rounded-full bg-gray-100 px-2 py-0.5 text-[10px] font-medium text-gray-600 dark:bg-gray-700 dark:text-gray-300">
      {lead.interesse}
    </span>
  )}

  {/* Faixa de valor */}
  {lead.faixa_valor && (
    <p className="text-xs font-semibold text-gray-700 dark:text-gray-200">{lead.faixa_valor}</p>
  )}
</div>
```

**Estado vazio da coluna:**
```tsx
{stageLeads.length === 0 && (
  <div className="flex items-center justify-center h-20 rounded-lg border border-dashed border-gray-200 dark:border-gray-700">
    <p className="text-xs text-gray-300 dark:text-gray-600">Sem leads nesta etapa</p>
  </div>
)}
```

Manter `"use client"` no topo. Nao importar nada que nao seja usado. Nao adicionar drag-and-drop (sem onDragStart/onDrop).
  </action>
  <verify>
    <automated>rtk tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>KanbanBoard.tsx compilado sem erros de tipo, usando classes TailAdmin (border-gray-200, bg-gray-50, rounded-xl) nas colunas e cards, com as 5 novas colunas e os campos nome/telefone/interesse/faixa_valor nos cards.</done>
</task>

</tasks>

<verification>
1. `rtk tsc --noEmit` sem erros em KanbanBoard.tsx
2. Inspecionar o arquivo: nenhuma classe old-theme (bg-boxdark, bg-meta-4, strokedark, shadow-default)
3. Inspecionar o arquivo: STAGES contem exatamente ['Novo Lead', 'Lead Quente', 'Em Qualificação', 'Qualificando', 'Agendamento']
4. Inspecionar o arquivo: nenhum emoji presente
5. Export `KanbanBoard` preservado e props (leads, onMoveLead, onSelectLead) mantidas
</verification>

<success_criteria>
- Componente compilado sem erros TypeScript
- Visual espelha PipelineClient.tsx (TailAdmin dark/light)
- 5 colunas corretas no array STAGES
- Cards exibem nome, telefone, interesse, faixa_valor
- Zero emojis no arquivo
- Export e assinatura de props inalterados
</success_criteria>

<output>
Apos conclusao, criar `.planning/quick/260415-1zp-copiar-kanban-tailadmin-para-leads-colun/260415-1zp-SUMMARY.md`
</output>
