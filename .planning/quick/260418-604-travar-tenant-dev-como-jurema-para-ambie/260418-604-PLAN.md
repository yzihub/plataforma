---
phase: quick-260418-604
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/context/TenantContext.tsx
autonomous: true
requirements:
  - DEV-TENANT-FIX
must_haves:
  truths:
    - "Em ambiente local com DEV_BYPASS=true, tenant.id é 82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"
    - "Todas as queries de cockpit (CRM, leads, pipeline, financeiro) usam o UUID correto de Jurema Brokers"
    - "O bypass só ativa quando não há usuário autenticado E DEV_BYPASS=true"
  artifacts:
    - path: "src/context/TenantContext.tsx"
      provides: "DEV_BYPASS com UUID correto de Jurema Brokers"
      contains: "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"
  key_links:
    - from: "src/context/TenantContext.tsx"
      to: "src/app/cockpit/*/page.tsx"
      via: "useTenantContext() → tenant.id"
      pattern: "useTenantContext"
---

<objective>
Corrigir o UUID do tenant de desenvolvimento em TenantContext.tsx para que corresponda ao tenant real de Jurema Brokers no Supabase.

Purpose: O UUID `b179ae75-3d56-4de8-8840-fc9c4d9ec21e` hardcoded no DEV_BYPASS está errado. Todas as queries de cockpit que usam `tenant.id` retornam dados vazios ou erros porque esse ID não existe no banco. O ID correto é `82cc7aa9-fc6e-4f37-8d8e-8a71c1691361`.

Output: TenantContext.tsx com UUID corrigido; nenhuma outra mudança necessária pois todas as páginas de cockpit já consomem `useTenantContext().tenant.id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/context/TenantContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir UUID do DEV_BYPASS em TenantContext.tsx</name>
  <files>src/context/TenantContext.tsx</files>
  <action>
Na linha 67 de src/context/TenantContext.tsx, substituir o UUID incorreto pelo UUID real de Jurema Brokers:

ANTES (linha 67):
  id: "b179ae75-3d56-4de8-8840-fc9c4d9ec21e",

DEPOIS (linha 67):
  id: "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361",

O comentário de log na linha 65 já diz "using Jurema Brokers fallback" — apenas o UUID está errado.

Não alterar nenhuma outra parte do arquivo. O DEV_BYPASS já está corretamente guardado por:
  - `process.env.NEXT_PUBLIC_DEV_BYPASS === "true"`
  - `process.env.NODE_ENV !== "production"`

Isso é suficiente — o bypass só ativa em ambiente local explicitamente configurado. A condição atual (rodar antes de `getUser()`) é correta pois evita hang de DNS. Não mover a condicional.
  </action>
  <verify>
    <automated>grep -n "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361" src/context/TenantContext.tsx</automated>
  </verify>
  <done>O UUID 82cc7aa9-fc6e-4f37-8d8e-8a71c1691361 aparece na linha do DEV_BYPASS. O UUID antigo b179ae75-3d56-4de8-8840-fc9c4d9ec21e não existe mais no arquivo.</done>
</task>

</tasks>

<verification>
1. `grep -n "b179ae75" src/context/TenantContext.tsx` → nenhum resultado (UUID antigo removido)
2. `grep -n "82cc7aa9" src/context/TenantContext.tsx` → linha ~67 com o UUID correto
3. Em browser local com `NEXT_PUBLIC_DEV_BYPASS=true`: cockpit/leads, cockpit/crm, cockpit/pipeline e cockpit/financeiro carregam dados reais de Jurema Brokers (não vazios)
</verification>

<success_criteria>
- UUID de Jurema Brokers correto em TenantContext.tsx
- Nenhuma página de cockpit alterada (todas já consomem `tenant.id` via hook)
- Build sem erros de TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260418-604-travar-tenant-dev-como-jurema-para-ambie/260418-604-SUMMARY.md`
</output>
