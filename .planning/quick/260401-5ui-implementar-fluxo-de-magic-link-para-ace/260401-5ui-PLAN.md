---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/auth.ts
  - src/components/auth/SignInForm.tsx
autonomous: true
requirements: [MAGIC-LINK]
must_haves:
  truths:
    - "User sees 'Entrar com Link Magico' option on signin page below Google Auth"
    - "User enters email and receives a magic link email from Supabase"
    - "Clicking the magic link logs user in and redirects to /cockpit (or /control for admin)"
    - "User without profiles row is redirected to /unauthorized after magic link login"
  artifacts:
    - path: "src/lib/supabase/auth.ts"
      provides: "sendMagicLink function using signInWithOtp"
      exports: ["sendMagicLink"]
    - path: "src/components/auth/SignInForm.tsx"
      provides: "Magic Link UI section with email input and send button"
  key_links:
    - from: "src/components/auth/SignInForm.tsx"
      to: "src/lib/supabase/auth.ts"
      via: "import sendMagicLink"
      pattern: "sendMagicLink"
    - from: "src/lib/supabase/auth.ts"
      to: "supabase.auth.signInWithOtp"
      via: "Supabase client call"
      pattern: "signInWithOtp"
    - from: "auth callback"
      to: "proxy.ts gatekeeper"
      via: "redirect to /cockpit triggers proxy profiles check"
      pattern: "profiles.*tenant_id"
---

<objective>
Add Magic Link authentication flow to the signin page so users can log in via email link (passwordless) as an alternative to Google OAuth and email/password.

Purpose: Provides a frictionless login option for cockpit users who may not have Google accounts or passwords set up.
Output: New `sendMagicLink` helper + updated SignInForm with Magic Link UI section.
</objective>

<execution_context>
@.planning/quick/260401-5ui-implementar-fluxo-de-magic-link-para-ace/260401-5ui-PLAN.md
</execution_context>

<context>
@src/lib/supabase/client.ts
@src/components/auth/SignInForm.tsx
@src/app/auth/callback/route.ts
@src/proxy.ts

<interfaces>
<!-- Existing browser Supabase client used by SignInForm -->
From src/lib/supabase/client.ts:
```typescript
import { createBrowserClient } from '@supabase/ssr'
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

<!-- Existing callback route — handles code exchange for ALL auth methods (OAuth + OTP magic link) -->
From src/app/auth/callback/route.ts:
```typescript
// exchangeCodeForSession(code) works for both Google OAuth and Magic Link (PKCE flow)
// Redirects to /cockpit for regular users, /control for global_admin
```

<!-- Proxy gatekeeper — already validates profiles.tenant_id on /cockpit routes -->
From src/proxy.ts (lines 62-76):
```typescript
// /cockpit exige perfil na tabela profiles (Gatekeeper)
const isCockpitRoute = pathname.startsWith('/cockpit')
if (user && isCockpitRoute) {
  const isGlobalAdmin = user.user_metadata?.role === 'global_admin'
  if (!isGlobalAdmin) {
    const { data: profile } = await supabase
      .from('profiles')
      .select('id, tenant_id')
      .eq('id', user.id)
      .maybeSingle()
    if (!profile) {
      return NextResponse.redirect(new URL('/unauthorized', request.url))
    }
  }
}
```

<!-- Env var already exists -->
.env.local: NEXT_PUBLIC_APP_URL=http://localhost:3000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sendMagicLink helper</name>
  <files>src/lib/supabase/auth.ts</files>
  <action>
Create NEW file `src/lib/supabase/auth.ts` with a `sendMagicLink(email: string)` function.

Implementation:
1. Import `createClient` from `@/lib/supabase/client` (browser client, since this runs client-side)
2. Export async function `sendMagicLink(email: string)` that:
   - Creates browser supabase client via `createClient()`
   - Calls `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo } })`
   - For `emailRedirectTo`: use `${process.env.NEXT_PUBLIC_APP_URL || window.location.origin}/auth/callback`
     - This env var is already set in .env.local (`NEXT_PUBLIC_APP_URL=http://localhost:3000`)
     - On Vercel, set `NEXT_PUBLIC_APP_URL` to the production URL in project settings
   - Return the Supabase response `{ data, error }` so the caller can handle UI states

NOTE: The existing `/auth/callback` route.ts already handles magic link auth because `exchangeCodeForSession(code)` works for both OAuth and OTP PKCE flows. No callback changes needed.

NOTE: The proxy.ts gatekeeper already validates profiles.tenant_id for /cockpit routes, so unauthorized users (no profile row) will be redirected to /unauthorized automatically after magic link login, same as Google Auth.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/supabase/auth.ts 2>&1 | head -20</automated>
  </verify>
  <done>File exists with exported sendMagicLink function, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add Magic Link UI to SignInForm</name>
  <files>src/components/auth/SignInForm.tsx</files>
  <action>
Modify `src/components/auth/SignInForm.tsx` to add a Magic Link section below the Google Auth button and above the email/password form.

Changes:
1. Add import: `import { sendMagicLink } from '@/lib/supabase/auth'`
2. Add state variables:
   - `const [magicLinkEmail, setMagicLinkEmail] = useState('')`
   - `const [magicLinkLoading, setMagicLinkLoading] = useState(false)`
   - `const [magicLinkSent, setMagicLinkSent] = useState(false)`
3. Add handler function `handleMagicLink`:
   - Set loading state, clear errors
   - Call `sendMagicLink(magicLinkEmail)`
   - On error: set error state with message
   - On success: set `magicLinkSent = true` (show success message)
4. Add UI between the Google button and the "ou entre com email" divider:
   - Another divider: "ou entre com link magico"
   - If `magicLinkSent` is true, show a green success box: "Link enviado! Verifique seu e-mail e clique no link para entrar."
   - If `magicLinkSent` is false, show:
     - An `<Input>` (use existing Input component) with type="email", placeholder="seu@email.com", value bound to `magicLinkEmail`, onChange updates state
     - A `<Button>` (use existing Button component) with text "Enviar Link Magico", disabled when `magicLinkLoading` or email empty, shows "Enviando..." when loading
   - Style consistently with existing TailAdmin dark theme classes used in the form (same spacing, same dark: classes pattern)
5. Keep the existing "ou entre com email" divider + email/password form below the magic link section

Layout order from top to bottom:
- Google OAuth button
- Divider: "ou entre com link magico"  
- Magic Link email input + button (or success message)
- Divider: "ou entre com email"
- Email/password form
- Sign up link

IMPORTANT: The Input component from `@/components/form/input/InputField` is an uncontrolled component (uses defaultValue). For the magic link email, use a standard `<input>` element styled with the same Tailwind classes as the existing inputs in the form (look at how the Input component renders — dark bg, rounded, border classes). This avoids issues with controlled state. Alternatively, check if the Input component accepts `value` and `onChange` props — if it does, use it directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/auth/SignInForm.tsx 2>&1 | head -20</automated>
  </verify>
  <done>SignInForm renders Magic Link section below Google Auth with email input and send button. Success message appears after sending. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — full project compiles without type errors
2. `npm run build` — Next.js build succeeds
3. Manual: visit /signin, see Magic Link section below Google Auth, enter email, click send, verify email arrives (requires Supabase email provider configured)
</verification>

<success_criteria>
- sendMagicLink function exists in src/lib/supabase/auth.ts using signInWithOtp
- SignInForm shows "Enviar Link Magico" option with email input between Google Auth and email/password form
- Magic link redirectTo points to /auth/callback using NEXT_PUBLIC_APP_URL
- Existing auth callback handles magic link login without modification
- Existing proxy gatekeeper validates profiles.tenant_id for magic link users same as Google Auth users
- TypeScript compiles, Next.js builds
</success_criteria>

<output>
After completion, create `.planning/quick/260401-5ui-implementar-fluxo-de-magic-link-para-ace/260401-5ui-SUMMARY.md`
</output>
