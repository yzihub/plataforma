---
phase: quick-260414-x36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/crm/types.ts
  - src/components/yzihub/LeadDrawer.tsx
autonomous: true
requirements:
  - LD-01  # Scrollbar customizada (sem nativa Windows)
  - LD-02  # Botões do drawer alinhados ao pipeline Jurema Brokers
  - LD-03  # TabDados alinhada com schema real Supabase (imobiliária)

must_haves:
  truths:
    - "Ao abrir o drawer de um lead e rolar o conteúdo da aba, a scrollbar exibida é fina e estilizada (não é a scrollbar nativa do Windows)"
    - "O header de ações do drawer mostra botões relevantes ao pipeline imobiliário (QUALIFICAR, AGENDAR VISITA, ENVIAR PARA CORRETOR, GERAR PROPOSTA, FECHAR, PERDER) sem overflow horizontal visível"
    - "A aba 'Dados' exibe campos alinhados ao schema da Jurema Brokers: Nome, Telefone, E-mail, Score, Perfil Resumido (notes), Região de Interesse, Objetivo, Interesse Principal, Finalidade, Faixa de Valor, Janela de Visita, Corretor Responsável, imovel_ref, Status de Agendamento, Data do Agendamento, Status, Origem"
    - "O tipo Lead em types.ts compila sem quebrar uso existente (novos campos são opcionais)"
    - "Os novos CommandButton actions ('schedule_visit', 'send_to_broker') não quebram o build; se o componente CommandButton não suportar tipos novos, fallback para botão customizado ou reuso dos existentes mais próximos"
  artifacts:
    - path: "src/lib/crm/types.ts"
      provides: "Interface Lead extendida com campos opcionais para pipeline imobiliário"
      contains: "regiao_interesse"
    - path: "src/components/yzihub/LeadDrawer.tsx"
      provides: "Drawer com scrollbar custom, botões do pipeline Jurema e TabDados alinhada ao Supabase"
      contains: "[&::-webkit-scrollbar]"
  key_links:
    - from: "src/components/yzihub/LeadDrawer.tsx"
      to: "src/lib/crm/types.ts"
      via: "import Lead"
      pattern: "import type \\{ Lead"
    - from: "TabDados (LeadDrawer.tsx)"
      to: "Lead interface"
      via: "renderiza campos opcionais de lead (regiao_interesse, objetivo, etc.)"
      pattern: "lead\\.(regiao_interesse|objetivo|interesse_principal|finalidade|faixa_valor|janela_visita|imovel_ref)"
---

<objective>
Corrigir três problemas visuais/estruturais no LeadDrawer:
1. Scrollbar nativa do sistema (feia no Windows) → scrollbar fina customizada via Tailwind arbitrary variants
2. Botões de ação do header não alinhados ao pipeline imobiliário da Jurema Brokers
3. Aba "Dados" com campos genéricos desalinhados com o schema real do Supabase

Purpose: LeadDrawer é a interface principal de gestão de lead — atualmente não reflete os dados reais capturados pela Luana (agente Jurema) e tem scrollbar feia.
Output: Tipo Lead estendido com campos imobiliários opcionais + LeadDrawer corrigido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/yzihub/LeadDrawer.tsx
@src/lib/crm/types.ts

<interfaces>
<!-- Tipo Lead atual (src/lib/crm/types.ts) -->
```typescript
export interface Lead {
  id: string
  tenant_id: string
  stage_id: string | null
  name: string
  email: string | null
  phone: string | null
  company: string | null
  source: string | null
  status: LeadStatus
  score: number // 0-100
  value: number // R$ em reais
  notes: string | null
  assigned_to: string | null
  last_action_at: string | null
  created_at: string
}
```

<!-- Campos do Supabase (Jurema Brokers) que faltam na UI atual -->
Ausentes no tipo: janela_visita, regiao_interesse, objetivo, interesse_principal, finalidade, faixa_valor, imovel_ref, status_agendamento, data_agendamento
</interfaces>

<constraints>
- NÃO alterar o banco Supabase; apenas estender o tipo TypeScript.
- TODOS os campos novos em Lead devem ser opcionais (`?: string | null` ou similar) para não quebrar mocks e outros consumidores.
- CommandButton (@/components/yzihub/CommandButton) tem action types fixos (qualify, ai_assume, schedule, send_proposal, close, lose). Se for necessário adicionar novas actions, verificar o tipo do componente — se apertar no build, reaproveitar 'schedule' como "Agendar Visita" e 'ai_assume' como "Enviar p/ Corretor" via props label, OU reduzir para subset existente (QUALIFICAR, AGENDAR, GERAR PROPOSTA, FECHAR, PERDER).
- Scrollbar deve funcionar em dark e light mode.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender tipo Lead com campos opcionais do pipeline imobiliário</name>
  <files>src/lib/crm/types.ts</files>
  <action>
Adicionar à interface `Lead` (após `created_at`) os seguintes campos opcionais (todos `?: T | null`) para refletir o schema real do Supabase usado pela Luana (Jurema Brokers):

```typescript
// Campos específicos imobiliário (Jurema) — opcionais para compatibilidade com outros tenants
janela_visita?: string | null        // ex: "Sábado manhã"
regiao_interesse?: string | null     // ex: "Barra da Tijuca"
objetivo?: 'investimento' | 'moradia' | string | null
interesse_principal?: 'apartamento' | 'casa' | 'terreno' | 'cobertura' | string | null
finalidade?: 'compra' | 'aluguel' | string | null
faixa_valor?: string | null          // ex: "R$ 700k – R$ 1.000k"
imovel_ref?: string | null           // referência do imóvel (id_imovel)
status_agendamento?: string | null   // ex: "confirmado", "pendente"
data_agendamento?: string | null     // ISO timestamp
```

Manter todos os campos existentes intactos. Não alterar `LeadStatus`, `CrmAction`, `PipelineStage`, `Tenant`, `KanbanData`.

Implementa: LD-03 (campos do schema real).
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
- Interface Lead contém os 9 novos campos opcionais.
- `rtk tsc --noEmit` passa sem erros novos.
- Nenhum mock/consumidor existente de Lead quebra (campos são todos `?`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Corrigir scrollbar, botões do header e TabDados no LeadDrawer</name>
  <files>src/components/yzihub/LeadDrawer.tsx</files>
  <action>
Três mudanças cirúrgicas em `src/components/yzihub/LeadDrawer.tsx`:

**2.1 — Scrollbar customizada (fix scrollbar Windows 7 feia):**
Localizar os 3 elementos com `overflow-y-auto` ou `overflow-x-auto`:
- Linha ~513: `<div className="flex gap-2 px-5 py-3 ... overflow-x-auto">` (botões header)
- Linha ~523: `<div className="flex border-b ... overflow-x-auto">` (tabs)
- Linha ~540: `<div className="flex-1 overflow-y-auto p-5">` (conteúdo das tabs)
- Linha ~223 (dentro de TabConversas): `<div className="flex-1 overflow-y-auto space-y-3 pr-1">`

Adicionar em cada um as classes Tailwind arbitrary variants para scrollbar fina:
```
[&::-webkit-scrollbar]:w-1.5
[&::-webkit-scrollbar]:h-1.5
[&::-webkit-scrollbar-track]:bg-transparent
[&::-webkit-scrollbar-thumb]:bg-gray-300
[&::-webkit-scrollbar-thumb]:rounded-full
dark:[&::-webkit-scrollbar-thumb]:bg-gray-600
hover:[&::-webkit-scrollbar-thumb]:bg-gray-400
dark:hover:[&::-webkit-scrollbar-thumb]:bg-gray-500
```

Para containers overflow-x, usar `h-1.5` (horizontal). Para overflow-y, usar `w-1.5`. Pode aplicar ambos `w-1.5 h-1.5` em todos para simplicidade.

Implementa: LD-01.

**2.2 — Botões do header alinhados ao pipeline Jurema Brokers:**

Localizar o bloco `{/* ── Ações principais (sempre visíveis) ── */}` (linha ~512–520).

Primeiro, abrir `src/components/yzihub/CommandButton.tsx` e verificar os valores válidos do prop `action`. Com base nos 6 atuais (qualify, ai_assume, schedule, send_proposal, close, lose), reorganizar para o pipeline imobiliário:

**Abordagem A (preferida — se CommandButton aceitar custom label):** Manter as 6 actions existentes mas reordenar para fluxo Jurema:
```tsx
<CommandButton action="qualify" leadId={lead.id} tenantId={lead.tenant_id} />          {/* QUALIFICAR */}
<CommandButton action="schedule" leadId={lead.id} tenantId={lead.tenant_id} />         {/* AGENDAR VISITA */}
<CommandButton action="ai_assume" leadId={lead.id} tenantId={lead.tenant_id} />        {/* ENVIAR P/ CORRETOR (reuso) */}
<CommandButton action="send_proposal" leadId={lead.id} tenantId={lead.tenant_id} />    {/* GERAR PROPOSTA */}
<CommandButton action="close" leadId={lead.id} tenantId={lead.tenant_id} />            {/* FECHAR */}
<CommandButton action="lose" leadId={lead.id} tenantId={lead.tenant_id} />             {/* PERDER */}
```

**Abordagem B (se overflow ainda incomoda):** Reduzir para 4 botões principais em grid 2x2 ou flex-wrap, mantendo o container `flex flex-wrap gap-2 px-5 py-3 border-b ...` (remover `overflow-x-auto` e adicionar `flex-wrap`):
```tsx
<div className="flex flex-wrap gap-2 px-5 py-3 border-b border-gray-100 dark:border-gray-800 shrink-0">
  <CommandButton action="qualify" ... />
  <CommandButton action="schedule" ... />
  <CommandButton action="send_proposal" ... />
  <CommandButton action="close" ... />
  <CommandButton action="lose" ... />
</div>
```

**Decisão executor:** Preferir Abordagem B (flex-wrap, sem scroll horizontal, grid natural). Manter 5 botões: QUALIFICAR, AGENDAR, PROPOSTA, FECHAR, PERDER. Remover `ai_assume` do header (ainda disponível via TabIA). NÃO inventar novos tipos de action (evita quebra do CommandButton).

Implementa: LD-02.

**2.3 — TabDados alinhada com schema Supabase:**

Substituir a função `TabDados` (linha ~122–208) para renderizar os campos imobiliários. Manter o padrão visual (label xs + input rounded-xl). Reorganizar o array `fields` e adicionar novos selects.

Novo array `fields` (texto/textarea):
```tsx
const fields = [
  { label: "Nome do cliente",          key: "name",            value: lead.name,                             type: "text" },
  { label: "Telefone",                 key: "phone",           value: lead.phone ?? "",                      type: "tel" },
  { label: "E-mail",                   key: "email",           value: lead.email ?? "",                      type: "email" },
  { label: "Score do Lead (0-100)",    key: "score",           value: String(lead.score ?? 0),              type: "number" },
  { label: "Janela de visita",         key: "janela_visita",   value: lead.janela_visita ?? "",              type: "text" },
  { label: "Região / Bairro de interesse", key: "regiao_interesse", value: lead.regiao_interesse ?? "",    type: "text" },
  { label: "Faixa de valor",           key: "faixa_valor",     value: lead.faixa_valor ?? "",                type: "text" },
  { label: "Corretor responsável",     key: "assigned_to",     value: lead.assigned_to ?? "",                type: "text" },
  { label: "Referência do imóvel",     key: "imovel_ref",      value: lead.imovel_ref ?? "",                 type: "text" },
  { label: "Data do agendamento",      key: "data_agendamento",value: lead.data_agendamento ?? "",           type: "text" },
  { label: "Perfil resumido",          key: "notes",           value: lead.notes ?? "",                      type: "textarea" },
];
```

Adicionar selects abaixo dos fields (reutilizar o mesmo padrão dos selects existentes de Status e Origem):
- **Objetivo** (`lead.objetivo`): options `["Investimento", "Moradia"]`
- **Interesse principal** (`lead.interesse_principal`): options `["Apartamento", "Casa", "Terreno", "Cobertura"]`
- **Finalidade** (`lead.finalidade`): options `["Compra", "Aluguel"]`
- **Status do agendamento** (`lead.status_agendamento`): options `["Pendente", "Confirmado", "Realizado", "Cancelado"]`

Manter o select **Status** (LeadStatus existente) e o select **Origem** (lead.source) como estão, mas atualizar a lista de origens para refletir Jurema: `["WhatsApp", "Instagram", "Site Jurema", "Zap Imóveis", "OLX", "Indicação", "Orgânico"]`.

Remover os campos: Empresa (`company`) e Valor estimado (`value`) — não fazem sentido para imobiliária no contexto da Jurema. (Observação: deixar comentário `{/* Empresa e valor estimado omitidos do pipeline imobiliário */}` se preferir preservar a semântica.)

Manter: seção Tags, Upload de arquivo e botão "Salvar alterações" como estão.

Implementa: LD-03.

**Notas finais:**
- Não alterar lógica de salvar (inputs `defaultValue` são readonly por enquanto — manter).
- Testar build com `rtk tsc --noEmit` + `rtk next build` (ou dev).
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
- Scrollbar fina aparece em todos os containers com overflow (verificar visualmente no Windows — deve ser cinza fino, não a nativa).
- Header do drawer mostra 5 botões (QUALIFICAR, AGENDAR, PROPOSTA, FECHAR, PERDER) com flex-wrap, sem scroll horizontal.
- Aba Dados mostra os novos campos imobiliários (Janela de visita, Região, Faixa de valor, Objetivo, Interesse principal, Finalidade, Status do agendamento, Referência do imóvel, Data do agendamento) e remove Empresa/Valor estimado.
- `rtk tsc --noEmit` passa sem erros.
- Drawer abre/fecha sem crash; TabDados renderiza para leads sem campos novos (fallback `""` funciona).
  </done>
</task>

</tasks>

<verification>
1. `rtk tsc --noEmit` — sem erros TypeScript novos.
2. Rodar dev server (`rtk pnpm dev`), abrir `/cockpit` (ou rota de leads), clicar num lead para abrir drawer:
   - Header: 5 botões em flex-wrap (não mais scroll horizontal)
   - Aba "Dados": campos imobiliários visíveis e alinhados
   - Rolar o conteúdo: scrollbar fina cinza (não a nativa do Windows)
3. Dark mode + light mode: scrollbar legível em ambos.
4. Mocks existentes de Lead não quebram (campos novos são opcionais).
</verification>

<success_criteria>
- [ ] Interface Lead estendida com 9 campos opcionais imobiliários em `src/lib/crm/types.ts`
- [ ] LeadDrawer com scrollbar customizada (`[&::-webkit-scrollbar]:w-1.5` etc.) nos 4 containers com overflow
- [ ] Header do drawer reorganizado para 5 botões Jurema em flex-wrap
- [ ] TabDados alinhada com schema Supabase (novos campos + selects Objetivo/Interesse/Finalidade/Status agendamento)
- [ ] `rtk tsc --noEmit` passa
- [ ] Build/dev roda sem erros de runtime
</success_criteria>

<output>
After completion, create `.planning/quick/260414-x36-corrigir-leaddrawer-scrollbar-botoes-err/260414-x36-SUMMARY.md`
</output>
