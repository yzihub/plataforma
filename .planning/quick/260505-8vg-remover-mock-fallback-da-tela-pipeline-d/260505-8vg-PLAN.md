---
phase: quick-260505-8vg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/jurema/pipeline/route.ts
  - src/app/cockpit/pipeline/page.tsx
  - src/components/yzihub/PipelineDashboardClient.tsx
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - "Tela /cockpit/pipeline carrega dados reais de jurema_deals + leads (sem mock)"
    - "Quando não há deals no tenant Jurema, a tela mostra empty state real (não fallback Café com Pam)"
    - "Cards exibem nome/telefone, deal_stage, qualification_status, lead_score, missing_fields (se houver) e updated_at"
    - "npm run typecheck e npm run build passam"
  artifacts:
    - path: "src/app/api/jurema/pipeline/route.ts"
      provides: "GET handler que retorna jurema_deals agrupados por deal_stage"
      exports: ["GET"]
    - path: "src/app/cockpit/pipeline/page.tsx"
      provides: "Server component que consome /api/jurema/pipeline (sem fallback cafePamData)"
    - path: "src/components/yzihub/PipelineDashboardClient.tsx"
      provides: "UI somente leitura para deals agrupados por deal_stage"
  key_links:
    - from: "src/app/cockpit/pipeline/page.tsx"
      to: "src/app/api/jurema/pipeline/route.ts"
      via: "fetch server-side ou função compartilhada"
      pattern: "jurema/pipeline"
    - from: "src/app/api/jurema/pipeline/route.ts"
      to: "supabase jurema_deals + leads"
      via: "supabase query com tenant_id filter"
      pattern: "from\\(\"jurema_deals\"\\)"
---

<objective>
Remover o fallback `cafePamData` da tela `/cockpit/pipeline` e conectar a UI aos dados reais do funil da Jurema (`jurema_deals` + `leads`) agrupados por `deal_stage`.

Purpose: Hoje a página `/cockpit/pipeline` usa o modelo legado (`pipeline_stages` + `leads`) com mock `cafePamData` como fallback. Para o tenant Jurema Brokers, os deals reais vivem em `jurema_deals` (criados pelo backend Agno/Ju). A tela precisa refletir esse funil real, não o modelo legado nem o mock do Café com Pam.

Output:
- Nova rota `GET /api/jurema/pipeline` que retorna deals do tenant Jurema agrupados por `deal_stage`, com join leve em `leads` (nome/telefone/email/score) quando houver `lead_id`.
- `page.tsx` consumindo a rota e passando `deals` + `stages` (constantes) para o client.
- `PipelineDashboardClient` reescrita para renderizar deals reais agrupados por stage (somente leitura, sem mock).
- Empty state real quando não houver deals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Arquivos chave do estado atual:
@src/app/cockpit/pipeline/page.tsx
@src/components/yzihub/PipelineDashboardClient.tsx
@src/lib/crm/types.ts
@src/lib/crm/mock-data.ts
@src/components/yzihub/JuremaKanbanClient.tsx
@src/app/api/imoveis/route.ts
@src/app/api/leads/route.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts

<interfaces>
<!-- Tipos e contratos relevantes extraídos do codebase. -->

Tabela jurema_deals (do CLAUDE.md, seção Tabelas Jurema):
```
id (uuid)
tenant_id (uuid)
lead_id (uuid | null)
deal_stage: qualificacao | perfil_busca | score | curadoria | corretor | visita | proposta | fechamento | nutricao | perdido
qualification_status: incompleto | frio | morno | quente | desqualificado
client_name, client_phone, client_email
intent, property_type, location_preference
budget_min, budget_max, bedrooms, suites, parking_spots
purpose, timeline, payment_method
lead_score (int 0-100)
assigned_broker_id, broker_status
metadata (jsonb) — pode conter missing_fields
created_at, updated_at
```

Tabela leads (campos relevantes):
```
id, tenant_id, name, email, phone, phone_normalized, status, score
ai_status, ai_temperature, ai_last_summary
created_at, updated_at
```

Tipo já existente (reutilizar):
```ts
// src/components/yzihub/JuremaKanbanClient.tsx
export type JuremaDeal = {
  id: string;
  tenant_id: string;
  lead_id: string | null;
  deal_stage: string;
  qualification_status: string | null;
  client_name: string | null;
  client_phone: string | null;
  intent: string | null;
  property_type: string | null;
  location_preference: string | null;
  budget_max: number | null;
  bedrooms: string | null;
  lead_score: number | null;
  broker_status: string | null;
  created_at: string;
  updated_at: string;
};
```

Stages canônicos (mesma ordem do CLAUDE.md/JuremaKanbanClient):
```
qualificacao, perfil_busca, curadoria, corretor, visita, proposta, fechamento, nutricao, perdido
```

Tenant Jurema (constante, do CLAUDE.md):
```
NEXT_PUBLIC_JUREMA_TENANT_ID = 82cc7aa9-fc6e-4f37-8d8e-8a71c1691361
```

Padrão DEV_BYPASS (de src/app/api/imoveis/route.ts):
```ts
const isDevBypass =
  process.env.NEXT_PUBLIC_DEV_BYPASS === "true" &&
  process.env.NODE_ENV !== "production";
// Se isDevBypass: usa createAdminClient() + tenant fixo Jurema
// Senão: createClient() server, getUser, profile.tenant_id
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar rota GET /api/jurema/pipeline com query real de jurema_deals + leads</name>
  <files>src/app/api/jurema/pipeline/route.ts</files>
  <action>
Criar nova rota App Router em `src/app/api/jurema/pipeline/route.ts` exportando `GET`.

Comportamento:
1. Resolver `tenantId` seguindo o padrão de `src/app/api/imoveis/route.ts`:
   - Se `NEXT_PUBLIC_DEV_BYPASS === "true"` e `NODE_ENV !== "production"`: usar `createAdminClient()` e `tenantId = "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"` (constante `JUREMA_TENANT_ID`).
   - Caso contrário: `createClient()` server, `auth.getUser()`, ler `profiles.tenant_id`. Se não autenticado → 401. Se sem perfil → 401.
2. Query `jurema_deals`:
   ```ts
   supabase
     .from("jurema_deals")
     .select("id, tenant_id, lead_id, deal_stage, qualification_status, client_name, client_phone, client_email, intent, property_type, location_preference, budget_max, bedrooms, lead_score, assigned_broker_id, broker_status, metadata, created_at, updated_at")
     .eq("tenant_id", tenantId)
     .order("updated_at", { ascending: false });
   ```
3. Coletar `lead_id` distintos não nulos. Se houver, buscar leads relacionados:
   ```ts
   supabase
     .from("leads")
     .select("id, name, email, phone, phone_normalized, score, ai_temperature, ai_status")
     .eq("tenant_id", tenantId)
     .in("id", leadIds);
   ```
4. Para cada deal, montar objeto enriquecido. Preferir, em ordem: `deal.client_name` → `lead.name`; `deal.client_phone` → `lead.phone`; `deal.client_email` → `lead.email`. `lead_score` vem de `deal.lead_score` (não recalcular). Extrair `missing_fields` de `deal.metadata?.missing_fields` se for array, senão `[]`.
5. Retornar JSON `{ tenant_id, total, by_stage: Record<string, EnrichedDeal[]>, deals: EnrichedDeal[] }` onde `by_stage` agrupa por `deal_stage`. Os 9 stages canônicos devem aparecer como chaves mesmo se vazios (pré-popular com `[]`):
   ```
   ["qualificacao","perfil_busca","curadoria","corretor","visita","proposta","fechamento","nutricao","perdido"]
   ```
6. Tratamento de erro: erro de query → 500 com `{ error }` e `console.error("[GET /api/jurema/pipeline] ...", err)`. Sem mock/fallback — array vazio é resposta válida (200).

NÃO duplicar regra de funil/score: o backend Agno é a fonte da verdade (CLAUDE.md regra 7). Só ler.
NÃO usar `service_role` direto no client; usar `createAdminClient()` apenas server-side, igual ao padrão de `imoveis/route.ts`.
NÃO mexer em workflows de Ju, Evolution, Agno, calendário ou contratos.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Rota responde 200 com JSON `{ tenant_id, total, by_stage, deals }` para tenant Jurema autenticado (ou DEV_BYPASS); 401 sem auth; 500 em erro de query. Tipo TypeScript compila sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever PipelineDashboardClient para renderizar deals agrupados por deal_stage</name>
  <files>src/components/yzihub/PipelineDashboardClient.tsx</files>
  <action>
Reescrever `PipelineDashboardClient.tsx` como tela somente leitura focada em `jurema_deals`. Manter o nome e o caminho do arquivo (consumido por `pipeline/page.tsx`).

Nova interface de props:
```ts
interface EnrichedDeal {
  id: string;
  tenant_id: string;
  lead_id: string | null;
  deal_stage: string;
  qualification_status: string | null;
  client_name: string | null;
  client_phone: string | null;
  client_email: string | null;
  intent: string | null;
  property_type: string | null;
  location_preference: string | null;
  budget_max: number | null;
  bedrooms: string | null;
  lead_score: number | null;
  broker_status: string | null;
  missing_fields: string[];
  updated_at: string;
  created_at: string;
}

interface PipelineDashboardClientProps {
  deals: EnrichedDeal[];
  byStage: Record<string, EnrichedDeal[]>;
  tenantName: string;
  total: number;
}
```

Layout (TailAdmin dark, padrão do projeto):
1. Header simples: título "Pipeline — {tenantName}" + contador `{total} deals`.
2. Empty state real: se `total === 0`, renderizar card centralizado: "Nenhum deal no funil ainda. Quando a Ju qualificar leads, os cards aparecem aqui." (sem fallback mock).
3. Caso contrário, grid de colunas (uma por stage canônico, ordem: qualificacao, perfil_busca, curadoria, corretor, visita, proposta, fechamento, nutricao, perdido). Reaproveitar visualmente o estilo do `JuremaKanbanClient` (colunas com header colorido + lista vertical de cards). Pode importar/copiar os helpers `formatBudget`, `displayName`, mapas `QUAL_BADGE` e `BROKER_LABELS` (se mais conveniente, copiar inline para evitar acoplamento).
4. Cada card mostra (todos opcionais com fallback "—"):
   - Linha 1: `client_name || client_phone || "—"` (truncate)
   - Linha 2: badges `qualification_status` + `Score {lead_score}`
   - Linha 3: `intent · property_type` (capitalize)
   - Linha 4: `location_preference · {formatBudget(budget_max)}`
   - Linha 5: `{bedrooms} quarto(s)` se houver
   - Linha 6: badge `broker_status` (label legível)
   - Linha 7: `missing_fields` — se array não vazio, badge âmbar "Faltam: {missing_fields.slice(0,3).join(", ")}" (truncar para não estourar largura)
   - Linha 8 (footer): `Atualizado {relativeTime(updated_at)}` em texto pequeno cinza
5. Função `relativeTime(iso)`: simples `Intl.RelativeTimeFormat("pt-BR")` ou fallback "há X dias/horas/min".

REMOVER completamente:
- imports de `Lead`, `PipelineStage` de `@/lib/crm/types` (não são mais usados aqui).
- imports e uso dos sub-componentes `PipelineHeader`, `PipelineAlerts`, `PipelineKPIs`, `PipelineCharts`, `PipelineLeadsList`, `AssignBrokerModal` (ficam no disco mas não são mais consumidos por esta tela — não deletar pois podem ser reaproveitados depois).
- estado de filtros (`filters`, `setFilters`) e modal de atribuição (`modalState`). Tela é somente leitura.
- handlers `handleConfirmAssign`, `openAssignModal`, etc.

NÃO permitir drag-n-drop. NÃO permitir trocar stage manualmente. NÃO calcular score, missing_fields ou stage no front (CLAUDE.md regra 6 e 7).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Componente compila com a nova assinatura de props. Renderiza colunas por stage com cards reais. Empty state aparece quando `total === 0`. Sem referências a `cafePamData`, `Lead`, `PipelineStage`, ou aos sub-componentes antigos.</done>
</task>

<task type="auto">
  <name>Task 3: Reescrever pipeline/page.tsx consumindo /api/jurema/pipeline (sem mock)</name>
  <files>src/app/cockpit/pipeline/page.tsx</files>
  <action>
Reescrever `src/app/cockpit/pipeline/page.tsx` removendo todo o uso de `cafePamData`, `Lead`, `PipelineStage`, `pipeline_stages`, `brokers` e o tipo `Broker` local.

Comportamento:
1. Server Component async. Resolver `tenantId` igual à rota: DEV_BYPASS → tenant Jurema fixo + `createAdminClient`; senão `createClient` server + `auth.getUser` + `profiles.tenant_id`.
2. Em vez de chamar a API por HTTP, **chamar diretamente uma função compartilhada** (mais simples e estável em SSR) — extrair a lógica de query da Task 1 para função `fetchJuremaPipeline(supabase, tenantId)` exportada de `src/app/api/jurema/pipeline/route.ts` OU criar `src/lib/jurema/pipeline.ts` com a função e importar tanto na rota quanto no page. **Escolher: criar `src/lib/jurema/pipeline.ts`** com a função pura `fetchJuremaPipeline(supabase: SupabaseClient, tenantId: string): Promise<{ tenant_id; total; by_stage; deals }>` e refatorar a Task 1 para usar essa função (atualizar a rota agora, não criar dois caminhos). Atualizar `files_modified` mentalmente; o arquivo extra `src/lib/jurema/pipeline.ts` é criado nesta task.
3. Buscar `tenants.name` (ou usar "Jurema Brokers" como fallback se a query falhar) para `tenantName`.
4. Renderizar `<PipelineDashboardClient deals={deals} byStage={by_stage} total={total} tenantName={tenantName} />`.
5. Em caso de exceção (try/catch externo): renderizar UI mínima de erro: "Erro ao carregar pipeline. Tente novamente em instantes." (NÃO renderizar mock).

REMOVER:
- `import { cafePamData } from "@/lib/crm/mock-data"`.
- `import type { Lead, PipelineStage } from "@/lib/crm/types"`.
- bloco `fallback = { leads: cafePamData.leads, stages: cafePamData.stages, ... }`.
- query a `pipeline_stages` e `brokers`.
- a interface `Broker` local (não é mais usada na page).

Validar local:
- Sem auth (e sem DEV_BYPASS): página deve renderizar UI de erro/empty (ou redirect via `proxy.ts` se já protegida — deixar comportamento atual de proxy intacto).
- Com tenant Jurema autenticado e nenhum deal: empty state real.
- Com deals: colunas populadas.

NÃO chamar n8n direto. NÃO usar `service_role` em código client/edge. NÃO mexer em contratos.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build</automated>
  </verify>
  <done>`page.tsx` não importa `cafePamData` nem tipos legados. `npm run build` passa sem erro. Em runtime, `/cockpit/pipeline` renderiza dados reais de `jurema_deals` ou empty state — nunca mock.</done>
</task>

</tasks>

<verification>
Verificações ao final do plano:

1. `npx tsc --noEmit` passa sem erros.
2. `npm run build` passa.
3. Grep no projeto: `cafePamData` não aparece mais em `src/app/cockpit/pipeline/` nem em `src/components/yzihub/PipelineDashboardClient.tsx`.
4. Endpoint manual: `curl http://localhost:3000/api/jurema/pipeline` (com DEV_BYPASS=true ou cookie de sessão Jurema) retorna JSON `{ tenant_id: "82cc7aa9-...", total: N, by_stage: {...}, deals: [...] }`.
5. Em browser: `/cockpit/pipeline` carrega sem erro de console; com deals reais aparecem cards; sem deals aparece empty state real (não cards do Café com Pam).
</verification>

<success_criteria>
- [ ] Arquivo `src/app/api/jurema/pipeline/route.ts` existe e retorna 200 com payload `{ tenant_id, total, by_stage, deals }`.
- [ ] Arquivo `src/lib/jurema/pipeline.ts` existe e é importado tanto pela rota quanto pelo page.
- [ ] `src/app/cockpit/pipeline/page.tsx` não contém referências a `cafePamData`, `Lead`, `PipelineStage`, `pipeline_stages` ou `brokers`.
- [ ] `src/components/yzihub/PipelineDashboardClient.tsx` recebe nova interface `{ deals, byStage, total, tenantName }` e renderiza colunas por `deal_stage`.
- [ ] Cards exibem: nome/telefone, deal_stage (via coluna), qualification_status, lead_score, missing_fields (quando houver), updated_at (relativo).
- [ ] Empty state real quando `total === 0` (sem fallback mock).
- [ ] `npx tsc --noEmit` passa.
- [ ] `npm run build` passa.
- [ ] Nenhuma alteração em: workflows da Ju/Agno, Evolution, calendário, contratos, schema Supabase.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260505-8vg-remover-mock-fallback-da-tela-pipeline-d/260505-8vg-SUMMARY.md` documentando:
- Arquivos criados/alterados
- Como testar local (DEV_BYPASS=true ou login real Jurema)
- Comportamento de empty state validado
- Pendências (ex.: filtros, KPIs, lista operacional ficaram fora do escopo desta task — referenciar o componente legado `PipelineLeadsList` que continua em disco para futura reativação)
</output>
