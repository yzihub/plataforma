---
phase: quick
plan: 260404-d6a
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/contracts.ts
  - src/lib/contracts/mock-data.ts
  - src/components/yzihub/ContractsClient.tsx
  - src/components/yzihub/ContractDrawer.tsx
  - src/app/cockpit/contratos/page.tsx
  - src/layout/AppSidebar.tsx
autonomous: true
must_haves:
  truths:
    - "Usuario pode acessar /cockpit/contratos via sidebar na secao Gestao"
    - "Usuario ve lista de contratos em DataTable com colunas: Lead, Valor, Status, Tipo, Vencimento"
    - "Usuario pode abrir drawer lateral com detalhes do contrato ao clicar em uma linha"
    - "Contratos exibem status visual (Rascunho, Enviado, Assinado, Cancelado) com badges coloridos"
    - "Dados do contrato referenciam lead_id e tenant_id do TenantContext"
  artifacts:
    - path: "src/types/contracts.ts"
      provides: "Contract type definitions + status config"
    - path: "src/lib/contracts/mock-data.ts"
      provides: "Mock contracts data for two tenants"
    - path: "src/components/yzihub/ContractsClient.tsx"
      provides: "DataTable view of contracts with search/filter"
    - path: "src/components/yzihub/ContractDrawer.tsx"
      provides: "Slide-over drawer with contract details"
    - path: "src/app/cockpit/contratos/page.tsx"
      provides: "Route page wiring ContractsClient"
    - path: "src/layout/AppSidebar.tsx"
      provides: "Sidebar nav entry under Gestao section"
  key_links:
    - from: "src/app/cockpit/contratos/page.tsx"
      to: "src/components/yzihub/ContractsClient.tsx"
      via: "default import"
    - from: "src/components/yzihub/ContractsClient.tsx"
      to: "src/context/TenantContext.tsx"
      via: "useTenantContext() for tenant_id filtering"
    - from: "src/components/yzihub/ContractsClient.tsx"
      to: "src/components/yzihub/ContractDrawer.tsx"
      via: "drawer open state on row click"
---

<objective>
Construir sistema de contratos integrado ao TenantContext com DataTable + Drawer, adicionando rota /cockpit/contratos e entrada no sidebar.

Purpose: Permitir que cada tenant visualize e gerencie contratos vinculados a leads, completando o fluxo CRM -> Proposta -> Contrato.
Output: Pagina de contratos funcional com mock data, types, DataTable e Drawer no padrao TailAdmin dark.
</objective>

<execution_context>
@.claude/skills/yzihub-patterns/SKILL.md
</execution_context>

<context>
@CLAUDE.md
@src/context/TenantContext.tsx
@src/lib/crm/types.ts
@src/types/crm.ts
@src/components/yzihub/LeadDrawer.tsx (drawer pattern reference)
@src/app/cockpit/crm/page.tsx (DataTable + page pattern reference)
@src/layout/AppSidebar.tsx (sidebar nav structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, mock data e sidebar entry</name>
  <files>src/types/contracts.ts, src/lib/contracts/mock-data.ts, src/layout/AppSidebar.tsx</files>
  <action>
1. Create `src/types/contracts.ts` with:
   - `ContractStatus` type: `'draft' | 'sent' | 'signed' | 'cancelled' | 'expired'`
   - `ContractType` type: `'service' | 'sale' | 'partnership' | 'rental'`
   - `Contract` interface with fields: `id` (string), `tenant_id` (string), `lead_id` (string), `lead_name` (string), `title` (string), `type` (ContractType), `status` (ContractStatus), `value` (number), `description` (string | null), `signed_at` (string | null), `expires_at` (string | null), `created_at` (string), `updated_at` (string)
   - `CONTRACT_STATUS_CONFIG` record mapping each status to `{ color: BadgeVariant, label: string }` using the BadgeVariant type from `@/types/crm`:
     - draft: `{ color: 'light', label: 'Rascunho' }`
     - sent: `{ color: 'warning', label: 'Enviado' }`
     - signed: `{ color: 'success', label: 'Assinado' }`
     - cancelled: `{ color: 'error', label: 'Cancelado' }`
     - expired: `{ color: 'dark', label: 'Expirado' }`
   - `CONTRACT_TYPE_LABELS` record: `{ service: 'Servico', sale: 'Venda', partnership: 'Parceria', rental: 'Locacao' }`

2. Create `src/lib/contracts/mock-data.ts` with ~8-10 mock contracts:
   - Use tenant IDs matching the two active clients (Cafe com Pam and Jurema Brokers) from the existing seeds
   - Reference realistic lead names, varied statuses, BRL values (R$500 to R$50.000 range)
   - Mix of contract types and statuses
   - Dates spread over last 3 months

3. Update `src/layout/AppSidebar.tsx`:
   - Add "Contratos" as a child item under the "Financeiro" submenu in the "Gestao" section
   - Path: `/cockpit/contratos`
   - Add it after "Comissoes" and before "Geral": `{ name: "Contratos", path: "/cockpit/contratos" }`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Types file exports Contract, ContractStatus, ContractType, CONTRACT_STATUS_CONFIG, CONTRACT_TYPE_LABELS. Mock data exports an array of 8-10 contracts. Sidebar shows Contratos link under Financeiro.</done>
</task>

<task type="auto">
  <name>Task 2: ContractsClient DataTable + ContractDrawer + route page</name>
  <files>src/components/yzihub/ContractsClient.tsx, src/components/yzihub/ContractDrawer.tsx, src/app/cockpit/contratos/page.tsx</files>
  <action>
1. Create `src/components/yzihub/ContractDrawer.tsx` following the exact pattern from LeadDrawer.tsx:
   - Props: `contract: Contract | null`, `onClose: () => void`
   - Fixed right slide-over with backdrop (same transition pattern as LeadDrawer)
   - Header: contract title, type badge, status badge
   - Body sections (no tabs, simpler than LeadDrawer — single scrollable view):
     - "Informacoes" block: Lead vinculado (lead_name), Valor (formatted BRL), Tipo, Descricao
     - "Datas" block: Criado em, Assinado em (or "Pendente"), Vencimento (or "Sem vencimento")
     - "Acoes" block: Two ghost buttons — "Enviar Contrato" and "Marcar como Assinado" (non-functional, UI only for now)
   - Use same dark theme classes: `dark:bg-gray-900`, `dark:border-gray-800`, `dark:text-white/90`
   - Use CloseIcon from `@/icons` for close button

2. Create `src/components/yzihub/ContractsClient.tsx` ("use client"):
   - Import `useTenantContext` from `@/context/TenantContext`
   - Import mock contracts, filter by `tenant.id` (or show all if DEV_BYPASS with "dev-tenant")
   - Layout: header row with title "Contratos" + tenant name + count, search input, status filter dropdown
   - DataTable using `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableCell` from `@/components/ui/table` (same imports as CRM page)
   - Columns: Lead (lead_name), Titulo, Valor (BRL formatted), Status (Badge component), Tipo, Vencimento (formatted date or "—")
   - Row click opens ContractDrawer
   - Search filters by lead_name or title (case insensitive)
   - Status filter dropdown with all ContractStatus options + "Todos"
   - Use motion from framer-motion for row entrance animation (same pattern as CRM page GridView)
   - Follow the TailAdmin dark theme: rounded-2xl border cards, gray-50/gray-800 alternating

3. Create `src/app/cockpit/contratos/page.tsx`:
   - Simple "use client" page that renders `<ContractsClient />`
   - No additional layout needed (inherits from cockpit layout.tsx)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - /cockpit/contratos renders DataTable with mock contracts
    - Clicking a row opens ContractDrawer slide-over with full details
    - Search and status filter work correctly
    - Tenant filtering via useTenantContext is wired
    - Visual style matches existing CRM page (TailAdmin dark theme)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (or `next build`)
3. Navigate to /cockpit/contratos — DataTable renders with mock data
4. Sidebar shows "Contratos" under Financeiro submenu
5. Click row — drawer slides in from right with contract details
6. Search and filter narrow results correctly
</verification>

<success_criteria>
- Contract types exported and reusable
- Mock data covers varied statuses and types for two tenants
- DataTable follows exact same visual pattern as CRM page (Table components, Badge, motion rows)
- ContractDrawer follows LeadDrawer slide-over pattern
- Sidebar entry under Gestao > Financeiro > Contratos
- TenantContext integration filters contracts by active tenant
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260404-d6a-construir-sistema-de-contratos-integrado/260404-d6a-SUMMARY.md`
</output>
