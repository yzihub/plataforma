---
phase: quick-260415-1nb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/LeadsDataTable.tsx
  - src/components/yzihub/LeadsClient.tsx
autonomous: false
requirements:
  - UI-LEADS-TABLE-TAILADMIN-MIRROR

must_haves:
  truths:
    - "Lista de leads renderiza no padrão TailAdmin Datatable 3 (tabs de status + search + action button no header, paginação no footer)"
    - "Tabs de status substituem o <select> de status, com contagem por status e estado ativo estilizado"
    - "Coluna de ações usa icon buttons (edit → abre LeadDrawer; qualificar → POST /api/actions/execute) sem labels longos quebrando o layout"
    - "Todos os campos exibidos são dados reais do Lead Supabase (sem mocks: bairro mock e score mock são removidos ou preservados somente se já vierem do banco)"
    - "Paginação 'Showing X to Y of Z results' com botões Prev/Next funciona sobre filteredLeads"
    - "Dark mode TailAdmin preservado (bg-gray-900 base, bg-gray-800 borders, hover em rows)"
    - "Toggle Kanban/Table existente continua funcionando sem regressão"
  artifacts:
    - path: "src/components/yzihub/LeadsDataTable.tsx"
      provides: "Datatable TailAdmin espelhada com tabs, search, paginação e icon actions"
      contains: "status tabs, pagination, icon buttons"
    - path: "src/components/yzihub/LeadsClient.tsx"
      provides: "Header ajustado: search bar movida p/ dentro da tabela card (padrão TailAdmin), botão Novo Lead preservado"
      contains: "LeadsDataTable integration"
  key_links:
    - from: "src/components/yzihub/LeadsDataTable.tsx"
      to: "LeadDrawer via onSelect prop"
      via: "click no icon edit"
      pattern: "onSelect\\?\\(lead\\)"
    - from: "src/components/yzihub/LeadsDataTable.tsx"
      to: "/api/actions/execute"
      via: "fetch POST no icon qualificar"
      pattern: "fetch.*api/actions/execute"
---

<objective>
Espelhar fielmente o padrão TailAdmin Datatable 3 na lista de leads (`LeadsDataTable.tsx`), substituindo mocks pelos campos reais do Lead Supabase (Jurema Brokers). Manter busca, filtros (como tabs), paginação e coluna de ações conforme o TailAdmin original — sem inventar layout.

Purpose: O layout atual mistura padrões customizados com campos mock (bairro, score inferido). O usuário exigiu espelhamento 1:1 do TailAdmin — só trocar os campos pelos dados reais.

Output: `LeadsDataTable.tsx` reescrita no padrão TailAdmin Datatable 3 + ajuste mínimo em `LeadsClient.tsx` para mover a SearchBar (já que o padrão TailAdmin coloca search + action dentro do card da tabela).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/components/yzihub/LeadsClient.tsx
@src/components/yzihub/LeadsDataTable.tsx
@src/components/yzihub/LeadDrawer.tsx
@src/lib/crm/types.ts
@src/components/ui/table/index.tsx
@src/components/ui/badge/Badge.tsx

<interfaces>
<!-- Contratos já existentes no projeto que o executor DEVE consumir -->

Lead type (src/lib/crm/types.ts) — já usado no arquivo atual:
- id: string
- name: string
- phone: string | null
- status: "new" | "contacted" | "qualified" | "meeting" | "proposal" | "negotiation" | "won" | "lost"
- source: string | null
- assigned_to: string | null (corretor UUID ou nome)
- value: number
- score: number
- created_at / last_action_at

Endpoint de ações (conforme CLAUDE.md, Action Flow):
- POST /api/actions/execute
- Body: { action: string, lead_id: string, tenant_id?: string }
- Frontend NUNCA chama n8n direto — sempre via esse endpoint que enfileira em job_queue.

STATUS_BADGE map (já definido em LeadsDataTable.tsx linhas 18-27) — reaproveitar:
- new → info "🔥 Novo Lead"
- contacted → warning "📞 Contato"
- qualified → primary "📅 Agendado"
- meeting → primary "📅 Reunião"
- proposal → warning "💰 Proposta"
- negotiation → warning "📋 Contrato"
- won → success "✅ Fechado"
- lost → dark "❌ Perdido"
</interfaces>

## Referência visual TailAdmin (Datatable 3)

Estrutura a espelhar:
1. Header da tabela (dentro do card): `[Tabs de status] ........ [Search] [Novo Lead]`
2. Tabela com colunas: Avatar+Nome | WhatsApp | Status (badge) | Score | Corretor | Origem | Valor | Ações (icons)
3. Footer: `Showing 1 to 10 of 42 results` + `[Prev] [1] [2] [3] [Next]`
4. Dark: `bg-white dark:bg-white/[0.03]` card, `dark:border-gray-800` bordas, hover `dark:hover:bg-white/[0.02]`

## Regras do usuário (absolutas)
1. NÃO inventar layout novo — espelhar TailAdmin
2. NÃO adaptar parcialmente — espelhar inteiro
3. SÓ trocar campos pelos dados reais
4. Manter busca, filtros, paginação e ações no padrão TailAdmin
5. Botões de ação como icon buttons (edit + qualificar), não labels longos

## O que NÃO mexer
- LeadsKanban.tsx
- LeadDrawer.tsx (funcionalidade)
- Toggle view Kanban/Table no LeadsClient.tsx (deve continuar funcionando)
- Hook de leads/Supabase (dados já vêm prontos via `initialLeads`)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescrever LeadsDataTable.tsx no padrão TailAdmin Datatable 3</name>
  <files>src/components/yzihub/LeadsDataTable.tsx</files>
  <behavior>
    - Card wrapper: rounded-2xl border bg-white dark:bg-white/[0.03] dark:border-gray-800
    - Header interno (acima da tabela): tabs de status à esquerda + search à direita + botão "Novo Lead" (slot opcional via prop)
    - Tabs: "Todos" + cada status do Lead, cada tab com contagem entre parênteses. Active = bg-brand-500/10 text-brand-500 border-b-2 border-brand-500. Inactive = text-gray-500 hover:text-gray-700
    - Colunas (substituir mocks): Nome (avatar+nome) | WhatsApp | Status (badge) | Score | Corretor | Origem | Valor Imóvel | Ações
    - REMOVER coluna "Bairro Interesse" (é mock — BAIRROS array) e helper bairroFromId
    - Ações como icon buttons (24px): botão lápis (edit) abre onSelect(lead) — que chama LeadDrawer; botão raio/zap (qualificar) faz fetch POST /api/actions/execute com {action:"qualify", lead_id:lead.id} e mostra toast ou disable durante request
    - Footer paginação: 10 leads por página; "Showing X to Y of Z results" à esquerda; botões Prev [páginas] Next à direita (padrão TailAdmin)
    - Hover row: hover:bg-gray-50 dark:hover:bg-white/[0.02] — row inteira clicável continua abrindo drawer (mantém UX)
    - Empty state: preservar o existente (UserCircleIcon + "Nenhum lead encontrado")
  </behavior>
  <action>
    Reescrever `src/components/yzihub/LeadsDataTable.tsx` completamente no padrão TailAdmin Datatable 3:

    1. **Props estendidas**:
       ```ts
       interface LeadsDataTableProps {
         leads: Lead[];
         onSelect?: (lead: Lead) => void;
         headerActions?: React.ReactNode; // slot p/ botão "Novo Lead" vindo do LeadsClient
         activeStatus: string; // "" = todos
         onStatusChange: (status: string) => void;
         search: string;
         onSearchChange: (q: string) => void;
       }
       ```
       (Filtros agora moram dentro da tabela — LeadsClient passa state via props.)

    2. **Header do card** (dentro do wrapper rounded-2xl, acima da <Table>):
       - Row 1: Tabs de status (scroll-x se overflow) com contagens derivadas de `leads` (array completo, não filtrado por status) — contagem de "Todos" = leads.length, cada tab = leads.filter(l => l.status === s.value).length
       - Row 2 (ou mesmo row em flex-wrap): Search input (reaproveitar o SVG de lupa do LeadsClient atual) + `{headerActions}` à direita

    3. **Tabela**:
       - Remover `<colgroup>` de larguras fixas excessivas — usar larguras TailAdmin (deixar flex natural, só forçar w-min em Ações)
       - REMOVER coluna "Bairro Interesse" e função `bairroFromId` + array `BAIRROS`
       - Headers: `["Lead", "WhatsApp", "Status", "Score", "Corretor", "Origem", "Valor", "Ações"]`
       - Row onClick continua chamando `onSelect?.(lead)` (row inteira clicável)

    4. **Coluna Ações (icon buttons)**:
       - Trocar o `<span>Ver detalhes →</span>` por dois icon buttons dentro de `<div className="flex items-center gap-2">`
       - Botão editar: ícone lápis (inline SVG TailAdmin), `onClick={(e) => { e.stopPropagation(); onSelect?.(lead); }}`, title="Editar", className padrão icon button TailAdmin (`p-2 rounded-lg text-gray-500 hover:bg-gray-100 hover:text-gray-700 dark:hover:bg-white/[0.05] dark:hover:text-white/90 transition-colors`)
       - Botão qualificar: ícone raio/bolt inline SVG, `onClick` chama handler async:
         ```ts
         async function handleQualify(lead: Lead, e: React.MouseEvent) {
           e.stopPropagation();
           try {
             await fetch("/api/actions/execute", {
               method: "POST",
               headers: {"content-type": "application/json"},
               body: JSON.stringify({ action: "qualify", lead_id: lead.id }),
             });
           } catch (err) { console.error(err); }
         }
         ```
         (Sem toast library nova — apenas console.error. Se sonner já existir no projeto usar; se não, deixar console.)
       - Nunca chamar n8n direto (regra Action Flow do CLAUDE.md).

    5. **Footer paginação**:
       - useState<number> pageIndex dentro do componente; PAGE_SIZE = 10
       - `const paginated = leads.slice(pageIndex * 10, (pageIndex+1) * 10);` — itera sobre `paginated` no TableBody
       - IMPORTANTE: quando `activeStatus`/`search` mudam, resetar pageIndex para 0 via useEffect
       - Footer div separado abaixo da <Table> (ainda dentro do card wrapper), com border-top:
         - Esquerda: `Showing {start} to {end} of {leads.length} results`
         - Direita: botões Prev/Next com states disabled nos extremos; entre eles números de página (máx 5 visíveis, estilo TailAdmin: active = bg-brand-500 text-white, inactive = border bg-white dark:bg-white/[0.03])

    6. **Dark mode**: validar que todas as novas classes têm variante `dark:` seguindo o resto do componente.

    7. **Manter**: `getInitials`, `avatarColor`, `formatCurrency`, `formatPhone`, `scoreBadge`, `formatCorretor`, `LeadAvatar`, `STATUS_BADGE`, empty state.
       **Remover**: `BAIRROS`, `bairroFromId`.

    Nota: NÃO criar novos arquivos. Tudo dentro deste arquivo.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - TypeScript compila sem erros
    - Componente exporta default function com props novas (leads, onSelect, headerActions, activeStatus, onStatusChange, search, onSearchChange)
    - Tabs de status renderizam com contagem; clicando muda activeStatus via callback
    - Search input dentro do card, lupa à esquerda, chama onSearchChange
    - Coluna "Bairro Interesse" foi removida; array BAIRROS e bairroFromId removidos
    - Ações: 2 icon buttons (edit + qualify); edit chama onSelect, qualify faz POST /api/actions/execute
    - Paginação de 10 itens com "Showing X to Y of Z" + Prev/Next funcionando
    - Dark mode preservado em todas as novas classes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ajustar LeadsClient.tsx para integrar header da tabela TailAdmin</name>
  <files>src/components/yzihub/LeadsClient.tsx</files>
  <behavior>
    - Remover o componente SearchBar inline (search + status select + source select) do return quando view === "table"
    - Manter search state (`search`, `setSearch`) e status state (`filterStatus`, `setFilterStatus`) no LeadsClient — passar para LeadsDataTable via props
    - Filtro de source (origem) continua existindo MAS movido para cima da tabela OU mantido no header externo discreto (decisão: manter apenas no SearchBar quando view === "kanban"; remover do view === "table" para seguir estritamente o TailAdmin que só tem tabs de status + search)
    - Botão "Novo Lead" do header externo é movido para `headerActions` prop da LeadsDataTable (view === "table")
    - No view === "kanban", preservar o SearchBar externo atual (busca + status + source) — não quebrar o Kanban
    - Toggle view Kanban/Table permanece no header externo
    - filteredLeads continua computado no LeadsClient via useMemo aplicando search + filterStatus + filterSource (para kanban); para table, LeadsDataTable recebe leads completos + filtros e filtra internamente OU recebe já filtrado
  </behavior>
  <action>
    Ajustar `src/components/yzihub/LeadsClient.tsx`:

    1. **Manter** estados: `view`, `leads`, `search`, `filterStatus`, `filterSource`, `selectedLead`.

    2. **Refatorar filteredLeads**:
       - Para Kanban: continuar aplicando todos os 3 filtros (search + status + source) como hoje.
       - Para Table: a DataTable recebe os leads filtrados por source (filtro externo que ainda vamos preservar só no Kanban) — na verdade, simplificar: passar `leads` (todos) para DataTable e deixar a tabela filtrar por search + status internamente. Source não existe mais no modo table (conforme regra TailAdmin).
       - Separar em dois memos:
         ```ts
         const kanbanLeads = useMemo(() => leads.filter(l =>
           (!search || l.name.toLowerCase().includes(search.toLowerCase()) || (l.phone??"").includes(search)) &&
           (!filterStatus || l.status === filterStatus) &&
           (!filterSource || l.source === filterSource)
         ), [leads, search, filterStatus, filterSource]);

         const tableLeads = useMemo(() => leads.filter(l =>
           (!search || l.name.toLowerCase().includes(search.toLowerCase()) || (l.phone??"").includes(search)) &&
           (!filterStatus || l.status === filterStatus)
         ), [leads, search, filterStatus]);
         ```

    3. **Header externo quando view === "table"**:
       - Mantém: título "Leads" + subtítulo com contagem + toggle view
       - REMOVE o botão "Novo Lead" do header externo (vai pra dentro da tabela via headerActions)
       - Ou mais simples: deixa o botão onde está e NÃO passa headerActions — aí o card da tabela tem só tabs + search. Escolha: **manter "Novo Lead" no header externo ao lado do toggle view** (é o comportamento menos intrusivo e não viola o TailAdmin que permite action button no header externo do page; TailAdmin Datatable 3 mostra "Add Product" dentro do card, mas um "Novo Lead" fora também é aceitável). **DECISÃO: mover o botão Novo Lead para dentro da DataTable via prop headerActions, conforme pedido do usuário ("ESPELHAR o TailAdmin").**

    4. **Render view === "table"**:
       - Remover `<SearchBar ... />` do render
       - Substituir `<LeadsDataTable leads={filteredLeads} onSelect={setSelectedLead} />` por:
         ```tsx
         <LeadsDataTable
           leads={tableLeads}
           onSelect={setSelectedLead}
           activeStatus={filterStatus}
           onStatusChange={setFilterStatus}
           search={search}
           onSearchChange={setSearch}
           headerActions={
             <button type="button" className="inline-flex shrink-0 items-center gap-2 rounded-xl bg-brand-500 px-4 py-2.5 text-sm font-medium text-white shadow-sm hover:bg-brand-600 transition-colors">
               <PlusIcon className="size-4" />
               Novo Lead
             </button>
           }
         />
         ```
       - Remover o botão "Novo Lead" do header externo APENAS no view === "table". No view === "kanban", manter o botão externo + SearchBar externo como hoje.

    5. **Render view === "kanban"**: manter exatamente como está (SearchBar externo + LeadsKanban + botão Novo Lead no header externo).

    6. Função `SearchBar` e arrays `ALL_STATUS`/`ALL_SOURCES` permanecem (ainda usados pelo Kanban).

    7. Contador de subtítulo: usar `tableLeads.length` quando view === "table", `kanbanLeads.length` quando view === "kanban".
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - TypeScript compila
    - View table: tabela renderiza com header interno (tabs + search + Novo Lead); SearchBar externo sumiu
    - View kanban: SearchBar externo + Novo Lead externo preservados (sem regressão)
    - Toggle view Kanban/Table funciona em ambos os sentidos
    - filterStatus propaga corretamente entre client e DataTable
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação visual — tabela espelha TailAdmin Datatable 3</name>
  <what-built>
    LeadsDataTable reescrita no padrão TailAdmin Datatable 3:
    - Tabs de status com contagem no header do card
    - Search bar + botão Novo Lead dentro do header do card
    - Coluna Bairro removida (era mock)
    - Ações como 2 icon buttons (editar + qualificar)
    - Paginação de 10 leads com Prev/Next e "Showing X to Y of Z"
    - Dark mode TailAdmin preservado
  </what-built>
  <how-to-verify>
    1. Rodar `rtk pnpm dev` e acessar `/cockpit/leads` (ou rota equivalente dos leads Jurema)
    2. Modo Table (default): conferir visualmente contra a screenshot TailAdmin Datatable 3 fornecida:
       - [ ] Tabs de status no topo da tabela (Todos, Novo Lead, Contato, Agendado, ...) com números de contagem
       - [ ] Search à direita do header + botão "Novo Lead" (azul/brand) no canto
       - [ ] Colunas: Lead (avatar+nome) | WhatsApp | Status (badge) | Score | Corretor | Origem | Valor | Ações
       - [ ] NÃO tem mais coluna "Bairro Interesse"
       - [ ] Ações = 2 icones pequenos (lápis + raio/zap), sem texto longo
       - [ ] Rodapé: "Showing 1 to 10 of N results" + botões Prev/Next funcionais
       - [ ] Hover na row destaca linha inteira
       - [ ] Dark mode (se ativo no app): fundo escuro, bordas cinza-800
    3. Clicar em uma tab de status → lista filtra e paginação reseta para 1
    4. Digitar no search → lista filtra em tempo real
    5. Clicar no icon lápis de um lead → LeadDrawer abre
    6. Clicar no icon raio de um lead → network tab mostra POST /api/actions/execute com `{action:"qualify", lead_id:"..."}`
    7. Clicar numa row (fora dos icons) → LeadDrawer abre também (UX preservada)
    8. Alternar para view Kanban via toggle → Kanban renderiza sem regressão; SearchBar externa (com filtro de origem) aparece só no Kanban
    9. Voltar para Table → volta ao novo layout TailAdmin
  </how-to-verify>
  <resume-signal>Digite "approved" se a tabela espelha o TailAdmin, ou descreva o que ainda não bate com a referência</resume-signal>
</task>

</tasks>

<verification>
- `rtk tsc --noEmit` passa sem erros
- `rtk next build` não quebra (executar se tsc passar)
- Inspeção visual bate com screenshot TailAdmin Datatable 3 fornecida pelo usuário
- Nenhum arquivo além dos 2 listados em `files_modified` foi tocado
- KanbanBoard, LeadDrawer e toggle view continuam funcionando
</verification>

<success_criteria>
- Lista de leads em view Table é visualmente indistinguível do TailAdmin Datatable 3 (só mudando os campos para dados reais Jurema)
- Busca, filtro de status (agora como tabs), paginação e ações (icons) funcionam
- Mock de bairro e labels longos de ação foram eliminados
- Nenhuma regressão no Kanban ou no drawer
- Commit realizado com mensagem padrão `feat(quick-260415-1nb): espelhar Datatable TailAdmin na lista de leads`
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260415-1nb-espelhar-datatable-tailadmin-na-lista-de/260415-1nb-SUMMARY.md` documentando:
- Decisões visuais tomadas (ex: manter row inteira clicável além dos icons)
- Diff resumido dos 2 arquivos
- Campos mock removidos (BAIRROS, bairroFromId)
- Endpoint consumido nos botões de ação
</output>
