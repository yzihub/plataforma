---
phase: quick
plan: 260409-e9u
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [PADRONIZAR-ENTRADA-CONTRATO]

must_haves:
  truths:
    - "Webhook do workflow aceita phone, tenant_id e imovel_id como entrada"
    - "record_id do Airtable nao e mais usado na entrada do workflow"
    - "Logica interna do fluxo (geracao, envio, persistencia) permanece inalterada"
  artifacts: []
  key_links:
    - from: "Webhook node"
      to: "Primeiro node pos-webhook"
      via: "phone + tenant_id + imovel_id no body"
      pattern: "phone.*tenant_id.*imovel_id"
---

<objective>
Remover o uso de record_id (Airtable) da entrada do workflow "Onboarding | 2.1 Gerando Contrato" (bzK9KbNa5zEYcurj) e padronizar a entrada para: phone, tenant_id, imovel_id.

Purpose: Alinhar este workflow ao padrao ja adotado nos workflows migrados (consultar_imoveis, setar_lead_quente) onde a entrada usa phone+tenant_id em vez de record_id Airtable.
Output: Workflow n8n atualizado com nova entrada padronizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260409-ds4-mapear-workflow-de-gera-o-de-contrato-ex/260409-ds4-SUMMARY.md

Contexto do workflow (mapeado em 260409-ds4):
- Workflow ID: bzK9KbNa5zEYcurj
- Nome: "Onboarding | 2.1 Gerando Contrato"
- Entrada ATUAL: Webhook POST /webhook/enviar-contrato recebe { record_id } como query param
- O record_id e usado no node "Get a record" (Airtable) para buscar dados do contrato
- Depois os dados passam por Code JS, Switch, Set, AI Agent, Google Docs, etc.

Padrao dos workflows ja migrados:
- consultar_imoveis: entrada com phone + tenant_id
- setar_lead_quente: entrada com phone + tenant_id
- Padrão: body JSON com { phone, tenant_id, ... } em vez de query params com record_id
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ler workflow atual e identificar nodes que usam record_id</name>
  <files>nenhum — read-only via n8n MCP</files>
  <action>
    Usar ferramenta MCP n8n para buscar o workflow bzK9KbNa5zEYcurj.

    1. Ler o workflow completo via mcp__n8n__get_workflow (ou equivalente) com ID bzK9KbNa5zEYcurj.

    2. Identificar TODOS os nodes que referenciam record_id:
       - Node "Webhook": como record_id e recebido (query param? body?)
       - Node "Get a record" (Airtable): como record_id e usado para buscar dados
       - Node "Dados do Formulario" (Set): se mapeia record_id para campos downstream
       - Node "buscar_dados_lead" (Airtable): se usa record_id
       - Node "Update record" (Airtable): se usa record_id

    3. Mapear quais campos downstream dependem dos dados buscados via record_id.
       O objetivo e entender o impacto minimo: quais nodes precisam mudar para que
       a entrada seja phone+tenant_id+imovel_id SEM alterar a logica do fluxo.

    IMPORTANTE: Este task e apenas leitura e analise. Nao alterar o workflow ainda.
  </action>
  <verify>Lista de nodes que usam record_id esta documentada e impacto mapeado.</verify>
  <done>Todos os nodes que referenciam record_id identificados. Plano de alteracao minima claro.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar webhook e nodes de entrada para usar phone, tenant_id, imovel_id</name>
  <files>nenhum — workflow n8n via MCP</files>
  <action>
    Usar ferramenta MCP n8n para atualizar o workflow bzK9KbNa5zEYcurj.

    Alteracoes MINIMAS necessarias (NAO alterar logica do fluxo):

    1. **Node Webhook**: Garantir que aceita body JSON com:
       ```json
       { "phone": "5511999999999", "tenant_id": "jurema-brokers", "imovel_id": "uuid-do-imovel" }
       ```
       Remover qualquer referencia a query.record_id.

    2. **Node "Get a record" (Airtable)**: Este node busca contrato por record_id.
       - OPCAO A (preferida): Se este node so busca dados que ja estarao disponiveis via
         phone+tenant_id+imovel_id nos nodes downstream, REMOVER ou DESABILITAR o node
         e ajustar o node "Code in JavaScript" ou "Dados do Formulario" para passar
         phone, tenant_id e imovel_id adiante.
       - OPCAO B: Se a busca Airtable e essencial para o fluxo (dados que nao vem de outro
         lugar), manter o node mas trocar a busca de record_id para usar outro campo disponivel,
         ou adicionar um Set node que injeta os novos campos.

    3. **Node "Dados do Formulario" (Set)**: Atualizar para mapear phone, tenant_id e imovel_id
       dos dados do webhook em vez de record_id do Airtable. Manter todos os outros campos
       que ja existem (id_contrato, endereco, responsavel_followup, notas_gestor, tipo_contrato_raw).

    4. **Node "buscar_dados_lead" (Airtable)**: Se busca por record_id, avaliar se precisa
       de ajuste ou se ja usa outro identificador.

    5. **Nodes downstream**: NAO ALTERAR. A logica de geracao (AI Agent), Google Docs,
       envio (WhatsApp, Email) e persistencia (Update record) devem permanecer intactos.

    REGRAS:
    - Usar mcp__n8n__update_workflow (ou equivalente) para salvar as alteracoes
    - NAO ativar o workflow (manter inactive)
    - NAO alterar logica de geracao, envio ou persistencia
    - Apenas a ENTRADA do workflow muda: de record_id para phone+tenant_id+imovel_id
  </action>
  <verify>
    Usar mcp__n8n__get_workflow para re-ler o workflow e confirmar:
    1. Webhook nao referencia record_id
    2. Campos phone, tenant_id, imovel_id estao disponiveis nos nodes de entrada
    3. Nodes downstream (AI Agent, Google Docs, envio) permanecem inalterados
  </verify>
  <done>
    - Webhook aceita { phone, tenant_id, imovel_id } no body
    - Nenhuma referencia a record_id na entrada do workflow
    - Logica interna do fluxo (geracao, envio, persistencia) inalterada
    - Workflow salvo mas permanece inativo
  </done>
</task>

</tasks>

<verification>
- Workflow bzK9KbNa5zEYcurj nao usa record_id na entrada
- Webhook espera body com phone, tenant_id, imovel_id
- Todos os nodes downstream da entrada funcionam sem record_id
- Workflow permanece inativo (active: false)
</verification>

<success_criteria>
- record_id removido da entrada do workflow
- Entrada padronizada: phone, tenant_id, imovel_id
- Logica do fluxo intacta (zero alteracoes em geracao/envio/persistencia)
- Workflow salvo no n8n (inativo)
</success_criteria>

<output>
After completion, create `.planning/quick/260409-e9u-remover-record-id-e-padronizar-entrada-d/260409-e9u-SUMMARY.md`
</output>
