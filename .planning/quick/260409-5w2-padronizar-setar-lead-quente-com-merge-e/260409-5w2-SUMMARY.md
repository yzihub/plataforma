---
phase: quick
plan: 260409-5w2
subsystem: n8n/setar_lead_quente
tags: [n8n, leads, luana, jurema-brokers, supabase, migration, metadata-merge]
dependency_graph:
  requires: []
  provides: [setar_lead_quente migrado para Supabase com GET+PATCH+merge de metadata]
  affects: [agente Luana, tool setar_lead_quente]
key_files:
  modified:
    - .planning/quick/260408-sub-refactor-luana-airtable-supabase/setar_lead_quente.json
decisions:
  - status='lead_quente' (snake_case limpo, sem emoji)
  - score=3 (numérico, não string)
  - GET antes do PATCH — obrigatório para merge de metadata
  - Fallback tenant_id hardcoded (aaaaaaaa-0002-0002-0002-000000000002) se não vier no payload
  - Sem criação de lead — PATCH filtra por tenant_id+phone, zero rows atualizados se não encontrar
metrics:
  completed_date: "2026-04-09"
  tasks_completed: 2
---

# Quick 260409-5w2: Padronizar setar_lead_quente com merge e contexto — Summary

**One-liner:** Workflow `setar_lead_quente` migrado de Airtable para Supabase com arquitetura linear de 5 nodes: Trigger → Normalize → GET Lead → Build Metadata (merge) → PATCH Lead.

## Tasks Completed

| # | Task | Resultado |
|---|------|-----------|
| 1 | Atualizar workflow n8n `QZ3VcIrxE6BRtCpj` via MCP | 4 nodes Airtable substituídos por 5 nodes Supabase (success: true, nodeCount: 5) |
| 2 | Atualizar `setar_lead_quente.json` local | JSON sincronizado com arquitetura real (executeWorkflowTrigger) |

## Nova Arquitetura

```
Trigger → Normalize → Get Lead (HTTP GET) → Build Metadata → PATCH Lead (HTTP PATCH)
```

### Node 1 — `Dados recebidos da Luana` (Trigger)
- Inputs simplificados: `phone`, `tenant_id`, `localizacao_visita`
- (removido: airtable_record_id e campos redundantes)

### Node 2 — `Normalize` (Code)
- `phone = String(data.phone).replace(/\D/g, '')` — remove formatação
- `tenant_id` com fallback hardcoded para Jurema Brokers

### Node 3 — `Get Lead` (HTTP GET Supabase)
- `GET /rest/v1/leads?select=id,tenant_id,phone,name,status,score,metadata&tenant_id=eq.X&phone=eq.Y`
- Retorna lead existente (ou array vazio)

### Node 4 — `Build Metadata` (Code)
- Lê `$('Normalize').first().json` para phone/tenant_id/localizacao_visita
- `antigo = existingLead?.metadata || {}`
- `novoMetadata = { ...antigo, localizacao_visita }` (merge não-destrutivo)
- Campos fixos: `status: 'lead_quente'`, `score: 3`

### Node 5 — `PATCH Lead` (HTTP PATCH Supabase)
- `PATCH /rest/v1/leads?tenant_id=eq.X&phone=eq.Y`
- Body: `{ status, score, metadata }`
- Header: `Prefer: return=representation`

## Self-Check: PASSED

- [x] Workflow `QZ3VcIrxE6BRtCpj` atualizado via MCP (success: true, nodeCount: 5)
- [x] GET antes do PATCH — metadata do lead existente é lido antes de atualizar
- [x] Merge de metadata — spread `{ ...antigo, localizacao_visita }` preserva dados anteriores
- [x] status='lead_quente', score=3 (campos fixos)
- [x] localizacao_visita adicionado ao metadata se presente no payload
- [x] PATCH filtra por tenant_id + phone
- [x] Sem criação de novo lead — PATCH é update-only
- [x] setar_lead_quente.json local sincronizado
