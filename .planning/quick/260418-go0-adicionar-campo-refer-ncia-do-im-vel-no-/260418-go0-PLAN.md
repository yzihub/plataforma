---
phase: quick-260418-go0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/ImovelSearchSelect.tsx
  - src/components/yzihub/LeadDrawer.tsx
autonomous: true
requirements:
  - QUICK-260418-GO0
must_haves:
  truths:
    - "Campo 'Referência do imóvel' aparece no LeadDrawer em modo novo lead E em modo edição (seção Interesse Imobiliário)"
    - "Ao clicar/focar no campo, lista de imóveis reais do tenant aparece carregada via GET /api/imoveis"
    - "Usuário consegue digitar texto (ex: 'Barra', 'apto', titulo_comercial) e a lista filtra imóveis em tempo real"
    - "Ao selecionar um imóvel, o UUID (id) é persistido em form.imovel_ref e o label visível mostra título + bairro"
    - "Ao salvar o lead (POST/PATCH /api/leads), o campo imovel_ref enviado é o id_imovel (UUID), não texto livre"
    - "Seção 'Imóvel Associado' (read-only) mostra o título do imóvel quando imovel_ref está preenchido (não apenas o UUID)"
  artifacts:
    - path: "src/components/yzihub/ImovelSearchSelect.tsx"
      provides: "Combobox pesquisável com fetch real de /api/imoveis, filtro por texto, seleção que retorna imovel.id"
      min_lines: 80
    - path: "src/components/yzihub/LeadDrawer.tsx"
      provides: "LeadDrawer usando ImovelSearchSelect no lugar do <input> de texto livre"
      contains: "ImovelSearchSelect"
  key_links:
    - from: "src/components/yzihub/ImovelSearchSelect.tsx"
      to: "/api/imoveis"
      via: "fetch no useEffect (mount) para carregar lista inicial"
      pattern: "fetch.*api/imoveis"
    - from: "src/components/yzihub/LeadDrawer.tsx"
      to: "ImovelSearchSelect"
      via: "onSelect(id) -> set('imovel_ref', id)"
      pattern: "ImovelSearchSelect.*onSelect"
    - from: "LeadDrawer FormState"
      to: "POST/PATCH /api/leads payload"
      via: "form.imovel_ref enviado direto no body (já existe; só muda tipo de conteúdo: UUID ao invés de texto)"
      pattern: "imovel_ref:\\s*form\\.imovel_ref"
---

<objective>
Substituir o campo de texto livre "Referência do imóvel" no LeadDrawer por um combobox pesquisável que carrega imóveis reais do banco (via GET /api/imoveis) e persiste o id_imovel (UUID) em lead.imovel_ref.

Purpose: hoje o campo é texto livre — corretor digita "Apto Manaíra" e não há vínculo real com a tabela imoveis. Isso quebra a integração com n8n (consultar_imoveis, gerar_contrato) que precisa do UUID para join. Também impede UX como "Ver imóvel →" funcionar corretamente.

Output:
- Novo componente reutilizável src/components/yzihub/ImovelSearchSelect.tsx
- LeadDrawer atualizado nos dois pontos onde imovel_ref é editável (seção "Perfil Imobiliário" modo novo lead E seção "Interesse Imobiliário" modo edição)
- Seção "Imóvel Associado" passa a mostrar título legível (não UUID)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/yzihub/LeadDrawer.tsx
@src/app/api/imoveis/route.ts
@src/lib/crm/types.ts
@src/types/n8n-payloads.ts

<interfaces>
<!-- Contratos relevantes extraídos do codebase. Executor usa direto, sem explorar. -->

Lead type (src/lib/crm/types.ts):
```typescript
export interface Lead {
  // ...
  imovel_ref?: string | null  // referência do imóvel (id_imovel) — UUID após este plano
}
```

N8nImovel (src/types/n8n-payloads.ts) — shape do item que /api/imoveis retorna em envelope.data:
```typescript
export interface N8nImovel {
  id: string;               // UUID — vira imovel_ref
  tenant_id: string;
  titulo_comercial: string; // label primário
  bairro: string | null;    // sub-label
  valor: number;            // exibir formatado opcional
  quartos: number;
  suites: number;
  vagas: number;
  metragem: number | null;
  tipo_de_imovel: string | null;  // "Apartamento", "Casa" etc.
  status_publicacao: string;
  // ... outros campos não necessários para o select
}
```

Resposta GET /api/imoveis (envelope n8n):
```typescript
{
  entity: "imoveis",
  tenant_id: string,
  count: number,
  fetched_at: string,
  data: N8nImovel[]  // já filtrado por tenant + status_publicacao="Publicado"
}
```

LeadDrawer FormState (src/components/yzihub/LeadDrawer.tsx:152-171):
```typescript
type FormState = {
  // ...
  imovel_ref: string;  // hoje texto livre; passa a ser UUID
}
```

Uso atual no LeadDrawer:
- Linha 648: <input type="text"> dentro da seção "Perfil Imobiliário" (modo novo lead) — SUBSTITUIR por ImovelSearchSelect
- Linha 537-568: seção "Interesse Imobiliário" (modo edição) NÃO tem campo de imovel_ref — ADICIONAR ImovelSearchSelect lá
- Linha 482-516: seção "Imóvel Associado" (read-only) exibe form.imovel_ref como texto — AJUSTAR para mostrar título do imóvel selecionado (ou UUID curto como fallback)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente ImovelSearchSelect (combobox pesquisável com fetch real)</name>
  <files>src/components/yzihub/ImovelSearchSelect.tsx</files>
  <action>
    Criar novo componente cliente `'use client'` em src/components/yzihub/ImovelSearchSelect.tsx.

    Props:
    ```typescript
    interface ImovelSearchSelectProps {
      value: string;                          // imovel_ref atual (UUID ou "")
      onChange: (imovelId: string, imovel: N8nImovel | null) => void;
      placeholder?: string;                   // default: "Buscar imóvel por título ou bairro..."
      className?: string;                     // para herdar INPUT_CLS do LeadDrawer
    }
    ```

    Comportamento:
    1. No mount (useEffect sem deps): fetch("/api/imoveis") → parse envelope → setImoveis(data). Gerenciar loading/error states.
    2. Estado interno:
       - `imoveis: N8nImovel[]` — lista completa do tenant
       - `query: string` — texto digitado pelo usuário
       - `open: boolean` — dropdown aberto?
       - `loading`, `error`
    3. UI:
       - Input de texto (mesmas classes CSS do INPUT_CLS do LeadDrawer para consistência visual)
       - Quando `value` preenchido e `!open`: mostra "{titulo_comercial} · {bairro}" do imóvel selecionado + botão "x" para limpar
       - Quando focado (onFocus → setOpen(true)): mostra dropdown com lista filtrada
       - Filtro client-side (case-insensitive): `titulo_comercial`, `bairro`, `tipo_de_imovel` incluem `query`
       - Cada item do dropdown: título em bold, linha secundária com "{bairro} · {tipo_de_imovel} · {quartos}Q" + valor formatado R$
       - Ao clicar item: onChange(imovel.id, imovel), setQuery(""), setOpen(false)
       - Empty state no dropdown: "Nenhum imóvel encontrado" se lista filtrada vazia
       - Loading state: "Carregando imóveis..."
       - Error state: "Erro ao carregar imóveis" + botão "Tentar novamente"
    4. Fechar dropdown quando clicar fora (useRef + useEffect com listener mousedown em document).
    5. Se `value` vier preenchido mas o imóvel correspondente não estiver na lista (caso lead antigo com id órfão): mostrar fallback "Ref: {value.slice(0,8)}..." e permitir trocar.
    6. Suportar dark mode (classes dark:* no padrão TailAdmin). Usar absolute positioning para dropdown (max-h-60 overflow-y-auto, rounded-lg border shadow-lg z-20 bg-white dark:bg-gray-900).
    7. Importar tipos: `import type { N8nImovel, N8nEnvelope } from "@/types/n8n-payloads";`
    8. Formatar valor: `new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 }).format(valor)`.

    IMPORTANTE:
    - Não usar nenhuma lib externa (Combobox do @headlessui, downshift, etc.) — implementação custom com input + ul, consistente com padrão TailAdmin já usado no projeto.
    - Componente fica em src/components/yzihub/ conforme Regra de Ouro #3 do CLAUDE.md.
    - Usar dados reais da API, ZERO mock.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    Arquivo src/components/yzihub/ImovelSearchSelect.tsx existe, exporta default, compila sem erros de tipo. Componente renderiza input + dropdown condicional, faz fetch em /api/imoveis no mount, filtra lista client-side, chama onChange(id, imovel) ao selecionar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar ImovelSearchSelect no LeadDrawer (novo lead + edição + exibição)</name>
  <files>src/components/yzihub/LeadDrawer.tsx</files>
  <action>
    Três mudanças cirúrgicas em src/components/yzihub/LeadDrawer.tsx:

    **(1) Import no topo do arquivo (após imports existentes):**
    ```tsx
    import ImovelSearchSelect from "@/components/yzihub/ImovelSearchSelect";
    import type { N8nImovel } from "@/types/n8n-payloads";
    ```

    **(2) Adicionar estado para guardar o imóvel selecionado (para label/exibição):**
    No componente `TabDados`, adicionar ao lado do `useState<FormState>`:
    ```tsx
    const [selectedImovel, setSelectedImovel] = useState<N8nImovel | null>(null);
    ```
    No `useEffect` que reseta o form quando lead muda, também resetar selectedImovel (null — será re-resolvido pelo próprio ImovelSearchSelect quando carregar a lista).

    **(3) Substituir o `<input type="text">` no bloco "Perfil Imobiliário" (modo novo lead, ~linha 647-649):**
    ANTES:
    ```tsx
    <div>
      <label className={LABEL_CLS}>Referência do imóvel</label>
      <input type="text" value={form.imovel_ref} onChange={(e) => set("imovel_ref", e.target.value)} className={INPUT_CLS} />
    </div>
    ```
    DEPOIS:
    ```tsx
    <div className="col-span-2">
      <label className={LABEL_CLS}>Referência do imóvel</label>
      <ImovelSearchSelect
        value={form.imovel_ref}
        onChange={(id, imovel) => { set("imovel_ref", id); setSelectedImovel(imovel); }}
      />
    </div>
    ```

    **(4) Adicionar o mesmo campo na seção "Interesse Imobiliário" (modo edição, ~linha 537-568), dentro do grid:**
    Após o campo "Faixa de valor" (linha ~565), adicionar:
    ```tsx
    <div className="col-span-2">
      <label className={LABEL_CLS}>Referência do imóvel</label>
      <ImovelSearchSelect
        value={form.imovel_ref}
        onChange={(id, imovel) => { set("imovel_ref", id); setSelectedImovel(imovel); }}
      />
    </div>
    ```

    **(5) Ajustar exibição na seção "Imóvel Associado" (~linha 482-497):**
    ANTES:
    ```tsx
    {form.imovel_ref && (
      <div className="flex items-center justify-between">
        <div>
          <p className="text-xs text-gray-400 uppercase tracking-wider">Referência</p>
          <p className="text-sm font-medium text-gray-800 dark:text-white/90">{form.imovel_ref}</p>
        </div>
        <a href={`/cockpit/imoveis?ref=${encodeURIComponent(form.imovel_ref)}`} ...>Ver imóvel →</a>
      </div>
    )}
    ```
    DEPOIS:
    ```tsx
    {form.imovel_ref && (
      <div className="flex items-center justify-between">
        <div>
          <p className="text-xs text-gray-400 uppercase tracking-wider">Imóvel</p>
          <p className="text-sm font-medium text-gray-800 dark:text-white/90">
            {selectedImovel?.titulo_comercial ?? `Ref: ${form.imovel_ref.slice(0, 8)}...`}
          </p>
          {selectedImovel?.bairro && (
            <p className="text-[11px] text-gray-400">{selectedImovel.bairro}</p>
          )}
        </div>
        <a href={`/cockpit/imoveis?ref=${encodeURIComponent(form.imovel_ref)}`}
           className="text-xs text-brand-500 hover:underline whitespace-nowrap">
          Ver imóvel →
        </a>
      </div>
    )}
    ```

    **(6) Ajustar o prop `propertyTitle` do GerarContratoDrawer (linha 1156):**
    ANTES: `propertyTitle={lead?.imovel_ref ?? null}`
    DEPOIS: passar o título se disponível. Como selectedImovel está dentro de TabDados e não acessível aqui, a solução pragmática é manter `lead?.imovel_ref ?? null` por enquanto (GerarContratoDrawer já tem fallback interno). Deixar TODO comment: `// TODO: resolver titulo do imovel via cache ou contexto de imoveis`.

    **(7) Remover o botão quebrado "Associar imóvel" na seção "Imóvel Associado" (~linha 518-528)** — era um TODO morto que agora perde sentido porque o campo editável na aba "Interesse Imobiliário" cobre esse fluxo. Substituir o bloco do empty state por uma mensagem mais clara:
    ```tsx
    <div className="rounded-xl border border-dashed border-gray-200 dark:border-gray-700 p-4 flex flex-col items-center gap-1 text-center">
      <span className="text-2xl">🏠</span>
      <p className="text-sm text-gray-400">Nenhum imóvel associado</p>
      <p className="text-[11px] text-gray-400">Use o campo "Referência do imóvel" abaixo para associar</p>
    </div>
    ```

    Zero mudança no fluxo de save: `form.imovel_ref` continua sendo enviado no payload de POST/PATCH exatamente como antes — o que muda é que o conteúdo agora é um UUID válido da tabela imoveis ao invés de texto livre.
  </action>
  <verify>
    <automated>rtk tsc --noEmit && rtk next build</automated>
  </verify>
  <done>
    LeadDrawer.tsx compila sem erros. Next build completa. Em /cockpit/leads, abrir drawer em modo "Novo Lead" mostra ImovelSearchSelect na seção "Perfil Imobiliário". Abrir lead existente mostra ImovelSearchSelect na seção "Interesse Imobiliário". Digitar filtra lista real de imóveis. Selecionar um imóvel salva o UUID em form.imovel_ref e exibe título+bairro na seção "Imóvel Associado". POST/PATCH /api/leads persiste o UUID no banco (campo imovel_ref da tabela leads).
  </done>
</task>

</tasks>

<verification>
Após ambas as tasks:
1. `rtk tsc --noEmit` — zero erros de tipo
2. `rtk next build` — build completa sem erros
3. Teste manual:
   - Abrir /cockpit/leads
   - Clicar "Novo Lead" → dropdown "Referência do imóvel" aparece com imóveis reais do Jurema
   - Digitar "manai" (ou bairro real) → lista filtra
   - Selecionar imóvel → label visível é título+bairro, não UUID
   - Salvar → lead aparece na lista
   - Reabrir o lead → aba Dados → seção "Interesse Imobiliário" mostra imóvel selecionado
   - Seção "Imóvel Associado" mostra título do imóvel (não UUID)
   - DB check via Supabase: `select imovel_ref from leads where id = '<novo>';` retorna UUID (36 chars), não texto livre
</verification>

<success_criteria>
- ImovelSearchSelect renderiza combobox pesquisável com dados reais de /api/imoveis
- Filtro client-side por titulo_comercial/bairro/tipo_de_imovel funciona
- Seleção persiste id_imovel (UUID) em lead.imovel_ref no banco via POST/PATCH /api/leads
- Campo aparece em ambos os modos (novo lead + edição)
- Seção "Imóvel Associado" mostra título legível, não UUID
- tsc e next build passam sem erros
- Zero uso de mock — toda a lista vem de GET /api/imoveis (tenant-scoped via DEV_BYPASS ou auth)
</success_criteria>

<output>
Após completar, criar .planning/quick/260418-go0-adicionar-campo-refer-ncia-do-im-vel-no-/260418-go0-SUMMARY.md com:
- Arquivos criados/modificados
- Decisões-chave (ex: por que client-side filter e não server-side; por que inline custom component vs lib)
- Como testar manualmente
- Follow-ups opcionais (ex: cache global de imóveis via Context, server-side filter se lista >200 imóveis)
</output>
