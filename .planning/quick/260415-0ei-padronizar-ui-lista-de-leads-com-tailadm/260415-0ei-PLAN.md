---
phase: quick-260415-0ei
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/yzihub/LeadsClient.tsx
  - src/components/yzihub/LeadsDataTable.tsx
autonomous: false
requirements:
  - UI-LEADS-LAYOUT-02
must_haves:
  truths:
    - "Lista de leads (/cockpit/leads) nao tem barras/bordas duplicadas entre header e body"
    - "Tabela nao gera scrollbar horizontal indesejada em telas >=1280px (xl)"
    - "Toolbar (busca + filtros + view toggle + Novo Lead) fica em UMA unica fileira bem distribuida (igual CorretoresClient/PropertyTable), nao em duas linhas soltas"
    - "Coluna de acao tem header 'Acoes' (nao vazio) e largura consistente com o conteudo 'Ver detalhes'"
    - "Alinhamento vertical das celulas e identico entre colunas (sem saltos quando badges coexistem com texto simples)"
    - "Layout responsivo: em < sm empilha, em sm-md a toolbar quebra elegantemente sem comprimir o botao Novo Lead"
  artifacts:
    - path: "src/components/yzihub/LeadsClient.tsx"
      provides: "Toolbar unificada (busca + filtros + toggle view + CTA) em uma fileira"
      contains: "flex-wrap"
    - path: "src/components/yzihub/LeadsDataTable.tsx"
      provides: "Tabela com bordas limpas, largura coerente, header 'Acoes' e colgroup"
      contains: "table-fixed"
  key_links:
    - from: "src/components/yzihub/LeadsClient.tsx"
      to: "src/components/yzihub/LeadsDataTable.tsx"
      via: "prop leads + onSelect (nao muda)"
      pattern: "LeadsDataTable leads=.* onSelect="
---

<objective>
Segunda passagem de padronizacao visual da lista de leads (/cockpit/leads). O plano anterior (260414-wph) corrigiu tokens (rounded-xl, py-2.5, brand-500), mas o usuario reporta que ainda ha problemas visiveis: "botoes, barras, espacamento e alinhamento fora do padrao". Este plano ataca causas estruturais remanescentes:

1. **Barras duplicadas** no topo da tabela: `TableHeader` tem `border-b border-gray-100` E a `TableRow` interna tem `bg-gray-50 dark:bg-gray-800/40` com `divide-y` depois — isso gera 2-3 linhas horizontais visiveis.
2. **Scrollbar horizontal**: o componente `<Table>` usa `min-w-full` (nao `w-full`) e ha 9 colunas — em telas normais vira scroll horizontal.
3. **Header de acao vazio** (`""` na lista de colunas) — alem de warning de key duplicada, deixa visual sem label.
4. **Toolbar em duas fileiras soltas**: header + CTA em cima, busca/filtros embaixo. Padrao TailAdmin dos outros clients e 1 fileira unica.
5. **Falta de colgroup/widths**: colunas de badges (Status, Score) coexistem com texto (Bairro, Origem) e ficam com larguras desproporcionais dependendo do conteudo.

Purpose: Resolver o que sobrou da iteracao anterior, com foco em *visual estrutural* (nao em tokens). Sem tocar em logica, types, filtros ou handlers.

Output: `LeadsClient.tsx` e `LeadsDataTable.tsx` com layout limpo e consistente com `CorretoresClient.tsx` e `PropertyTable.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Arquivos a modificar
@src/components/yzihub/LeadsClient.tsx
@src/components/yzihub/LeadsDataTable.tsx

# Referencias de padrao TailAdmin que devem ser matched
@src/components/yzihub/CorretoresClient.tsx
@src/components/ui/table/index.tsx

# Historico do problema
@.planning/quick/260414-wph-corrigir-layout-da-lista-de-leads-padron/260414-wph-PLAN.md

<interfaces>
<!-- NAO MUDAR estes contratos -->

De src/components/ui/table/index.tsx:
```typescript
// Table usa min-w-full (nao w-full); TableHeader e thead, TableBody e tbody
// TableRow e tr; TableCell e th (isHeader) ou td
// Nao tem larguras; responsabilidade do consumidor passar className.
```

De src/lib/crm/types.ts:
```typescript
type Lead = { id; name; phone; status; score; assigned_to; source; value; stage_id; last_action_at; ... }
```

Props:
- LeadsClient: `{ initialLeads: Lead[]; stages: PipelineStage[] }`
- LeadsDataTable: `{ leads: Lead[]; onSelect?: (lead: Lead) => void }`
</interfaces>

<design_targets>
**Toolbar unificada (uma fileira):**
```
[ Titulo + subtitulo ] ........................ [ toggle ] [ Novo Lead ]
[ Busca (flex-1) ] [ select status ] [ select origem ]        ← segunda linha de filtros APENAS quando view=table
```
Em mobile (< sm): tudo empilha naturalmente.

**Tabela:**
- Container: `rounded-2xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-white/[0.03] overflow-hidden`
- `<Table className="w-full">` (forcar w-full pra anular o min-w-full default)
- UM unico divisor entre header e body: remover `border-b` da TableHeader, manter apenas o `bg-gray-50 dark:bg-gray-800/40` no TableRow do header
- Header cells: `py-3 px-5 text-xs font-semibold uppercase tracking-wide text-gray-400 dark:text-gray-500 text-left`
- Body cells: `py-3.5 px-5 align-middle text-sm`
- Row hover: `hover:bg-gray-50/80 dark:hover:bg-white/[0.02] transition-colors`
- Divisores entre linhas: `border-b border-gray-50 dark:border-gray-800/60 last:border-0` (remover `divide-y` que colide com border-b)
- Ultima coluna header: texto "Acoes" (nao string vazia)
- `<colgroup>` com larguras enxutas para evitar overflow (ver Task 2)
</design_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unificar toolbar em LeadsClient.tsx (header + filtros em layout coeso)</name>
  <files>src/components/yzihub/LeadsClient.tsx</files>
  <action>
    Objetivo: colocar busca/filtros no mesmo "card" visual do header, em uma estrutura coesa ao inves de duas fileiras soltas. Sem mudar state, handlers, filtros, onSelect, ou integracao com LeadsKanban/LeadDrawer.

    Mudancas:

    1. **Manter o container externo** `<div className="space-y-5">`.

    2. **Header row** (sem mudar): titulo + subtitulo a esquerda, grupo (toggle + Novo Lead) a direita.
       - Garantir que o botao "Novo Lead" tem `shrink-0` (ja tem — manter).
       - No grupo da direita trocar `gap-3` por `gap-2` — mais compacto, alinhado com CorretoresClient.

    3. **Toolbar de filtros** (aparece apenas em view=table):
       - Envolver o `<SearchBar />` num wrapper visual alinhado: nada a fazer no SearchBar em si, mas atualizar o wrapper interno do SearchBar:
         - Trocar `flex flex-col gap-3 sm:flex-row sm:items-center` por `flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-center`
         - Adicionar `flex-wrap` permite que em larguras medias os selects quebrem pra baixo sem esmagar o input.
       - Input de busca: manter `flex-1`, mas adicionar `min-w-[240px]` (evita ficar apertado ao quebrar).
       - Selects: manter `min-w-[160px]` (ja existe).

    4. **Remover qualquer padding/margin residual** no container raiz do JSX do componente que possa estar competindo com o `<div className="p-4 mx-auto max-w-(--breakpoint-2xl) md:p-6">` do `cockpit/layout.tsx`. O fragment + `<div className="space-y-5">` ja esta correto — apenas conferir que nao existe `mt-*` ou `p-*` extra em elementos filhos do topo.

    5. **Icones TableViewIcon / KanbanViewIcon** — manter `size-4`, sem mudancas.

    6. **IMPORTANTE**: nao alterar o botao Novo Lead ate que exista handler real. Manter sem `onClick` (comportamento atual), mas adicionar `type="button"` pra nao virar submit acidental.

    NAO MUDAR: types, imports, lista ALL_STATUS / ALL_SOURCES, state, useMemo de filteredLeads, integracao com LeadsKanban e LeadDrawer, Suspense externo.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - tsc --noEmit passa sem erros novos
    - Header + toolbar renderizam em layout coeso; em sm+ fica 1 linha de header + 1 linha de filtros; em md esmagado, selects quebram por causa do flex-wrap; em mobile tudo empilha
    - Botao Novo Lead tem type="button" e shrink-0
    - Zero mudanca de logica (grep por "useState|useMemo|handleMove|onSelect" mostra blocos inalterados)
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpar barras/bordas e fixar larguras em LeadsDataTable.tsx</name>
  <files>src/components/yzihub/LeadsDataTable.tsx</files>
  <action>
    Objetivo: eliminar barras duplicadas e scrollbar horizontal indesejada. Sem tocar em badges, formatacao, avatar, ou tipos.

    Mudancas cirurgicas:

    1. **Table tag** (linha atual: `<Table>`):
       - Trocar por `<Table className="w-full">` — o componente default tem `min-w-full`; adicionar `w-full` garante que em telas largas a tabela use 100% e nao estoure. O tailwind vai resolver ambos (w-full tem precedencia pratica).

    2. **TableHeader** (linha atual: `<TableHeader className="border-b border-gray-100 dark:border-gray-800">`):
       - REMOVER o `border-b border-gray-100 dark:border-gray-800` — essa e a "barra duplicada". Deixar `<TableHeader>` sem className.
       - A separacao visual vem do `bg-gray-50 dark:bg-gray-800/40` do TableRow interno, que basta.

    3. **Coluna "Acoes"**:
       - No array de headers atual:
         `["Nome", "WhatsApp", "Status", "Score Luana", "Bairro Interesse", "Corretor", "Origem", "Valor Imovel", ""]`
       - Trocar o ultimo `""` por `"Acoes"`.
       - Remover a logica condicional `${h === "" ? " w-[120px]" : ""}` dentro do className do header cell — larguras virao do colgroup (item 4).

    4. **Adicionar `<colgroup>`** imediatamente antes de `<TableHeader>` dentro do `<Table>`:
       ```jsx
       <colgroup>
         <col className="w-[18%]" />  {/* Nome */}
         <col className="w-[12%]" />  {/* WhatsApp */}
         <col className="w-[11%]" />  {/* Status */}
         <col className="w-[9%]"  />  {/* Score */}
         <col className="w-[10%]" />  {/* Bairro */}
         <col className="w-[10%]" />  {/* Corretor */}
         <col className="w-[9%]"  />  {/* Origem */}
         <col className="w-[11%]" />  {/* Valor */}
         <col className="w-[10%]" />  {/* Acoes */}
       </colgroup>
       ```
       Total = 100%. Isso impede que colunas com texto longo empurrem a tabela alem da tela.

    5. **TableBody** (linha atual: `<TableBody className="divide-y divide-gray-50 dark:divide-gray-800">`):
       - REMOVER a classe `divide-y divide-gray-50 dark:divide-gray-800`. Passar `className=""` ou sem className.
       - O divisor sera aplicado linha-a-linha via `border-b ... last:border-0` (item 6) — padrao usado em CorretoresClient, evita colisao com border de linha hover/ativa.

    6. **Cada `<tr>` de dados** (dentro do map):
       - className atual: `"cursor-pointer align-middle hover:bg-gray-50/80 dark:hover:bg-white/[0.02] transition-colors"`
       - Novo: `"cursor-pointer align-middle border-b border-gray-50 dark:border-gray-800/60 last:border-0 hover:bg-gray-50/80 dark:hover:bg-white/[0.02] transition-colors"`

    7. **Empty-state row** (`<tr><td colSpan={9}>...</td></tr>`):
       - Adicionar `className="border-b-0"` na `<tr>` vazia (garantir que sem dados nao aparece borda fantasma).
       - Aumentar icone pra `size-14` e espacamento pra `py-16` (nao `py-20` — evita card gigante com 0 leads).

    8. **Celulas com texto longo** (Nome, Origem, Corretor) — adicionar `truncate` para cortar graciosamente se o conteudo exceder a col width:
       - `<td className="py-3.5 px-5">` de Nome: manter (Avatar+span ja controlam)
       - `<td className="py-3.5 px-5 text-sm text-gray-500 dark:text-gray-400">` de Origem: adicionar `truncate`
       - `<td className="py-3.5 px-5 text-xs font-mono text-gray-500">` de Corretor: adicionar `truncate`
       - Celula de WhatsApp, Valor, Bairro ja sao curtos — nao precisa.

    NAO MUDAR: imports, STATUS_BADGE, AVATAR_COLORS, BAIRROS, getInitials, avatarColor, formatCurrency, formatPhone, scoreBadge, bairroFromId, formatCorretor, LeadAvatar, LeadsDataTableProps interface, onSelect.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - tsc --noEmit passa sem erros novos
    - Apenas UMA barra horizontal visivel entre header e primeira linha (o bg-gray-50 do header)
    - Ultima coluna tem header "Acoes"
    - colgroup presente com 9 cols totalizando 100%
    - Em telas >=1280px a tabela nao gera scroll horizontal
    - Divisores entre linhas sao gray-50/gray-800-60, sem duplicacao com divide-y
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validacao visual em /cockpit/leads (dark mode)</name>
  <what-built>
    Toolbar de leads unificada e tabela limpa sem barras duplicadas, sem scrollbar horizontal em telas normais, com coluna Acoes corretamente rotulada.
  </what-built>
  <how-to-verify>
    1. Garantir servidor Next rodando (`rtk pnpm dev` se necessario).
    2. Abrir http://localhost:3000/cockpit/leads em dark mode.
    3. Conferir (view=table):
       - Topo: titulo "Leads" + contador, e a direita: view toggle + "Novo Lead" (tudo na MESMA linha em telas >=sm).
       - Abaixo: barra de busca + 2 selects em uma unica linha; em telas medias os selects podem quebrar (flex-wrap) — OK.
       - Tabela: UMA linha divisoria clara entre header (fundo cinza claro) e primeira row. NAO deve aparecer linha dupla.
       - Ultima coluna do header mostra "Acoes" (nao vazio).
       - Em telas 1280px+ nao aparece scrollbar horizontal no card da tabela.
       - Linhas tem hover suave e divisor claro entre si.
       - Celulas alinhadas verticalmente (badges de Status/Score no mesmo eixo de texto).
    4. Alternar para view=kanban — barra de busca desaparece (comportamento atual preservado).
    5. Clicar em uma linha — LeadDrawer abre (regressao check).
    6. Resize para 375px mobile — toolbar empilha em coluna, sem overflow horizontal.
    7. Comparar visualmente com /cockpit/corretores e /cockpit/imoveis — devem ter MESMA linguagem visual (raios, border, hover, divisores simples).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva exatamente o que ainda aparece quebrado (prints/pixel hunt bem-vindos).</resume-signal>
</task>

</tasks>

<verification>
- rtk tsc --noEmit sem erros novos
- Nenhuma mudanca de logica, types, filtros ou props
- Zero scrollbar horizontal na tabela em telas normais
- Zero barras horizontais duplicadas entre header e body
- Coluna "Acoes" nomeada
- Toolbar coesa em uma estrutura visual
</verification>

<success_criteria>
- Layout visual coeso com CorretoresClient e PropertyTable
- Eliminacao estrutural de scrollbar/bordas/acao-sem-label (nao apenas ajuste de tokens)
- Responsivo: mobile empilha, tablet flex-wrap elegante, desktop 1 linha
- Usuario aprova visualmente na checkpoint
</success_criteria>

<output>
Apos completar, criar `.planning/quick/260415-0ei-padronizar-ui-lista-de-leads-com-tailadm/260415-0ei-SUMMARY.md`
</output>
