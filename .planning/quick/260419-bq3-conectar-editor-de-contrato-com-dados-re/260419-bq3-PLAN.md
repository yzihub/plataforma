---
phase: quick-260419-bq3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/imoveis/[id]/route.ts
  - src/components/yzihub/Contratos/ContratoEditor.tsx
autonomous: true
requirements:
  - BQ3-01
  - BQ3-02
  - BQ3-03
user_setup: []

must_haves:
  truths:
    - "Editor carrega o imóvel correto (por id) via endpoint dedicado, independente do status_publicacao"
    - "A variável {{valor}} do contrato é preenchida pelo preço do imóvel (property.valor), caindo para lead.value só se ausente"
    - "A variável {{id_imovel}} existe no painel de chips e é substituída no preview pelo UUID do imóvel"
    - "Salvar rascunho e Gerar contrato não ficam bloqueados quando o lead tem value=0 mas o imóvel tem valor > 0"
    - "Dados salvos no job_queue / contracts preservam leadId, propertyId e brokerId reais (nenhum fallback textual)"
  artifacts:
    - path: "src/app/api/imoveis/[id]/route.ts"
      provides: "GET /api/imoveis/:id — retorna 1 imóvel por UUID do tenant (sem filtro de status_publicacao)"
      exports: ["GET"]
    - path: "src/components/yzihub/Contratos/ContratoEditor.tsx"
      provides: "Editor de contrato com property fetch eficiente, valor prioritário do imóvel e variável id_imovel"
      contains: "fetch(`/api/imoveis/${propertyId}`)"
  key_links:
    - from: "src/components/yzihub/Contratos/ContratoEditor.tsx"
      to: "/api/imoveis/[id]"
      via: "fetch no useEffect de load"
      pattern: "fetch\\(`/api/imoveis/\\$\\{propertyId\\}`"
    - from: "ContratoEditor vars map"
      to: "property.valor (fallback lead.value)"
      via: "useMemo"
      pattern: "property\\?\\.valor\\s*\\?\\?\\s*lead\\?\\.value"
---

<objective>
Conectar o editor de contrato (`/cockpit/contratos/novo`) com dados reais do banco de forma correta e eficiente.

Purpose: Hoje o editor (a) busca TODOS os imóveis e filtra client-side por `status_publicacao = "Publicado"` (falha silenciosa para imóveis não publicados), (b) usa `lead.value` como fonte do preço (deal-value do CRM, frequentemente 0 — bloqueia Salvar/Gerar), e (c) não expõe `{{id_imovel}}` como variável no template. Vamos consertar os três problemas mantendo layout intacto e sem fallbacks textuais.

Output:
- Nova route `GET /api/imoveis/[id]` que retorna 1 imóvel por UUID dentro do tenant (bypass DEV + auth real), sem filtro de status.
- `ContratoEditor.tsx` ajustado: fetch pontual do imóvel, `valor` vem de `property.valor` (fallback `lead.value`), chip `{{id_imovel}}` disponível e resolvido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/components/yzihub/Contratos/ContratoEditor.tsx
@src/app/api/imoveis/route.ts
@src/app/api/leads/[id]/route.ts
@src/app/api/brokers/[id]/route.ts

<interfaces>
<!-- Contratos existentes que esta task precisa honrar -->

Tenant resolution pattern (from src/app/api/imoveis/route.ts):
```ts
const isDevBypass =
  process.env.NEXT_PUBLIC_DEV_BYPASS === "true" &&
  process.env.NODE_ENV !== "production";

let tenantId: string;
if (isDevBypass) {
  const param = request.nextUrl.searchParams.get("tenant_id");
  tenantId = param ?? "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361";
} else {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  // ... fetch profile.tenant_id
}

const supabase = isDevBypass ? createAdminClient() : await createClient();
```

Imoveis columns selected (from existing route):
```
id, tenant_id, titulo_comercial, bairro, valor, quartos, suites, vagas,
metragem, descricao_imovel, foto_principal, tipo_de_imovel, finalidade,
link_do_imovel, status_publicacao, created_at, updated_at
```

PropertyData interface expected by ContratoEditor:
```ts
interface PropertyData {
  id: string;
  titulo_comercial: string;
  bairro: string | null;
  valor: number;
}
```

Current vars shape (ContratoEditor):
```ts
const vars = {
  comprador, imovel, corretor, valor, comissao, data, valor_extenso
}
// FALTA: id_imovel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar GET /api/imoveis/[id] para fetch pontual por UUID</name>
  <files>src/app/api/imoveis/[id]/route.ts</files>
  <action>
    Criar nova route handler seguindo o MESMO padrão de tenant resolution de `src/app/api/imoveis/route.ts` (DEV_BYPASS + createAdminClient fallback para auth real + createClient). Diferenças críticas:

    1. Assinatura: `export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> })` — Next.js 15 usa params como Promise, precisa await.
    2. Extrair `id` via `const { id } = await params;` — validar que é UUID não-vazio, senão 400.
    3. Query: `.from("imoveis").select("id, tenant_id, titulo_comercial, bairro, valor, quartos, suites, vagas, metragem, descricao_imovel, foto_principal, tipo_de_imovel, finalidade, link_do_imovel, status_publicacao, created_at, updated_at").eq("tenant_id", tenantId).eq("id", id).maybeSingle()` — NÃO filtrar `status_publicacao` (contratos podem ser gerados para imóveis em rascunho/despublicados).
    4. Retorno: retornar o objeto direto (NÃO usar `buildN8nEnvelope` porque o consumidor é o ContratoEditor, não n8n). Formato: `NextResponse.json({ id, titulo_comercial, bairro, valor, ... }, { status: 200 })`.
    5. Se não encontrado, retornar `NextResponse.json({ error: "Imóvel não encontrado" }, { status: 404 })`.
    6. Tratar erros com `console.error` e 500 como na route lista.

    Por que não reutilizar buildN8nEnvelope: o frontend consome objeto plano (idêntico ao contrato do `GET /api/brokers/[id]` e `GET /api/leads/[id]`), manter consistência com as outras routes `[id]` do projeto.
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - Arquivo `src/app/api/imoveis/[id]/route.ts` existe
    - TypeScript compila sem erros
    - `curl http://localhost:3000/api/imoveis/<uuid-real>?tenant_id=82cc7aa9-fc6e-4f37-8d8e-8a71c1691361` retorna 200 com `{ id, titulo_comercial, valor, ... }` — independentemente de `status_publicacao`
    - UUID inexistente retorna 404
    - UUID inválido (string vazia ou malformada) retorna 400
  </done>
</task>

<task type="auto">
  <name>Task 2: Ajustar ContratoEditor — fetch pontual de imóvel, valor de property, chip id_imovel</name>
  <files>src/components/yzihub/Contratos/ContratoEditor.tsx</files>
  <action>
    Três mudanças cirúrgicas em `src/components/yzihub/Contratos/ContratoEditor.tsx`. NÃO alterar layout, JSX, classes Tailwind, nem nomes de handlers.

    **Mudança A — Fetch do imóvel usa /api/imoveis/[id]** (linhas ~123-135):

    Substituir o bloco que faz `fetch("/api/imoveis")` + `.find(p => p.id === propertyId)` por um fetch direto:

    ```tsx
    if (propertyId) {
      tasks.push(
        fetch(`/api/imoveis/${propertyId}`)
          .then((r) => r.ok ? r.json() : null)
          .then((d) => { if (!cancelled && d?.id) setProperty(d as PropertyData); })
          .catch(() => {})
      );
    }
    ```

    Motivo: eficiência (1 row vs N), correto para imóveis não publicados, consistente com fetch de lead/broker.

    **Mudança B — valor prioritário do imóvel** (linhas ~77-89 no useMemo `vars`, e linhas 195-196, 246-247 nos guards):

    No useMemo `vars`, trocar:
    ```ts
    const valor = lead?.value ?? 0;
    ```
    por:
    ```ts
    const valor = property?.valor ?? lead?.value ?? 0;
    ```

    No `handleSaveDraft`, substituir:
    ```ts
    const valor = lead?.value ?? 0;
    if (valor <= 0) { setError("O lead precisa ter um valor definido."); return; }
    ```
    por:
    ```ts
    const valor = property?.valor ?? lead?.value ?? 0;
    if (valor <= 0) { setError("O imóvel ou lead precisa ter um valor definido."); return; }
    ```

    Aplicar a mesma substituição em `handleGenerateAndSend`.

    Motivo: contratos imobiliários usam preço do imóvel; `lead.value` é campo de CRM que frequentemente é 0.

    **Mudança C — Variável {{id_imovel}}**:

    1. Em `AVAILABLE_VARS` (linhas 39-47), adicionar ao final do array:
       ```ts
       { key: "id_imovel",    hint: "UUID do imóvel" },
       ```
    2. No useMemo `vars` (linhas 78-88), adicionar ao objeto retornado:
       ```ts
       id_imovel: property?.id ?? "{{id_imovel}}",
       ```
       (manter a mesma convenção de fallback dos outros campos — a variável permanece visível no preview se não houver imóvel)

    Motivo: usuário quer poder colar `{{id_imovel}}` no template e ter o UUID substituído automaticamente.

    **NÃO fazer**:
    - Não remover nem renomear nenhuma variável existente
    - Não alterar o tamanho do grid, chips, header, ou qualquer classe Tailwind
    - Não alterar os payloads de `/api/contracts/draft` ou `/api/contracts/generate` (eles já enviam `property_id` / IDs corretos — o valor numérico continua sendo enviado, agora com a fonte certa)
  </action>
  <verify>
    <automated>rtk tsc --noEmit</automated>
  </verify>
  <done>
    - `fetch(`/api/imoveis/${propertyId}`)` presente no useEffect de load (sem `.find(...)`)
    - `property?.valor ?? lead?.value ?? 0` usado nos três lugares (useMemo vars, handleSaveDraft, handleGenerateAndSend)
    - `{ key: "id_imovel", ... }` em `AVAILABLE_VARS`
    - `id_imovel: property?.id ?? "{{id_imovel}}"` em `vars` useMemo
    - TypeScript compila sem erros
    - Abrir `/cockpit/contratos/novo?leadId=X&propertyId=Y&brokerId=Z` com lead.value=0 mas property.valor>0: sidebar mostra imóvel, chip `{{id_imovel}}` aparece, botão "Salvar Rascunho" não está bloqueado por erro de valor, preview substitui `{{valor}}` pelo preço do imóvel e `{{id_imovel}}` pelo UUID
  </done>
</task>

</tasks>

<verification>
Verificação manual end-to-end (dev rodando):
1. Navegar a um lead que tenha `imovel_ref` apontando para imóvel não-publicado → clicar "Gerar Contrato" no drawer → editor abre com dados do imóvel carregados na sidebar (anteriormente não carregava)
2. Lead sem `value` definido mas imóvel com preço → preview mostra `R$ X.XXX,XX` vindo do imóvel, botões "Salvar Rascunho" e "Gerar e Enviar" funcionais
3. Colar `{{id_imovel}}` no textarea → preview substitui pelo UUID do imóvel
4. Clicar "Salvar Rascunho" → `contracts` row criado no Supabase com `lead_id`, `project_id` (property), `broker_id` corretos
5. Clicar "Gerar e Enviar" → `job_queue` row enfileirado com os três IDs reais

Checks automatizados:
- `rtk tsc --noEmit` passa
- `rtk next build` não quebra rotas existentes
</verification>

<success_criteria>
- `GET /api/imoveis/:id` retorna qualquer imóvel do tenant por UUID (independente de status_publicacao)
- Editor de contrato não depende mais de listar todos os imóveis
- `{{valor}}` no preview reflete o preço do imóvel quando disponível
- `{{id_imovel}}` é variável reconhecida no painel de chips e no substituidor
- Nenhuma mudança visual no layout do editor (3 colunas 280px/1fr/360px, header, chips, textarea — idênticos)
- Nenhum fallback de texto digitado é aceito como substituto de ID real (lead_id, property_id, broker_id continuam obrigatórios)
- `rtk tsc --noEmit` passa sem erros
</success_criteria>

<output>
After completion, create `.planning/quick/260419-bq3-conectar-editor-de-contrato-com-dados-re/260419-bq3-SUMMARY.md`
</output>
