---
phase: quick-260429-b3x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.ps1
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/config/mcp-wordpress-config.example.json
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/.env.mcp.example
  - .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/VALIDATION.md
autonomous: false
requirements:
  - QUICK-MCP-WP-01
must_haves:
  truths:
    - "Plugin mcp-wp/mcp-server instalado e ativo no WordPress juremabksimoveis.com.br"
    - "Endpoint https://juremabksimoveis.com.br/wp-json/mcp/v1/ responde com 200 (autenticado) ou 401 (sem auth)"
    - "Application Password criada para o usuario MCP e armazenada de forma segura"
    - "Script curl testa o endpoint sem alterar conteudo (apenas leitura/discovery)"
    - "Arquivo de configuracao MCP exemplo permite conectar ao WordPress"
  artifacts:
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md"
      provides: "Passo-a-passo manual no WP Admin (instalar plugin, criar usuario, gerar Application Password)"
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh"
      provides: "Script curl para validar endpoint MCP sem mutacoes (bash)"
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.ps1"
      provides: "Script PowerShell equivalente para Windows"
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/config/mcp-wordpress-config.example.json"
      provides: "Configuracao MCP exemplo (usar com Claude Desktop / cliente MCP)"
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/.env.mcp.example"
      provides: "Variaveis de ambiente exemplo (WP_URL, WP_USER, WP_APP_PASSWORD)"
    - path: ".planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/VALIDATION.md"
      provides: "Checklist de validacao executado pelo usuario com resultados"
  key_links:
    - from: "scripts/test-mcp-endpoint.sh"
      to: "https://juremabksimoveis.com.br/wp-json/mcp/v1/"
      via: "curl com Basic Auth (user:application_password)"
      pattern: "curl.*wp-json/mcp/v1"
    - from: "config/mcp-wordpress-config.example.json"
      to: ".env.mcp"
      via: "variaveis de ambiente WP_URL/WP_USER/WP_APP_PASSWORD"
      pattern: "\\$\\{WP_(URL|USER|APP_PASSWORD)\\}"
---

<objective>
Instalar e validar o plugin mcp-wp/mcp-server no WordPress juremabksimoveis.com.br, criando um usuario dedicado MCP com Application Password e validando o endpoint REST `/wp-json/mcp/v1/` via scripts de teste read-only.

Purpose: Habilitar comunicacao MCP entre clientes (Claude Desktop / outros) e o WordPress da Jurema sem precisar de SSH/PHP/WP-CLI. O plano nao mexe no YZI OS, no Supabase, nem em conteudo de imoveis — apenas instala o plugin, valida o endpoint e documenta como conectar.

Output:
- Documentacao passo-a-passo de instalacao manual no WP Admin (`INSTALL-WP-ADMIN.md`)
- Scripts de validacao curl/PowerShell (read-only)
- Configuracao MCP exemplo + .env.mcp.example
- Checklist de validacao preenchido (`VALIDATION.md`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<task_metadata>
Site: https://juremabksimoveis.com.br
Stack WP: Crocoblock + JetEngine + JetSmartFilters
Acesso disponivel: WP Admin (sem SSH, sem WP-CLI, sem PHP local)
Plugin alvo: mcp-wp/mcp-server (https://github.com/Automattic/mcp-wp)
Endpoint REST esperado: /wp-json/mcp/v1/
Auth: WordPress Application Passwords (Basic Auth via header)

Restricoes (NAO violar):
- NAO mexer no YZI OS
- NAO mexer no Supabase
- NAO mexer em imoveis (tabela/CPT)
- NAO criar/editar/apagar/publicar conteudo
- Apenas instalar plugin + validar endpoint + documentar
</task_metadata>

<reference_links>
- Plugin oficial: https://github.com/Automattic/mcp-wp
- Application Passwords (WP core): https://make.wordpress.org/core/2020/11/05/application-passwords-integration-guide/
- MCP spec: https://modelcontextprotocol.io
</reference_links>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar guia de instalacao manual no WP Admin (INSTALL-WP-ADMIN.md)</name>
  <files>
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md
  </files>
  <action>
Criar `INSTALL-WP-ADMIN.md` com passos numerados e claros para o usuario executar manualmente no WP Admin de juremabksimoveis.com.br. Como nao temos SSH/WP-CLI, o documento e a unica forma de instalar.

Estrutura obrigatoria do documento:

## Pre-requisitos
- Acesso de administrador em https://juremabksimoveis.com.br/wp-admin
- Confirmar versao WordPress >= 6.4 (Application Passwords nativos)
- Confirmar PHP >= 8.0 (em "Ferramentas > Saude do site > Informacoes")

## Parte 1 — Instalar plugin mcp-wp/mcp-server
Documentar duas vias (em ordem de preferencia):

**Via A — Upload do .zip (recomendada se plugin nao estiver no diretorio oficial):**
1. Baixar o release mais recente do plugin do repositorio GitHub:
   `https://github.com/Automattic/mcp-wp/releases` (baixar o `.zip` do plugin)
2. WP Admin > Plugins > Adicionar novo > Enviar plugin > Escolher arquivo > Selecionar o .zip > Instalar agora
3. Apos instalar, clicar em "Ativar plugin"

**Via B — Diretorio oficial WordPress.org (caso ja esteja publicado):**
1. WP Admin > Plugins > Adicionar novo
2. Buscar "mcp-server" ou "mcp-wp"
3. Verificar autor: Automattic
4. Instalar e ativar

## Parte 2 — Criar usuario MCP dedicado
1. WP Admin > Usuarios > Adicionar novo
2. Nome de usuario: `mcp-bot`
3. Email: usar email tecnico da Jurema (NAO o email pessoal)
4. Funcao: `Editor` (minimo para CRUD de posts/CPTs; NAO usar Administrator)
5. Gerar senha forte (sera substituida pela Application Password)
6. Salvar

## Parte 3 — Gerar Application Password
1. WP Admin > Usuarios > Editar usuario `mcp-bot`
2. Rolar ate "Application Passwords"
3. Em "New Application Password Name", digitar: `mcp-server-claude`
4. Clicar "Add New Application Password"
5. **COPIAR E SALVAR EM LOCAL SEGURO** o token gerado (formato: `xxxx xxxx xxxx xxxx xxxx xxxx`)
6. Esse token NAO sera mostrado novamente
7. Salvar credenciais em `.env.mcp` local (ver Task 3)

## Parte 4 — Verificar endpoint REST ativo
1. Abrir no navegador (sem auth): `https://juremabksimoveis.com.br/wp-json/`
2. Procurar no JSON pela namespace `mcp/v1`
3. Se aparecer, o plugin registrou o endpoint corretamente
4. Caso nao apareça: ir em Configuracoes > Permalinks > Salvar (re-flush rewrite rules)

## Troubleshooting
- 404 em /wp-json/mcp/v1/: re-salvar permalinks
- 401 sem auth: comportamento esperado, endpoint protegido
- 403 com auth: verificar role do usuario (Editor minimo)
- Plugin desativa sozinho: checar PHP version, conflitos, error_log

## Checklist final (marcar antes de Task 2)
- [ ] Plugin instalado e ativo
- [ ] Usuario `mcp-bot` criado com role Editor
- [ ] Application Password gerada e salva localmente
- [ ] `https://juremabksimoveis.com.br/wp-json/` lista namespace `mcp/v1`

Tom: instrucional, passo-a-passo, em portugues brasileiro. Sem jargao desnecessario.
  </action>
  <verify>
    <automated>test -f .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md && grep -q "mcp-wp/mcp-server" .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md && grep -q "Application Password" .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md && grep -q "wp-json/mcp/v1" .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md</automated>
  </verify>
  <done>Arquivo INSTALL-WP-ADMIN.md existe com 4 partes (instalar plugin, criar usuario, Application Password, verificar endpoint), troubleshooting e checklist final. Tudo em portugues, sem invencao de passos (apenas o que esta documentado oficialmente para WordPress + plugin mcp-wp).</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts de validacao curl (.sh + .ps1) e config MCP exemplo</name>
  <files>
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.ps1
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/config/mcp-wordpress-config.example.json
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/.env.mcp.example
  </files>
  <action>
Criar 4 arquivos de validacao e configuracao. **NENHUM script deve fazer mutacao** — apenas GET / discovery / introspection.

### A. `scripts/test-mcp-endpoint.sh` (bash, Linux/Mac/Git Bash)

```bash
#!/usr/bin/env bash
# test-mcp-endpoint.sh — valida o endpoint MCP do WordPress sem alterar conteudo
# Uso: ./test-mcp-endpoint.sh
# Pre-requisito: copiar .env.mcp.example para .env.mcp e preencher

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ENV_FILE="${SCRIPT_DIR}/../.env.mcp"

if [[ ! -f "$ENV_FILE" ]]; then
  echo "ERRO: $ENV_FILE nao encontrado. Copie .env.mcp.example para .env.mcp e preencha." >&2
  exit 1
fi

# shellcheck disable=SC1090
source "$ENV_FILE"

: "${WP_URL:?WP_URL nao definida}"
: "${WP_USER:?WP_USER nao definida}"
: "${WP_APP_PASSWORD:?WP_APP_PASSWORD nao definida}"

# Remover espacos do Application Password (WP gera com espacos visuais)
WP_APP_PASSWORD_CLEAN="${WP_APP_PASSWORD// /}"

echo "=== 1. Verificar namespace mcp/v1 (sem auth, GET publico) ==="
curl -sS "${WP_URL}/wp-json/" | grep -o '"mcp/v1"' || {
  echo "FALHA: namespace mcp/v1 nao encontrado em /wp-json/" >&2
  exit 1
}
echo "OK: namespace mcp/v1 presente"
echo

echo "=== 2. GET /wp-json/mcp/v1/ (autenticado, discovery) ==="
HTTP_CODE=$(curl -sS -o /tmp/mcp-discovery.json -w "%{http_code}" \
  -u "${WP_USER}:${WP_APP_PASSWORD_CLEAN}" \
  "${WP_URL}/wp-json/mcp/v1/")
echo "HTTP $HTTP_CODE"
if [[ "$HTTP_CODE" == "200" ]]; then
  echo "Resposta (primeiros 500 chars):"
  head -c 500 /tmp/mcp-discovery.json
  echo
  echo "OK: endpoint MCP autenticado responde 200"
else
  echo "FALHA: esperado 200, recebido $HTTP_CODE" >&2
  cat /tmp/mcp-discovery.json
  exit 1
fi
echo

echo "=== 3. GET /wp-json/mcp/v1/ (SEM auth, deve retornar 401) ==="
HTTP_CODE_NOAUTH=$(curl -sS -o /dev/null -w "%{http_code}" "${WP_URL}/wp-json/mcp/v1/")
echo "HTTP $HTTP_CODE_NOAUTH (esperado 401 ou 403)"
if [[ "$HTTP_CODE_NOAUTH" == "401" || "$HTTP_CODE_NOAUTH" == "403" ]]; then
  echo "OK: endpoint protegido"
else
  echo "AVISO: endpoint nao exigiu auth (HTTP $HTTP_CODE_NOAUTH) — verificar configuracao" >&2
fi
echo

echo "=== Validacao concluida (somente leitura, nenhum conteudo alterado) ==="
```

Tornar executavel: o usuario rodara `chmod +x` apos clone (instrucao em comentario).

### B. `scripts/test-mcp-endpoint.ps1` (PowerShell, Windows)

Equivalente em PowerShell:

```powershell
# test-mcp-endpoint.ps1 — valida endpoint MCP do WordPress sem alterar conteudo
# Uso: ./test-mcp-endpoint.ps1
# Pre-requisito: copiar .env.mcp.example para .env.mcp e preencher

$ErrorActionPreference = "Stop"

$scriptDir = Split-Path -Parent $MyInvocation.MyCommand.Path
$envFile = Join-Path $scriptDir "..\.env.mcp"

if (-not (Test-Path $envFile)) {
  Write-Error "$envFile nao encontrado. Copie .env.mcp.example para .env.mcp e preencha."
  exit 1
}

# Carregar .env.mcp
Get-Content $envFile | Where-Object { $_ -match "^\s*([A-Z_]+)\s*=\s*(.+)$" } | ForEach-Object {
  $name = $matches[1]
  $value = $matches[2].Trim('"').Trim("'")
  Set-Variable -Name $name -Value $value -Scope Script
}

if (-not $WP_URL -or -not $WP_USER -or -not $WP_APP_PASSWORD) {
  Write-Error "WP_URL, WP_USER e WP_APP_PASSWORD sao obrigatorias em .env.mcp"
  exit 1
}

$cleanPwd = $WP_APP_PASSWORD -replace '\s', ''
$pair = "$WP_USER:$cleanPwd"
$bytes = [System.Text.Encoding]::UTF8.GetBytes($pair)
$base64 = [Convert]::ToBase64String($bytes)
$headers = @{ Authorization = "Basic $base64" }

Write-Host "=== 1. Verificar namespace mcp/v1 (sem auth) ==="
$rootJson = Invoke-RestMethod -Uri "$WP_URL/wp-json/" -Method Get
if ($rootJson.namespaces -contains "mcp/v1") {
  Write-Host "OK: namespace mcp/v1 presente" -ForegroundColor Green
} else {
  Write-Error "FALHA: namespace mcp/v1 nao encontrado"
  exit 1
}

Write-Host "`n=== 2. GET /wp-json/mcp/v1/ (autenticado) ==="
try {
  $resp = Invoke-WebRequest -Uri "$WP_URL/wp-json/mcp/v1/" -Method Get -Headers $headers
  Write-Host "HTTP $($resp.StatusCode)"
  Write-Host ($resp.Content.Substring(0, [Math]::Min(500, $resp.Content.Length)))
  Write-Host "OK: endpoint MCP autenticado respondeu 200" -ForegroundColor Green
} catch {
  Write-Error "FALHA: $($_.Exception.Message)"
  exit 1
}

Write-Host "`n=== 3. GET sem auth (esperado 401/403) ==="
try {
  $respNoAuth = Invoke-WebRequest -Uri "$WP_URL/wp-json/mcp/v1/" -Method Get -ErrorAction Stop
  Write-Warning "AVISO: endpoint nao exigiu auth (HTTP $($respNoAuth.StatusCode))"
} catch {
  $code = $_.Exception.Response.StatusCode.Value__
  if ($code -eq 401 -or $code -eq 403) {
    Write-Host "OK: endpoint protegido (HTTP $code)" -ForegroundColor Green
  } else {
    Write-Warning "Codigo inesperado: $code"
  }
}

Write-Host "`n=== Validacao concluida (somente leitura) ==="
```

### C. `config/mcp-wordpress-config.example.json`

Configuracao MCP exemplo para clientes (Claude Desktop, etc). Como o cliente exato pode variar, usar formato Claude Desktop como referencia (formato JSON-RPC stdio):

```json
{
  "_comment": "Exemplo de configuracao MCP para conectar Claude Desktop ao WordPress mcp-server.",
  "_comment2": "Substituir variaveis ${WP_*} pelos valores reais ou usar npx mcp-remote.",
  "_docs": "https://github.com/Automattic/mcp-wp",
  "mcpServers": {
    "wordpress-jurema": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "${WP_URL}/wp-json/mcp/v1/sse"
      ],
      "env": {
        "WP_URL": "https://juremabksimoveis.com.br",
        "WP_USER": "mcp-bot",
        "WP_APP_PASSWORD": "REPLACE_WITH_APPLICATION_PASSWORD",
        "_note": "Application Password formato: 'xxxx xxxx xxxx xxxx xxxx xxxx' (com espacos OU sem)"
      }
    }
  }
}
```

### D. `.env.mcp.example`

```env
# Variaveis para scripts de validacao MCP — Jurema Brokers
# Copiar para .env.mcp (gitignored) e preencher com valores reais
# NAO commitar .env.mcp

WP_URL=https://juremabksimoveis.com.br
WP_USER=mcp-bot
WP_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx
```

Importante: garantir que `.env.mcp` (sem `.example`) seja ignorado pelo git. Adicionar regra ao `.gitignore` se necessario:

```
# MCP credentials (jamais commitar)
.planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/.env.mcp
```

Verificar `.gitignore` raiz do projeto e adicionar a linha acima se ela ja nao cobre `.env*` em qualquer subdiretorio.
  </action>
  <verify>
    <automated>test -f .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh && test -f .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.ps1 && test -f .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/config/mcp-wordpress-config.example.json && test -f .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/.env.mcp.example && bash -n .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh && grep -q "wp-json/mcp/v1" .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh && ! grep -q "POST\|PUT\|DELETE\|PATCH" .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/scripts/test-mcp-endpoint.sh</automated>
  </verify>
  <done>4 arquivos criados (.sh, .ps1, .json, .env.mcp.example). Bash script passa em `bash -n` (syntax check). Nenhum script contem verbos de mutacao (POST/PUT/DELETE/PATCH). `.env.mcp.example` tem placeholder, nao credencial real. Regra de gitignore adicionada se necessario.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — usuario executa instalacao no WP Admin e roda script de validacao</name>
  <files>
    .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/VALIDATION.md
  </files>
  <what-built>
- INSTALL-WP-ADMIN.md (guia manual)
- scripts/test-mcp-endpoint.sh (bash) e .ps1 (PowerShell)
- config/mcp-wordpress-config.example.json (config MCP)
- .env.mcp.example (variaveis de ambiente)
  </what-built>
  <how-to-verify>
**Etapa A — Instalar plugin no WordPress (manual):**
1. Abrir `.planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/INSTALL-WP-ADMIN.md`
2. Seguir Parte 1 (instalar plugin mcp-wp/mcp-server)
3. Seguir Parte 2 (criar usuario `mcp-bot` com role Editor)
4. Seguir Parte 3 (gerar Application Password e salvar)
5. Seguir Parte 4 (verificar `/wp-json/` lista namespace `mcp/v1`)

**Etapa B — Configurar credenciais localmente:**
1. `cd .planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress`
2. `cp .env.mcp.example .env.mcp`
3. Editar `.env.mcp` e preencher `WP_APP_PASSWORD` com o token copiado da Etapa A
4. Confirmar que `.env.mcp` esta no `.gitignore` (rodar `git check-ignore .env.mcp`)

**Etapa C — Rodar script de validacao:**
- Linux/Mac/Git Bash: `bash scripts/test-mcp-endpoke.sh`
- Windows PowerShell: `./scripts/test-mcp-endpoint.ps1`

**Resultados esperados:**
1. "OK: namespace mcp/v1 presente"
2. "HTTP 200" + "OK: endpoint MCP autenticado responde 200"
3. "OK: endpoint protegido" (HTTP 401 ou 403 sem auth)

**Etapa D — Preencher VALIDATION.md:**
Apos rodar o script, o Claude (proxima execucao) ira criar `VALIDATION.md` com o checklist marcado e a saida do script. Por enquanto, o usuario apenas reporta:

- [ ] Plugin instalado e ativo? (sim/nao)
- [ ] Usuario `mcp-bot` criado com role Editor? (sim/nao)
- [ ] Application Password gerada? (sim/nao)
- [ ] Script test-mcp-endpoint retornou todos OK? (sim/nao)
- [ ] Caso falha: colar saida do script

**NAO testar nenhuma operacao de escrita (criar/editar post). Apenas leitura.**
  </how-to-verify>
  <resume-signal>Digite "validado" se todos os 4 checks passaram, OU descreva o erro encontrado (ex.: "404 em /wp-json/mcp/v1/", "plugin nao aparece em Plugins", etc.). Apos resume, o Claude criara VALIDATION.md consolidando o resultado.</resume-signal>
</task>

</tasks>

<verification>
Apos checkpoint resolvido:
- [ ] `INSTALL-WP-ADMIN.md` existe e cobre as 4 partes (plugin, usuario, Application Password, endpoint)
- [ ] `scripts/test-mcp-endpoint.sh` e `.ps1` existem e passam em syntax check
- [ ] Nenhum script contem POST/PUT/DELETE/PATCH (read-only garantido)
- [ ] `config/mcp-wordpress-config.example.json` existe com placeholders
- [ ] `.env.mcp.example` existe (e `.env.mcp` nao foi commitado)
- [ ] `VALIDATION.md` registra resultado da execucao manual
- [ ] Nenhum arquivo do YZI OS, Supabase ou modulo de imoveis foi modificado
</verification>

<success_criteria>
1. Plugin mcp-wp/mcp-server instalado e ativo em juremabksimoveis.com.br (verificado pelo usuario via WP Admin)
2. Endpoint `https://juremabksimoveis.com.br/wp-json/mcp/v1/` responde 200 com Application Password e 401/403 sem auth
3. Usuario `mcp-bot` (role Editor) e Application Password `mcp-server-claude` existem
4. Scripts de validacao executam sem erro e nao alteram conteudo
5. Toda a documentacao + scripts ficam em `.planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/`
6. `.env.mcp` (com credenciais reais) nao e commitado
</success_criteria>

<output>
Apos completar, criar `.planning/quick/260429-b3x-gsd-instalar-mcp-server-wordpress/quick-260429-b3x-SUMMARY.md` com:
- Resumo do que foi instalado (plugin + usuario + Application Password)
- Saida do script de validacao
- Como conectar Claude Desktop (referenciar config/mcp-wordpress-config.example.json)
- Proximos passos (NAO mexer em conteudo ainda — proxima task definira escopo de tools MCP autorizadas)
</output>
