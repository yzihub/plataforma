---
phase: quick-260506-j0p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cockpit/chat/page.tsx
  - src/layout/AppSidebar.tsx
autonomous: false
requirements:
  - INBOX-01
  - INBOX-02
  - INBOX-03
  - INBOX-04

must_haves:
  truths:
    - "Usuário acessa /cockpit/chat e vê lista lateral de conversas reais (tabela conversations) filtradas por tenant"
    - "Usuário clica em uma conversa e o painel direito mostra mensagens reais (tabela conversation_messages) ordenadas por created_at ASC"
    - "Mensagens inbound aparecem à esquerda, outbound à direita, com timestamps formatados"
    - "Usuário clica botão Pausar/Retomar Ju e o campo conversations.ai_paused é atualizado no Supabase"
    - "Indicador visual mostra Ju ativa vs pausada em cada item da lista lateral"
    - "Input de resposta humana aparece visualmente mas está desabilitado (sem endpoint seguro ainda)"
    - "Item Chat aparece no sidebar (seção CRM) e roteia para /cockpit/chat"
  artifacts:
    - path: "src/app/cockpit/chat/page.tsx"
      provides: "Tela Inbox/Chat completa: lista lateral + chat view + toggle pausar Ju + input desabilitado"
      contains: "createClient, useTenant, conversations, conversation_messages, ai_paused"
    - path: "src/layout/AppSidebar.tsx"
      provides: "Link Chat na seção CRM do sidebar"
      contains: "/cockpit/chat"
  key_links:
    - from: "src/app/cockpit/chat/page.tsx"
      to: "supabase.from('conversations')"
      via: "select com join leads(name, phone) filtrado por tenant_id, order by last_message_at DESC"
      pattern: "from\\(['\"]conversations['\"]\\)"
    - from: "src/app/cockpit/chat/page.tsx"
      to: "supabase.from('conversation_messages')"
      via: "select content, direction, sender_type, created_at filtrado por conversation_id, order by created_at ASC"
      pattern: "from\\(['\"]conversation_messages['\"]\\)"
    - from: "src/app/cockpit/chat/page.tsx"
      to: "supabase.from('conversations').update({ ai_paused })"
      via: "toggle handler que chama .update().eq('id', selectedConversationId)"
      pattern: "\\.update\\(\\s*\\{[^}]*ai_paused"
    - from: "src/layout/AppSidebar.tsx"
      to: "/cockpit/chat"
      via: "NavItem na seção CRM com ChatIcon (já importado)"
      pattern: "/cockpit/chat"
---

<objective>
Criar a tela Inbox/Chat da Ju em `/cockpit/chat` no padrão TailAdmin dark, conectada ao Supabase.

Purpose: Permitir que o time veja em tempo (semi-)real as conversas que a Ju está conduzindo no WhatsApp, com a capacidade de pausar a IA por conversa para fazer atendimento humano. A tela é somente leitura para mensagens — envio humano vem em uma próxima quick task quando houver endpoint seguro (n8n + Evolution API).

Output:
- `src/app/cockpit/chat/page.tsx` reescrito como Inbox completo (lista lateral + chat view + pausar Ju)
- `src/layout/AppSidebar.tsx` com entrada "Chat" na seção CRM
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/app/cockpit/chat/page.tsx
@src/lib/supabase/client.ts
@src/app/cockpit/crm/page.tsx
@src/layout/AppSidebar.tsx
@src/hooks/useTenant.ts

<interfaces>
<!-- Contratos chave que o executor já tem em mãos (sem precisar explorar codebase) -->

Supabase client (browser):
```typescript
// src/lib/supabase/client.ts
import { createBrowserClient } from '@supabase/ssr'
export function createClient(): SupabaseClient
// Usa NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY
// Lança erro se .env.local não estiver configurado
```

Tenant hook:
```typescript
// src/hooks/useTenant.ts (re-export de TenantContext)
export { useTenantContext as useTenant } from "@/context/TenantContext";
// Uso: const { tenant, loading } = useTenant();
// tenant.id é o tenant_id que filtra a query
// Fallback se necessário: process.env.NEXT_PUBLIC_JUREMA_TENANT_ID
//   ?? "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"
```

Schema esperado (assumido — sem migration nesta task):
```sql
conversations:
  id uuid PK
  tenant_id uuid
  lead_id uuid (FK leads.id, nullable)
  ai_paused boolean default false
  last_message text
  last_message_at timestamptz
  created_at timestamptz

conversation_messages:
  id uuid PK
  conversation_id uuid FK
  content text       -- NÃO usar 'body'
  direction text     -- 'inbound' | 'outbound'
  sender_type text   -- 'lead' | 'agent' | 'human'
  created_at timestamptz

leads:
  id uuid PK
  name text
  phone text
```

Padrão TailAdmin dark mode (já em uso no projeto):
```
Painel:        bg-white dark:bg-white/[0.03]
               border border-gray-200 dark:border-gray-800
               rounded-2xl
Texto fg:      text-gray-900 dark:text-white/90
Texto muted:   text-gray-500 dark:text-gray-400
Hover:         hover:bg-gray-100 dark:hover:bg-white/[0.04]
Item ativo:    text-brand-500 bg-brand-500/5
Brand button:  bg-brand-500 hover:bg-brand-600 text-white
```

Sidebar (AppSidebar.tsx):
- ChatIcon JÁ está importado em `../icons/index` (linha 26)
- Inserir entrada na seção CRM (índice 1 do array SECTIONS)
- Padrão de NavItem flat: `{ name: "Chat", icon: <ChatIcon />, path: "/cockpit/chat" }`
- Posicionar entre "Evolution" e "Calendario" (ou logo após "Evolution") para coerência
</interfaces>

<tailadmin_layout>
Layout alvo (referência da demo TailAdmin):

```
┌─────────────────────────────────────────────────────────────┐
│ Inbox / Chat da Ju            (header com título + subtítulo)│
├──────────────┬──────────────────────────────────────────────┤
│ Conversas    │  [Header conversa: nome + telefone + toggle] │
│ ┌──────────┐ │  ─────────────────────────────────────────── │
│ │ avatar   │ │                                              │
│ │ Nome     │ │   [msgs inbound — esquerda, bg cinza]        │
│ │ telefone │ │                                              │
│ │ "última…"│ │             [msgs outbound — direita, brand] │
│ │ 14:32 •  │ │                                              │
│ └──────────┘ │  ─────────────────────────────────────────── │
│ ...          │  [textarea desabilitada]    [Enviar (off)]   │
│ (~260px)     │  (flex-1)                                    │
└──────────────┴──────────────────────────────────────────────┘
```

Container externo: `flex h-[calc(100vh-7rem)] gap-4`
- Lista esquerda: `w-[280px] shrink-0 flex flex-col`
- Divider: `w-px bg-gray-200 dark:bg-gray-800`
- Chat direita: `flex-1 min-w-0 flex flex-col`
</tailadmin_layout>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever /cockpit/chat com lista de conversas + chat view + toggle pausar Ju</name>
  <files>src/app/cockpit/chat/page.tsx</files>
  <action>
Reescrever COMPLETAMENTE `src/app/cockpit/chat/page.tsx` como componente client-side conectado ao Supabase. Substituir o stub atual (50 linhas, sem dados).

REGRAS DURAS (do CLAUDE.md e task_requirements):
- 'use client' no topo
- Filtrar por `tenant.id` do `useTenant()` — fallback: `process.env.NEXT_PUBLIC_JUREMA_TENANT_ID ?? "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361"`
- Tabela de mensagens: `conversation_messages` (NÃO `messages`)
- Campo de mensagem: `content` (NÃO `body`)
- NÃO chamar n8n direto, NÃO usar service_role, NÃO calcular score/stage no front
- Sem mock se houver dado real — em caso de erro/empty, exibir empty state (não placeholder fake)

ESTRUTURA DO COMPONENTE:

1. Imports:
   ```tsx
   "use client";
   import { useState, useEffect, useMemo, useRef, useCallback } from "react";
   import { createClient } from "@/lib/supabase/client";
   import { useTenant } from "@/hooks/useTenant";
   ```

2. Tipos locais (TypeScript estrito — sem `any`):
   ```ts
   type Conversation = {
     id: string;
     tenant_id: string;
     lead_id: string | null;
     ai_paused: boolean;
     last_message: string | null;
     last_message_at: string | null;
     created_at: string;
     // join leads
     leads?: { id: string; name: string | null; phone: string | null } | null;
   };

   type Message = {
     id: string;
     conversation_id: string;
     content: string;
     direction: "inbound" | "outbound" | string;
     sender_type: "lead" | "agent" | "human" | string;
     created_at: string;
   };
   ```

3. Estado:
   - `conversations: Conversation[]`
   - `selectedId: string | null`
   - `messages: Message[]`
   - `loadingList: boolean`, `loadingMsgs: boolean`
   - `togglingPause: boolean`
   - `errorList: string | null`, `errorMsgs: string | null`

4. Helpers (definidos no topo do arquivo, fora do componente):
   - `getInitials(name: string): string` — primeiras 2 iniciais maiúsculas
   - `formatRelative(iso: string | null): string` — "agora", "14:32", "ontem", ou data curta pt-BR
   - `formatTime(iso: string): string` — `Date.toLocaleTimeString("pt-BR", { hour: "2-digit", minute: "2-digit" })`
   - `avatarColor(id: string)` — retorna classe Tailwind de bg deterministicamente (mesmo padrão do crm/page.tsx, array de 7 cores)

5. Effect de carregar lista de conversas:
   - Resolver `tenantId`: `tenant?.id ?? (process.env.NEXT_PUBLIC_JUREMA_TENANT_ID ?? "82cc7aa9-fc6e-4f37-8d8e-8a71c1691361")`
   - Aguarda `tenant !== undefined` (ou usa fallback se `loading` finalizou sem tenant)
   - Query:
     ```ts
     supabase
       .from("conversations")
       .select("id, tenant_id, lead_id, ai_paused, last_message, last_message_at, created_at, leads(id, name, phone)")
       .eq("tenant_id", tenantId)
       .order("last_message_at", { ascending: false, nullsFirst: false })
       .limit(100)
     ```
   - Em caso de erro do Supabase: setar `errorList` com `error.message` e `console.error("[ChatInbox] conversations error:", error.message)` — NÃO fazer fallback para mock.
   - Auto-selecionar primeira conversa se houver e `selectedId` ainda for null.

6. Effect de carregar mensagens (depende de `selectedId`):
   - Se `!selectedId`: limpar `messages` e retornar.
   - Query:
     ```ts
     supabase
       .from("conversation_messages")
       .select("id, conversation_id, content, direction, sender_type, created_at")
       .eq("conversation_id", selectedId)
       .order("created_at", { ascending: true })
       .limit(500)
     ```
   - Em erro: setar `errorMsgs`, `console.error`, `messages` = [].

7. Auto-scroll ao final quando mensagens mudam:
   - `messagesEndRef = useRef<HTMLDivElement>(null)`
   - `useEffect(() => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }); }, [messages])`

8. Handler `togglePause`:
   ```ts
   const selectedConv = conversations.find(c => c.id === selectedId);
   if (!selectedConv) return;
   setTogglingPause(true);
   const next = !selectedConv.ai_paused;
   const { error } = await supabase
     .from("conversations")
     .update({ ai_paused: next })
     .eq("id", selectedId);
   if (!error) {
     setConversations(prev => prev.map(c => c.id === selectedId ? { ...c, ai_paused: next } : c));
   } else {
     console.error("[ChatInbox] toggle pause error:", error.message);
   }
   setTogglingPause(false);
   ```

9. Layout JSX (TailAdmin dark, ~280px lista + flex-1 chat):

   Container raiz:
   ```jsx
   <div className="flex flex-col h-[calc(100vh-7rem)] gap-4">
     {/* Header */}
     <div className="shrink-0">
       <h1 className="text-xl font-bold text-gray-800 dark:text-white/90">Inbox / Chat da Ju</h1>
       <p className="text-xs text-gray-400 dark:text-gray-500">
         Conversas conduzidas pela Ju via WhatsApp · {conversations.length} conversa(s)
       </p>
     </div>

     {/* Body 280px + flex-1 */}
     <div className="flex flex-1 min-h-0 gap-0 rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[0.03] overflow-hidden">
       {/* Coluna esquerda — lista */}
       <aside className="w-[280px] shrink-0 flex flex-col border-r border-gray-100 dark:border-gray-800 min-h-0">
         {/* Cabeçalho lista */}
         <div className="px-4 py-3 border-b border-gray-100 dark:border-gray-800 shrink-0">
           <p className="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400">
             Conversas
           </p>
         </div>
         {/* Lista scrollável */}
         <div className="flex-1 overflow-y-auto">
           {/* loading | erro | vazio | itens */}
         </div>
       </aside>

       {/* Coluna direita — chat */}
       <section className="flex-1 min-w-0 flex flex-col min-h-0">
         {/* header da conversa, mensagens, input */}
       </section>
     </div>
   </div>
   ```

   Estados da lista esquerda:
   - `loadingList`: spinner centralizado (`w-6 h-6 rounded-full border-2 border-brand-500 border-t-transparent animate-spin`)
   - `errorList`: card vermelho com mensagem
   - `conversations.length === 0`: empty state ("Nenhuma conversa ainda")
   - itens: ver abaixo

   Item da lista (botão clicável):
   ```jsx
   <button
     key={conv.id}
     onClick={() => setSelectedId(conv.id)}
     className={`w-full text-left px-4 py-3 border-b border-gray-50 dark:border-gray-800/60 transition-colors flex gap-3 items-start ${
       selectedId === conv.id
         ? "bg-brand-500/5"
         : "hover:bg-gray-50 dark:hover:bg-white/[0.04]"
     }`}
   >
     <div className={`w-9 h-9 rounded-full flex items-center justify-center text-xs font-bold text-white shrink-0 ${avatarColor(conv.id)}`}>
       {getInitials(conv.leads?.name ?? conv.leads?.phone ?? "?")}
     </div>
     <div className="flex-1 min-w-0">
       <div className="flex items-center justify-between gap-2">
         <p className="text-sm font-medium text-gray-800 dark:text-white/90 truncate">
           {conv.leads?.name ?? conv.leads?.phone ?? "Lead sem nome"}
         </p>
         <span className="text-[10px] text-gray-400 shrink-0">
           {formatRelative(conv.last_message_at)}
         </span>
       </div>
       <p className="text-xs text-gray-500 dark:text-gray-400 truncate">
         {conv.leads?.phone ?? ""}
       </p>
       <div className="flex items-center justify-between gap-2 mt-1">
         <p className="text-xs text-gray-500 dark:text-gray-400 truncate">
           {conv.last_message ?? "—"}
         </p>
         {/* Badge Ju ativa/pausada */}
         <span className={`inline-flex items-center gap-1 text-[10px] font-medium px-1.5 py-0.5 rounded-full shrink-0 ${
           conv.ai_paused
             ? "bg-amber-100 text-amber-700 dark:bg-amber-500/15 dark:text-amber-400"
             : "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-400"
         }`}>
           <span className={`w-1.5 h-1.5 rounded-full ${conv.ai_paused ? "bg-amber-500" : "bg-emerald-500"}`} />
           {conv.ai_paused ? "Pausada" : "Ju ativa"}
         </span>
       </div>
     </div>
   </button>
   ```

   Coluna direita — sem conversa selecionada (empty):
   ```jsx
   <div className="flex flex-col items-center justify-center h-full text-center gap-3 select-none p-6">
     <div className="w-12 h-12 rounded-full bg-gray-100 dark:bg-gray-800 flex items-center justify-center">
       <svg className="size-6 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
         <path strokeLinecap="round" strokeLinejoin="round" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
       </svg>
     </div>
     <p className="text-sm font-medium text-gray-500 dark:text-gray-400">Selecione uma conversa</p>
     <p className="text-xs text-gray-400 dark:text-gray-500 max-w-[260px]">
       Clique em qualquer conversa à esquerda para ver as mensagens e gerenciar o atendimento da Ju.
     </p>
   </div>
   ```

   Coluna direita — com conversa selecionada:
   ```jsx
   {/* Header da conversa */}
   <div className="flex items-center gap-3 px-5 py-3 border-b border-gray-100 dark:border-gray-800 shrink-0">
     <div className={`w-10 h-10 rounded-full flex items-center justify-center text-sm font-bold text-white shrink-0 ${avatarColor(selectedConv.id)}`}>
       {getInitials(selectedConv.leads?.name ?? selectedConv.leads?.phone ?? "?")}
     </div>
     <div className="flex-1 min-w-0">
       <p className="text-sm font-semibold text-gray-800 dark:text-white/90 truncate">
         {selectedConv.leads?.name ?? "Lead sem nome"}
       </p>
       <p className="text-xs text-gray-500 dark:text-gray-400 truncate">
         {selectedConv.leads?.phone ?? "—"}
       </p>
     </div>
     {/* Toggle pausar/retomar Ju */}
     <button
       onClick={togglePause}
       disabled={togglingPause}
       className={`flex items-center gap-2 rounded-xl px-3 py-1.5 text-xs font-medium transition-colors disabled:opacity-50 ${
         selectedConv.ai_paused
           ? "bg-amber-500/10 text-amber-700 dark:text-amber-400 hover:bg-amber-500/15"
           : "bg-emerald-500/10 text-emerald-700 dark:text-emerald-400 hover:bg-emerald-500/15"
       }`}
       title={selectedConv.ai_paused ? "Retomar Ju" : "Pausar Ju"}
     >
       <span className={`w-1.5 h-1.5 rounded-full ${selectedConv.ai_paused ? "bg-amber-500" : "bg-emerald-500"}`} />
       {selectedConv.ai_paused ? "Ju pausada — Retomar" : "Ju ativa — Pausar"}
     </button>
   </div>

   {/* Mensagens */}
   <div className="flex-1 overflow-y-auto px-5 py-4 space-y-3 min-h-0">
     {loadingMsgs ? (
       <div className="flex items-center justify-center h-full">
         <div className="w-6 h-6 rounded-full border-2 border-brand-500 border-t-transparent animate-spin" />
       </div>
     ) : errorMsgs ? (
       <div className="text-center text-sm text-red-500 py-8">{errorMsgs}</div>
     ) : messages.length === 0 ? (
       <div className="flex flex-col items-center justify-center h-full gap-2 text-center select-none">
         <p className="text-xs text-gray-400 dark:text-gray-500">Nenhuma mensagem nesta conversa.</p>
       </div>
     ) : (
       messages.map((m) => {
         const isInbound = m.direction === "inbound" || m.sender_type === "lead";
         return (
           <div key={m.id} className={`flex ${isInbound ? "justify-start" : "justify-end"}`}>
             <div className={`max-w-[75%] px-4 py-2.5 text-sm shadow-sm rounded-xl ${
               isInbound
                 ? "bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-200 rounded-tl-sm"
                 : "bg-brand-500 text-white rounded-tr-sm"
             }`}>
               <p className="leading-relaxed whitespace-pre-wrap break-words">{m.content}</p>
               <p className={`text-[10px] mt-1 ${isInbound ? "text-gray-400" : "text-white/60"}`}>
                 {formatTime(m.created_at)}
                 {m.sender_type === "human" && !isInbound ? " · humano" : ""}
                 {m.sender_type === "agent" && !isInbound ? " · Ju" : ""}
               </p>
             </div>
           </div>
         );
       })
     )}
     <div ref={messagesEndRef} />
   </div>

   {/* Input desabilitado (preparado p/ envio humano) */}
   <div className="flex items-center gap-2 px-5 py-3 border-t border-gray-100 dark:border-gray-800 shrink-0">
     <textarea
       rows={1}
       disabled
       placeholder="Envio humano em breve — em desenvolvimento"
       className="flex-1 resize-none rounded-xl border border-gray-200 bg-gray-50 px-3 py-2 text-sm text-gray-400 placeholder-gray-400 outline-none cursor-not-allowed dark:border-gray-700 dark:bg-gray-800/50 dark:text-gray-500 dark:placeholder-gray-500"
     />
     <button
       type="button"
       disabled
       title="Em breve"
       className="rounded-xl bg-brand-500 px-4 py-2 text-sm font-semibold text-white opacity-40 cursor-not-allowed"
     >
       Enviar
     </button>
   </div>
   ```

10. Computar `selectedConv` via `useMemo`:
    ```ts
    const selectedConv = useMemo(
      () => conversations.find(c => c.id === selectedId) ?? null,
      [conversations, selectedId]
    );
    ```

NÃO IMPLEMENTAR (escopo deliberadamente limitado):
- Realtime/subscription (Supabase realtime) — fica para próxima quick task
- Envio humano de mensagem — sem endpoint seguro definido
- Pesquisa/filtro na lista
- Paginação infinita
- Indicador de não-lidas
- Schema migrations (assume tabelas já existem; se não existir, erro Supabase aparecerá no console e empty state aparecerá — comportamento aceitável)

Em caso da tabela `conversations` ainda não existir, o componente deve renderizar erro elegante (via `errorList`) sem crash — o `error.message` do Supabase já fornece string informativa.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- Arquivo `src/app/cockpit/chat/page.tsx` reescrito (componente client com lista + chat + toggle pausar)
- `npx tsc --noEmit` passa sem erros novos no arquivo modificado
- Imports corretos: `createClient` de `@/lib/supabase/client`, `useTenant` de `@/hooks/useTenant`
- Tabela `conversation_messages` (não `messages`) e campo `content` (não `body`) usados
- Filtro por `tenant.id` ativo (com fallback para JUREMA_TENANT_ID)
- Layout TailAdmin dark (bg-white dark:bg-white/[0.03], rounded-2xl, brand-500)
- Toggle de `ai_paused` chama `supabase.from("conversations").update({ ai_paused }).eq("id", ...)`
- Input/botão desabilitados com texto "Em breve"
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar item "Chat" no AppSidebar (seção CRM)</name>
  <files>src/layout/AppSidebar.tsx</files>
  <action>
Adicionar uma única entrada de navegação no array `SECTIONS` em `src/layout/AppSidebar.tsx`.

LOCALIZAÇÃO EXATA: dentro da seção CRM (`SECTIONS[1]`, `label: "CRM"`), inserir o NavItem `Chat` LOGO APÓS o item `Evolution` (linha ~90: `{ name: "Evolution", icon: <ChatIcon />, ... }`).

PROBLEMA: hoje o `ChatIcon` está sendo usado em "Evolution". Para manter ambos com ícones distintos sem reestilizar:
- Manter "Evolution" com `ChatIcon` (já está assim — não mexer).
- Para "Chat", também usar `ChatIcon` (já importado de `../icons/index` na linha 26 — não precisa adicionar import).
  Embora os dois fiquem com mesmo ícone, são itens distintos com labels e paths diferentes; isso é aceitável para v1 e mantém o diff cirúrgico. Ícone customizado pode ser trocado em iteração futura.

INSERÇÃO EXATA — adicionar a linha:
```tsx
{ name: "Chat",            icon: <ChatIcon />,        path: "/cockpit/chat" },
```

DEPOIS de:
```tsx
{ name: "Evolution",       icon: <ChatIcon />,        path: "/cockpit/evolution" },
```

E ANTES de:
```tsx
{ name: "Calendario",      icon: <CalenderIcon />,    path: "/cockpit/calendario" },
```

NÃO MEXER:
- Em mais nada do arquivo
- Em nenhum import (ChatIcon já importado)
- Em outras seções
- Na lógica de `submenuKey` / `children` / plan gating

Diff esperado: +1 linha, dentro do array SECTIONS[1].items.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `src/layout/AppSidebar.tsx` contém entrada `path: "/cockpit/chat"` no array SECTIONS
- Item está dentro da seção CRM, entre Evolution e Calendario
- `npx tsc --noEmit` passa sem erros
- Nenhuma outra alteração no arquivo (diff = +1 linha)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação visual da tela /cockpit/chat</name>
  <files>(verificação humana — sem arquivos)</files>
  <action>Validar visualmente a tela /cockpit/chat conforme detalhado em <how-to-verify> abaixo.</action>
  <verify>Validação humana — passos detalhados em <how-to-verify></verify>
  <done>Usuário aprova com "approved" ou reporta problemas específicos.</done>
  <what-built>
Tela Inbox/Chat da Ju em `/cockpit/chat` (TailAdmin dark) com:
- Lista lateral esquerda (~280px) de conversas reais do Supabase, filtradas por tenant Jurema, com nome do lead, telefone, última mensagem, timestamp e badge Ju ativa/pausada.
- Chat view direita: mensagens reais da tabela `conversation_messages` ordenadas cronologicamente, inbound à esquerda (cinza), outbound à direita (brand-500).
- Botão Pausar/Retomar Ju no header da conversa que atualiza `conversations.ai_paused`.
- Input de resposta humana visualmente preparado mas desabilitado ("Em breve").
- Item "Chat" no sidebar (seção CRM, entre Evolution e Calendario).
  </what-built>
  <how-to-verify>
**Setup:**
1. `rtk npm run dev` (ou equivalente do projeto)
2. Acessar a aplicação no navegador autenticado como tenant Jurema Brokers (`82cc7aa9-fc6e-4f37-8d8e-8a71c1691361`).

**Sidebar:**
3. Verificar que o item "Chat" aparece no menu lateral, na seção CRM, logo abaixo de "Evolution".
4. Clicar em "Chat" — URL deve ir para `/cockpit/chat` e item deve ficar destacado (brand-500).

**Tela /cockpit/chat:**
5. Header "Inbox / Chat da Ju" com contagem de conversas correta.
6. Lista lateral à esquerda:
   - Se a tabela `conversations` existe e tem dados do tenant Jurema → ver itens com avatar colorido, nome do lead (ou telefone se sem nome), telefone, última mensagem, timestamp e badge "Ju ativa" (verde) ou "Pausada" (âmbar).
   - Se a tabela existe mas está vazia → empty state "Nenhuma conversa ainda" (sem crash).
   - Se a tabela NÃO existe ainda → mensagem de erro elegante (não tela branca).
7. Primeira conversa deve estar auto-selecionada com fundo `brand-500/5`.

**Chat view (direita):**
8. Header da conversa mostra avatar grande, nome, telefone e botão "Ju ativa — Pausar" (ou "Ju pausada — Retomar").
9. Mensagens carregadas:
   - Inbound (sender_type='lead' ou direction='inbound') à ESQUERDA com bg cinza.
   - Outbound (agent/human) à DIREITA com bg brand-500.
   - Timestamps formatados (HH:MM pt-BR) em cada bolha.
   - Marcador "· Ju" ou "· humano" em mensagens outbound conforme `sender_type`.
   - Scroll automático até o final ao carregar.
10. Clicar em outra conversa na lista → mensagens trocam corretamente.

**Toggle Ju pausada:**
11. Clicar no botão "Ju ativa — Pausar" no header da conversa.
12. Botão muda para "Ju pausada — Retomar" (cor âmbar).
13. Badge na lista lateral correspondente também muda para "Pausada" (âmbar).
14. Recarregar a página — estado persiste (confirma que escreveu no banco).
15. Clicar novamente para retomar — volta a "Ju ativa" (verde).

**Input desabilitado:**
16. Textarea no fundo da chat view está visivelmente desabilitado, com placeholder "Envio humano em breve — em desenvolvimento".
17. Botão "Enviar" está com opacidade reduzida e cursor not-allowed.

**Console:**
18. Nenhum erro vermelho no DevTools console (warnings de React Strict Mode são aceitáveis).

**Caso a tabela `conversations` ainda não exista no Supabase:**
- Aceitável: erro registrado no console + empty/error state na UI. Reportar e seguiremos com migration em quick task separada.
  </how-to-verify>
  <resume-signal>Digite "approved" se tudo OK, ou descreva problemas (ex: "lista vazia mas tabela tem dados", "toggle não persiste", "layout quebrado em mobile")</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sem novos erros
- Build Next.js não quebra (`rtk npm run build` opcional, mas recomendado se tempo permitir)
- Rota `/cockpit/chat` renderiza componente real (não mais o stub)
- Sidebar exibe item Chat
- Validação humana confirma fluxo end-to-end com dados reais do Supabase (Task 3)
</verification>

<success_criteria>
- `src/app/cockpit/chat/page.tsx` reescrito como Inbox funcional, conectado ao Supabase
- `src/layout/AppSidebar.tsx` com link Chat na seção CRM
- Lista de conversas reais do tenant Jurema visível
- Mensagens reais ordenadas cronologicamente, inbound/outbound bem distinguidas
- Botão pausar/retomar Ju atualiza Supabase e UI
- Input humano visualmente preparado mas inerte
- Sem violações do CLAUDE.md (sem service_role, sem n8n direto, sem regra de negócio duplicada, sem cálculo de score/stage no front)
- Validação humana aprovada (Task 3)
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260506-j0p-criar-tela-inbox-chat-da-ju-no-padrao-ta/260506-j0p-SUMMARY.md` com:
- Arquivos modificados e diff resumo
- Estado das tabelas `conversations`/`conversation_messages` encontrado em runtime (existem? têm dados?)
- Próximos passos sugeridos:
  - Migration para `conversations`/`conversation_messages` se ainda não existirem
  - Realtime/subscription para atualização live
  - Endpoint seguro para envio humano (POST /api/chat/send → n8n → Evolution API)
  - Filtros e paginação na lista
- Como testar (URL, tenant esperado)
</output>
