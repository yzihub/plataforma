---
quick_id: 260408-3os
description: Corrigir workflow Ler Imóveis JetEngine para YZI Supabase via HTTP Request
date: 2026-04-08
status: planned
---

# Quick Plan 260408-3os

## Goal

Gerar patch corrigido para o workflow "Ler Imoveis JetEngine" no n8n:
- Substituir node nativo Supabase (que usa credencial "JUREMA") por node HTTP Request
- HTTP Request chama REST API do Supabase YZI diretamente com service role key
- Upsert na tabela `imoveis` com conflict em `tenant_id,id_imovel`
- tenant_id fixo da Jurema Brokers hardcoded

## Context

O patch anterior (260408-2i3) usou `n8n-nodes-base.supabase` que:
1. Exige credencial "Supabase JUREMA" configurada no n8n
2. Usa banco errado (credencial Jurema aponta para outro projeto)

A solução correta usa HTTP Request com a URL/key do banco YZI diretamente.

Credenciais YZI (de .env.local):
- URL: https://dwmbklfkrtumfaxrbxio.supabase.co
- SERVICE_ROLE_KEY: REDACTED_SUPABASE_SERVICE_KEY

## Tasks

### Task 1 — Gerar patch HTTP Request para workflow Ler Imoveis JetEngine

**Action:** Criar arquivo JSON de referência com configuração completa do node HTTP Request para substituir o node Supabase nativo.

**Files:**
- `.planning/quick/260408-3os-corrigir-workflow-ler-im-veis-jetengine-/workflow-patch-ler-imoveis-http.json` (criar)

**Verify:** Arquivo JSON criado com:
- type: `n8n-nodes-base.httpRequest`
- método POST
- URL com `?on_conflict=tenant_id,id_imovel`
- headers: apikey, Authorization Bearer, Content-Type, Prefer
- body com os 18 campos mapeados
- tenant_id fixo Jurema

**Done:** JSON criado e validável via `cat` + verificação de campos obrigatórios
